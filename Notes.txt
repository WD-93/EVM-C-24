The most important things to do now:
* Fix bugs:
 Restrict main to () -> ()
 Add a kind check to types
* Broaden the pipeline by supporting new features:
 Storage, external contract state and tstore, with attendant virtual variables
 Type synonyms
 User-defined types (data ...); converted to word ops in IR
  dynSizeof for tagged unions
 Full primop support:
  All appropriate C operators, esp .field and *_
 Module support:
  Generate linkable object files without a main
  In-language support for manipulating code objects:
   Load, link, set immutables
 Static data beyond functions: 
  Strings, in-code arrays, globals, immutables
  Interpreter support: 
   Generate JTs and dispatch code from instruction set spec,
   with support for different CCs (ex: add using immediate argument or popped from interpreter stack)
   Scripts as first-class objects, at least in a language above EVMC
 Speculative: template polymorphism (implement in a language above EVMC?), Hindley-Milner, closures
 Ex use: replicate Sol's sha3: sha3(Ptr a) := 
* Libraries: Solidity ABI support
* External: parsing and ppr of EVMC returndata based on the module's interface
  Map function pointers to named functions...
To put on the backburner:
Performance opts:
 Allow reordering of read ops by changing how their memory effect is represented.
  It should be possible to eliminate an irrelevant read, so it should perhaps not produce a substate...
  Instead recognize the read won't be placeable after its memory is written, so implement a barrier in code
  scheduling preventing write placement before all reads?
 Opt call by breaking out pushReturn into a separate op and giving call the args $mem,f,ret,argWords
 Unnecessary jumpdest elimination
 DFS-R2L op ordering
 Live computation on CFG rather than IR to allow updating
 Constant expansion
 Common subexpression elimination, with code-optimized computation of related constants from other constants
  (ex: mask 255 => mask 128 by shr)
 Recognize copies on stack, don't pass as multiple words between SLCs when avoidable
 Dataflow analysis, eliminate live but irrelevant vars
 Demand-driven stack opt across SLCs, flexible layout selection using constraints
 Use of branch probability in optimizing jumpi stack shuffling; transfer cost to the unlikely branch
  Exec count and loop count annotations, exec cost vs code size optimization
  Optimization taking code load cost into account
 Function inline placement and full inlining, tree shaking of static data
****

Interesting idea: user access to the IR level or lower. Access to the nth word of a value...
User-defined primfun overloading. Defining features in terms of compile-time combinators in the language itself
could let you simplify the compiler.
What levels to provide access to? Control over data placement for generating JTs and static data in function
defs would be nice.
_+_ : (Int s n, Int s n) -> Int s n
_+_ (x,y) = add(x,y) & mask n
& mask 256 can be optimized to id
An int _ 0 has a 0-word repr.
Direct access to C var words could include assignment... and $mem.
By setting $mem to {}, you can disable implicit memory operations.

Inline decls

What information does the C module need? A map name -> D isn't enough...
Decl types: defun, tysyn, data, global, static data.
Break out imports and exports, put them in the module header?
module NM (exports) where {(import Mod [impList])*; decls}?
The simplest approach is to just concatenate the decls, but in future it would be desirable to build smart
contracts from objects stored on-chain.
Convert object files to a static and dynamic component; name data could be stored off-chain, but offsets
need to be dealt with dynamically.
Static data: immutable constants, arrays (with len)
Strings => "foo" => codePointer Byte string.foo [len] = {'f','o','o'}
'c' => an integer literal
Array initializers are distinct from normal exprs and must become valid assembly; they may contain constants
(including tuples and structs) and values that resolve to labels or constants (such as functions, array
pointers and array sizes).
Datatype tags could have the same restrictions, so a bytecode datatype could be tagged with jump addresses.
Problem: for a variable-sized datatype, where should the tag be? If it's first, you can pack values easily but
getting the tag on stack would be tricky.
String's auto-allocation of static data could be generalized: staticData [e1,e2,...]
Sol's overloading of strings to Bytes32 is nice, but the C type system doesn't support it... one could add
special syntax for it, or leave it to a higher-level language.

Support for asm in static data might be useful... esp for JTs.
Support for translation from safe to unsafe bytecode... perhaps don't put that in the lang.

Interpreter support: each instruction takes the interpreter state...
imm (f : (s,t) -> s) => pop t off the PC, tail call to f.
You could convert return () to a dispatch tail call.

Now: build a proper Module. To export data and type, I need to save them in an interface.
To partially apply Ptr, I need to simplify T to Con, var, nat, :$$. What do I need var for? Parameterized
datatypes, even without polymorphism.
Kinds: Type (including virtual values?), Region, Nat, k->k.
Virtual values are special: unlike 0-size stack types, they produce a var in the IR, but the var is not on
the stack.
Mem reads create a soft before requirement: they must be placed before the next write, but only if the
output of the read is used.
data List r a = Nil | Cons a (Ptr r (List a))
Ptr Calldata, Memory, Storage have a deref; Code, Returndata do not.
data ExtCodePtr a = ECR (Address,UInt 16)

coerce should just mask; unsafeCoerce should ignore masking, letting you construct values from a tuple of
words.

I traverse the CST in the desugaring phase... would it be advisable to do the scan for liftable exprs such as
strings, anonymous static data and anonymous functions there?
It's worth a try; lifting could be considered desugaring, and I do a typecheck when generating the
IR.

TySyn handling in IR: check for cycles in the tysyns, then expand all types in the module.
To represent the RHS of tysyns, I need to generalize T. Sweet, I'd already done so!
Potential errors: non-fully-applied type synonym, cycle.
I can also do the kind check at the same time.
Predefined types: "->" (which has an ambiguous kind), Type (Type :: Type), the regions, Ptr
Struct can still be its own constructor, to avoid having to mess with Symbol and padding.
I can delay the kind check until monomorphization... can I do the same for datatypes?
Problem: even if I restrict constructors to take a single argument, I must still infer the result type by
unification.
The reverse must be done for case, but you can check all vars in data decl rhses are in scope when
verifying the data.
Problem: can I get nontermination even with a passing cyclicality check since I defer the kind check?
Consider type FixedPoint f = f f, FixedPoint FixedPoint.
Partially applied type synonyms are not a valid parameter.
The presence of a DoS attack on the compiler isn't that serious, since you can get exponential blowup with
type Pair a = (a,a) anyway. If you're compiling for an untrusted party, you should containerize and
resource limit the compiler.

A better cycle finding algo: memoize a map from referenced ks to the shortest path through which it's
accessible. Memoize the length as well for O(1) comparison.
FW: the traversal algo should be a hof.
Goal: find the (first) shortest path with a repetition, so it should x,y,z,y should fail in the recursive
call to y and report [y,z,y].

TODO expose the other parser functions in E for easier debugging.
The prologue is parameterized by the main function; the compiled artifact of a C module needn't include it...

Buggy cases:
printCFG2 "module {type Pair a = (a,a); main:Pair Word->Word;main (x,y) := return (y|x)}"
printCFG2 "module {type Pair a = (a,a); main:Pair Word->Pair Word;main (x,y) := return (y,x)}"
printCFG2 "module {type Pair a = (a,a); main:Pair Word->Pair Word;main (x,y) := return (1,2)}"

Why does main (x,y) := x and y give the same result?
printIRM "module {main:(Word,Word,Word)->Word;main (x,y,z) := return z}"
The pattern matching is binding each var to the entire tuple!? --FIXED

printCFG2 "module {main:(Word,Word)->Word;main (x,y) := return (x&(y,y))}"
printCFG2 "module {main:(Word,Word)->Word;main (x,y) := return (x&(y,x))}"
--Give the same result - but x should be and-ed with the lower word, not the upper one!

*Compiler> printIRM "module {main:(Word,Word)->Word;main (x,y) := return (x&(x,y))}"
main(arity 2):
 $anon2 : uint256#1 = copy $anon0
 $anon3 : uint256#1 = copy $anon1
 x#1 : uint256#1 = copy $anon2
 y#1 : uint256#1 = copy $anon3
 $anon4 : uint256#1 = copy x#1
 $anon5 : uint256#1 = copy x#1
 $anon6 : uint256#1 = copy y#1
 $anon7 : (uint256, uint256)#1 = copy $anon5
 $anon8 : (uint256, uint256)#2 = copy $anon6
 $anon9 : (uint256, (uint256, uint256))#1 = copy $anon4
 $anon10 : (uint256, (uint256, uint256))#2 = copy $anon8 --!??
 $anon11 : (uint256, (uint256, uint256))#3 = copy $anon7
 $anon12 : uint256#1 = and $anon9, $anon11
 return $mem, $ret, $anon12
 $anon13 : uint256#1 = 0
 $anon14 : uint256#1 = copy $anon13
 return $mem, $ret, $anon14

*Compiler> printCFG2 "module {main:()->(Word,Word);main () := return ((1,2)&())}"
*** Exception: IR1.hs:(916,3)-(918,56): Non-exhaustive patterns in case
--Oh no, a bug in the tuple construction logic? Yes, return (1,()) fails too.
return (1,(2,3)) shows the problem

Rewriting struct construction logic:
Redesign padding and alignment? Bitfields could be a separate type...? Or a type of field.
A struct is a concatenation of values and padding bits... to access a field efficiently, it
needs to be byte-aligned. A word-aligned, word-padded field is the most efficient to access.
*ptr fits under prefixOp.
The IR can't distinguish between prefix and infix ops without a hint provided by the desugar step...
limit prefix ops to appropriate primops? *, +, -, maybe & (for exposing global ptrs).
p->field* is a ptr, stackWord->field* a slice.

Alternative approach: the bitsize of a struct is the sum of the bitsizes of fields + paddings.
Its wordsize can be computed from that.
struct[offset] = e --first split e into words: struct[off] = w.
Split that into struct words: structW |= w shift k
Use a map to accumulate the struct word => field word mapping.

Idea: padding annotations as syntactic sugar, with the T simply including padding sizes.
That won't work: size information of types is only known in the IR stage. It also wouldn't work with
overloading.
Useful overloaded transformations: pack (zeroing all padding), unpack to tuple.
Note such views have no runtime cost for unused words.
Word alignment and padding means cheaper stack access; for memory, only byte alignment and word padding
matter.
A struct is a multi-word value with dependencies on input values... what if you passed around symbolic value
refs rather than words? Then you could query them symbolically and perform opts that way.
Problem: if e = x + y, modifying x between e's definition and use could be buggy. You must still create an
output var for e to uniquely identify it.
Not worth it for now.

TODO prune old struct building code
Inefficiency: in 1|(2,3), the result is masked by 255 because the compiler doesn't recognize that the
"potentially corrupting" bits of (2,3) are padding, which may be assumed to be 0.
TODO improve corruptibility detection logic.

Allow align and pad byte despite them being defaults.
Make multiple pad/align pragmas a desugar error.

Renaming BitPad etc to Bit etc to reflect pad/align not just being about padding.
A useful overloaded op for EVMC: the nth word.

Accessing struct fields: compile .field* to a single slice. Make use of padding to avoid masking.
TODO: treat global or *ptr .field* specially; ditto for assignment.
e.slice(off,len) needn't be a new primitive; just implement in terms of struct coercion.

Next: .field* and .field* = e

****

First: failing while loop, main () = main () failure. Perhaps all fun calls fail?
 compileBranch fails because $mem[1] is of unknown type.
 Is $mem[0] given type Mem? I think so.
 Do mem ops not assign $mem a type? Perhaps I filter too early... yes, fixed.
 Call code is still inefficient, doing an unnecessary swap; TODO add DFS-R2L of ops and break out push ret
 into a separate op. Tail calls are not just calls with ret = $ret, since you need to clean up the stack
 first.
 main x := main x now compiles, but the code for calling main is so inefficient... it first pushes main, then
 permutes to get main,ret1,anon0 on top.
 It starts with $ret,anon0, where anon0 is last-use... I should first put anon0 (a suffix of the rhs) on top,
 then perform the prerequisite push ret1 and main ops.
 That requires more than just R2L-order op codegen, I also need to do nested op handling, considering what
 stack future ops need to see. Too complicated for now, just debug...
 DFS-R2L should still be a top prio, 1 + 2 + 3 just shouldn't be inefficient.
 Then: add ret to call args, break out op. But where to do that?
 Then: add more primops
 Lack of opt across SLCs (even when you fall through via a unique edge) is inefficient... but why wasn't the
 1-edge eliminated earlier? Presumably because it was newly generated by Stack to handle the jumpi... how to
 mix fallthrough calc and CFG opt?
 When code you continue to is shared but short, it may be worth copying to opt better... but need an opt
 choice monad.

Type synonyms: first just check for cycles.
 type Con args = t
 Defer kind check until after substitution.
 Where do you encounter types in exprs? Only coerce.
A simple module system: just concatenate all decls.
Global layout could be unspecified... except for calldata. The only requirement for a global is that its
value is consistent

How to handle __end__ if you have multiple modules? Just have a single global __end__ at
first, no dynamic linking.
Code objects could take modules first... but a compilation mode without $ret on the stack might be useful for
piecing together snippets.

Recompute live from graph; use SCC from Data.Graph.

****

Side-effecting code implicitly consumes and produces a state (ex: $mem, which represents all memory).
FW: fine-grained representation of mutable state a la Rust's borrow checker, first-class support for virtual
values (ex: store exclusive access to a list's elements in the list structure).
__mem = mv; --let's say this only contains a read-only borrow of ptr p1
*p2     --this should fail
*p1 = x --this should also fail
Fine-grained limitation of effects: CALL, state read/write, divergence and contract call exit.

****

Bugs:
*Compiler> pipeline2Bytecode "module {main:()->();main _ := main()}"
Left (AsmError "return v has no type in compileBranch: (1,\"$mem\")")
*Compiler> pipeline2Bytecode "module {main:()->Int Unsigned 8;main _ := return 0}"
*** Exception: Stack.hs:(854,1)-(857,17): Non-exhaustive patterns in function swapF'

*Compiler> pipeline2Bytecode "module {main:Int Unsigned 8->();main x := return ()}"
*** Exception: Stack.hs:(854,1)-(857,17): Non-exhaustive patterns in function swapF'

*Compiler L> printAsm "module {main:Int Unsigned 8->Int Unsigned 8;main x := main x}"
*** Exception: Compiler error: usesRemaining of nonexistent var (1,"$anon3")
CallStack (from HasCallStack):
  error, called at .\Stack.hs:968:16 in main:Stack

Fixed: soSwapIndex didn't handle 0 correctly.
TODO make my functions complete and add ExceptT to all my monads so errors are easy to locate.
For main _ := return 0 and main x := return (x+x) it seems to return correct and even efficient code, but...
Bug: an add of uint8s should include a mask
Any old word seems to be accepted as a param for Int, e.g. Unisgned...
Off-by-1 in dup.

The asm now looks correct for main x := if x then return 1 else return 0 end, but
($ret,$anon0)
swap1, pop
push 0,
swap1, jump
(spread across two SLCs) is not optimal; it could be simplified to push 0, swap 2, pop, jump.
My greedy layout setting is the problem: ideally I would work backwards from the point where stack is fixed.
Also TODO: eliminate unnecessary jumpdests.

"module {main:Int Unsigned 8->Int Unsigned 8;main x := return 0}"
generates efficient bytecode (0,swap,pop,jump), but that's because it's 1 SLC.

1 + 2 + 3 does not generate efficient code (3,2,1,add,add). The big question: do I reverse the eval order of
tuple elements to reflect their order of being pushed to the stack, or do I implement treegraph with DFS
(moving left-to-right in nodes)?
Choice 1 introduces a gotcha if you're modifying memory in the argument subexprs, while choice 2 would lead
to inefficient code (additional swapping) if you do.
The latter is preferable; the compiler should be able to generate efficient code even if you have intuitive
ordering of memory accesses.
Rather than treegraph, I'll try just DFS (left-to-right).

Cleanup:
Create a consistent naming scheme for boilerplate getters and setters, such as
monadTypeGetFieldName. getLive => stackSomething
StackOps and SelectOps are related... maybe make them datatypes, then derive their transformer instances
and put their shared functions in a typeclass.

I generate code for arguments in left-to-right order, rather than right-to-left... so with my naive
instruction ordering, x + 1 will require a swap.
Fixes: treegraph ordering with right-to-left the default for trees.
Pick cheapest (shortest) dup/swap sequence for commutative ops. Do that in the treegraph op ordering algo?
That's exp-time...

What do I need to do to pass multiple synonymous vars between SLCs in a single stack word?
Better CFG analysis... recompute live from a CFG, identify synonymous vars.
Even better than 1 stack word: identify constants, pass in 0 words.

You save 11 gas falling through in an else branch... assuming 50/50 branch probability it's well worth
spending 3 negating the cond. However, that's only if there's stack adjustment to do.
Before the jumpi, adjust to delete any garbage. Target: the union of the two maybe-layouts.
If the two live sets are equal and the layouts can be unified (either because they're both equal or one is
free), then adjust to that and branch. Goal: cond:layout.
FW: branch probability annotations.

selectOps takes ops and a live set and returns a list of stack ops, resulting in a layout. It may include
garbage, since at least the last op may leave some. If you dup in earlier ops without GC you can have even
more...
selectOps must also be given an initial layout.
Divide by branch:
jump l: 
There are three cases: 
 1) l is unplaced, so fall through and use 1) the current layout if 1-edge, 2) filter so only live vars are
 included, then set the layout to the current.
 Note CFG opt *should* have removed every 1-edge jump.
 2) l is placed and has been fallen through to; adjust to its layout and jump.
 3) l is placed and has not been fallen through to; adjust to its layout and fall through.
I don't need to build lists of SLCs, just record which SLC they fall through to if any.
Ignore truly empty SLCs for now.
return vs:
 ez, adjust to vs
jumpi v th el:
 There's a tradeoff... if th < el but el is fallthroughable...
 Cost of iszero: 3
 Cost of additional jump: 12 (push, jump, jumpdest)
A jumpi then branch with edge 1 should have a jumpdest, but a fallthrough else branch should not.

Selecting ops for a series of ops is just a mapM after you decide the ordering.
The simplest ordering would be id...
selectOp type: Writer [Asm], State (Map SSAName Int, [Name]) --uses and layout
Effect: find longest prefix of last-use vars, swap them to the top, dup the rest, emit payload op.
Popping all garbage TOS can be done both before and after.
Note eager popping is inefficient if you're going to STOP and discard the stack anyway, but it's a decent
first approximation of what to do.

Oh no - I must propagate Name -> IRT info to know whether a var should be on the stack!
For now I'll just uses a hack: the only non-word var I have is $mem, so I'll give $mem[0] that type if it's
live; the rest will be given the type tword. The type signatures on op will do the rest.

TODO now: place return address TOS on call so it doesn't mess up last-use consumption.
Handling call -> asm: I need to add location (function,SLC index,call count) to SelectOps state.
It'd be cleaner to add function and index to Reader since they don't change, but that would require changing
the monad type.
How to eliminate the unnecessary swap of ret with f in calls? Allocate the return address when generating the
IR, then place it after the call op.
FW: attempted dups or swaps beyond bounds should perhaps trigger a failure that can be handled, for example by
backtracking and attempting to GC or even stash stack variables in memory.

assembleLists:
 lists = {} --a map head label => list, containing a list which doesn't fall through at the end
 slcs --a map label -> slc, where each has a .fallthrough :: Maybe Label
 buildList = buildList' []
 buildList xs l =
  case lists[l] of
   --If a list starting with the next label exists, concatenate them; you're done
   Just ys -> do
     lists[l] = Nothing
     zs = reverse xs ++ ys, start = first label of zs
     lists[start] = Just zs
   _ -> case slcs[l] of
           Nothing -> something went wrong: LTN
           Just slc -> add slc to xs, recurse with slc.fallthrough
                       if it's nothing you're done

Is overapproximation of live set a problem? Pruning is done at SLC level, so only if a live but unused var
crosses a control flow boundary. Since function calls don't count, it shouldn't be a big problem.

Differentiating features of EVM C vs Ecomp, aka why develop EVM C?
Proper exprs and a type system, structs, tuples, ptrs, sized and signed/unsigned integers
Difference from Sol: structs are on the stack rather than allocated in memory; ptrs allow low-level memory
access.
Proper functions
Compiler-controlled rather than manually chosen stack layout; that allows opts such as varying the stack
layout, e.g. swapping x to the TOS when you increment it.
The basic structure is there, but it still needs 1) full set of primops, 2) full support for structs and
pointers, 3) coercion 4) lots of testing and debugging.
Also other static data.

(&), (|), (^) :: a -> b -> a
If sizeof b > sizeof a and the top word of a is < 256b, you need to mask the top word for | and ^.

---

Eliminating copies in CFG2 leads to coerce byte -> word creating empty SLCs; the copy information is only
preserved in the subst map.
Ex: return 3 in a function returning a word becomes
1(mem,ret): 
 anon0 : byte = 3
jump 0
0(mem,ret,anon1):
return anon1
When jumping from 1 to 0, you need to apply the substitution to the target stack layout (after applying the
version map). What if you have a series of substs...? You need to follow them all until you get Nothing.
When merging version maps, bumps due to a var being in scope and a var being assigned need to be treated
differently... subtract 1 from the versions of all vars that were live in the second SLC's ops.

CFG opts:
Merge jump->1
Bypass (a special case of merge, since copying an empty SLC is free; the property you need to preserve when
modifying the CFG is op trace given arguments).
Jumpi v x x => jump x

The latter opt may shrink live, but if you have a loop where v is/was used you won't discover that.

An empty SLC will eventually either jumpi, jump to a nonempty, return, or loop infinitely.
A loop with a call in it is not necessarily a divergent loop, since the callee may exec a stopping primfun.
However, any non-branching op before a divergence may be pruned.
If a SLC jumpis and both paths diverge, it diverges. Divergence may be replaced with revert(0,0).
jumpi(v,self,divergent) => also divergent
A divergent loop is not ordinary intended behavior, so I don't need to generate efficient (reverting) code for
it just yet.

Oh: when decrementing the refcount of a SLC, I might leave a garbage cycle. That means I need to scan from 
start to fully GC. However, when I decrement the refcount to zero I know I can delete the SLC immediately.
When modifying a SLC (as in merge), I can't delete and then add, as that might recursively delete referents.
Instead, I must atomically update the refcounts of referents.
Refcount changes may cause SLC merge to apply (but never stop it from applying), so I perhaps do need to do
a full scan whenever I decrement a count...
When can that actually occur? If the SLC is bypassable, but then it doesn't matter...
Otherwise, only if a jumpi becomes a jump. That can occur for if _ then do {} else do {} (or two breaks or
continues).
Ooh: what happens if you have two ifte branches to the same dest, containing conflicting copies?
That cannot be reduced to a single jump; you must preserve the copy info somehow. Using an empty SLC or by
annotating the branches of the ifte?

I don't actually generate any detectably divergent loops yet, since I don't simplify while(true)...
But surprise back-edges don't occur since refcount would then be > 1.

Where do I put "v is constant" info for use in optimizing the CFG and stack op output? No matter, I won't do
so now.

Note: when I eliminate ->1 edges by merging, I also eliminate the need for fallthrough of jumps in codegen.
However, jumpi else branch fallthrough is also desirable... place and fall through if it's not been placed
yet. If the then branch has not been placed, add an iszero and swap the branches?
The first jump to a dest with in>1 should also be a fallthrough.
First function call fallthrough: you can scan ops to detect a call to a static label and place the function
there instead. However, if the var is used repeatedly... you at least save a jump.
Don't place functions inline to start with.

I can start stack op generation concurrently with finishing the CFG2 opts since I know the type.
In fact, I can skip CFG2 opts for now...
As before: build op treegraph. I can even skip that and emit the ops in DFS order. That will correctly order
the mem ops.
The stack target depends on the op: for commutative ops I have more flexibility.
For reduce I have even more.
Special-case 1 and 2-argument ops, just consume the top word for the rest.
Eagerly pop garbage (0 use-count vars) when swapping; if I get stuck because I must dup a word beyond depth
17 to the top, GC all reachable garbage.
Alt: GC garbage between *every* op to minimize risk of getting stuck. I could backtrack on each op emitted,
GCing if the op fails...
On branch, pop any garbage remaining. The target layout in case of a jump is the dest's live with the version
and subst maps applied. If it's a jumpi and both layouts are equal, it's the same.
If it's a jumpi and the dests are 1-edges, stack shuffling can be deferred. You still need the cond TOS
though (target: cond:jump target).
If the then branch is a 1-edge, defer. If not, jump to an inserted shuffling section which jumps to the then.
If the else branch is not, you can insert the shuffling directly afterwards at no cost (but you may still
have to jump).
The starting layout of any SLC is its live (with version 0), with order determined earlier (either because
it's the function entry point or by the first node that jumps to it).
When you're the first to jump to a node, the target layout is more flexible: it should just be the live in
any order. You still need to handle two different sets in an ifte though.
Final stages: asm, bytecode, deployment.

layoutOf :: Label -> m (Maybe [Name])
emitOp, emitBranch
swapLiveAnyOrder :: Live -> m () --sets layoutOf
swapLiveTargetOrder :: [Name] -> m ()
State: list-list of SLCs, current list. A jump-back into the current list does not generate a concat.
The entry point must have a jumpdest, but you could still fall through to it (!).
That could happen if the function body is a while and you continue in an ifte.
Simple GC: if the top word is garbage, pop it; if not, swap it with the bottom reachable garbage word.
Repeat until there's no garbage left.
emitOp: take the longest suffix of last-use arguments and put them TOS; dup the rest.
That lets you avoid the 1-arg and 2-arg special cases.
Note mloads modifying $mem prevents them from being pruned normally... but if *only* the mem from an mload is
used... you can detect that by looking at the use count of the result variable.
emitOp (mem, x = mload y) where useCount x = 0 => noop.
There's no need to track which mem is current, so don't.

Note: when I have substs and can choose any stack layout and multiple aliases are live, I need only pass one
word on the stack! Then layouts need to represent that... a list of names per stack slot?

Consider start = x,g,g,y,z where target = x,y,z... you should be able to swap to g,g,x,y,z and pop the garbage.
But I won't explore that for now.

Hmm... if you have an ifte, the then case must jump. But if it has in-edges 2, its other source could fall
through to it, so don't consume the fallthrough opportunity!
State: a list of lists of SLCs (with a map SLC -> list list index). Those lists may be concatenated by a
fallthrough on either side (i.e. either when building a list or when jumping to the head of another list).

Forget only placing used functions, place them all. Use a prelude to use the ordinary calling convention for
main.
In the interest of time, I'll implement the absolute simplest codegen now.
DFS order of ops from live is equivalent to pruning unused ones. Once I've fixed the op order, use count
(#ops) is ez.
Note each op may correspond to multiple vars, so if you have f(x,y,z) where each x,y,z is from a multi-word
op...
An op tree (and index) per var gives you the var's definition as a function of live vars.
Simple potential opts: constant sharing, place first static call to an unplaced function. That reduces the
use count of the label by 1!
If I already have rollback due to swap/dup out of range, I might as well have a list monad for opt choices.
Generalization: an opt choice monad which queries a chooser to direct its control flow; that's much more
compact.
For now, just rollback on failure and use the first successful codegen.

Weird, for
printCFG2 "module {f : Int Unsigned 256 -> Int Unsigned 256; f x := do {x = 10; if (x) then x = (x + 1) else return 2 end}}"
I don't seem to prune every dead op. The same happens for
printCFG2 "module {f : Int Unsigned 256 -> Int Unsigned 256; f x := do {y = 1;return 2}}"
Maybe I need to print the version count and subst maps as well...
Chained copies at least seem to work.
Extracting the cfg2, I find that pruneDeadOps removes y = 1, but opt2Pass 5 cfg2 doesn't.

Oh snap - the live set for the badly pruned SLC does not include x, the function parameter!
Instead x#1 is assigned to $anon0. But if the function body is a while you need to remember...? No, it seems
that if the param is not (), you start with an empty SLC that jumps to the while.
printCFG2 "module {f : () -> Int Unsigned 256; f () := while (()) do {1}}"
throws an edge to nowhere in dfsGraph... but while (()) do {} does not
printCFG2 "module {f : () -> Int Unsigned 256; f () := if 1 then 2 else 3 end}"
also fails to prune the 2 and 3 (that it doesn't prune 1 is expected since it's branched on). But pruning is
done for do {1;2;3}! What is going on?
if 0 then do {1;2;3} else do {} end
Prunes the ops in the then branch, but fails to recognize it's now empty and can be bypassed...
The then branch becomes empty, but does not return directly... that's because I handle jumps to empty
branches, but don't modify the label when it's empty. That's maybe logical, I might need to shuffle and don't
want to duplicate large returns.

Lesson learned from defeating the pruner demon - channel all updates through a single case at the end.
FW: an algo which imposes discipline on when modifications are done...
And now the edge to nowhere bug is gone too! That makes sense, since I was just returning a modified SLC
without updating the CFG - creating an inconsistency that could trigger a bug in opt2PassM's cont after the
recurse.

How to add the C params to live for the entrypoint?
Note the C vars don't necessarily corresponds to param words, they could be bound from struct fields.
The param words are only used in the pattern-match IR code, so it's incorrect to jump to that SLC; if you
jump to somewhere expecting the C params, you must be jumping later.
Negative consequence: the first SLC after the pattern-matching will have a fallthrough from the entrypoint,
preventing fallthrough from anywhere else.
In CFG (before CFG2), whiles start with a node that jumps to the loop; that gets merged, but continue jumps
directly to the loop itself, avoiding the problem.
printCFG2 "module {f : Int Unsigned 8 -> Int Unsigned 8; f x := do {while (1) 1; return x}}"
--correctly has x#1 in scope in the loop body (which becomes empty)
printCFG2 "module {f : Int Unsigned 8 -> Int Unsigned 8; f x := do {while (1) (x=1); return x}}"
--still triggers an edge to nowhere! I must find it tomorrow.
f x := while (()) (x = 1) --seems to be the simplest code which triggers the error
It's the entrypoint to the loop which gets lost... either the start or the reduce, it seems to vary.
A problem with the refcounting?
I need to update the refcounts to be able to correctly apply merge...
printCFG2 "module {f : Int Unsigned 8 -> (); f x := while (()) (x=1)}"
--The simplest code that triggers it
Node 7 (the reduce) is wrongly deleted or looked up...
2 (the loop entry point) jumps to 7, to which it has an exclusive edge... perhaps it has something to do
with the recursion order or when I do the update.
I get the SLC before I check whether it's been visited... so if I merge 2 with 7, deleting 7...
The backjump to node 2 will return the unmerged node.
What if I fetched the child SLCs and performed the self update before recursing on any new children?
Could it be the bypass that's the issue - a bypass by the backjump? The update API shouldn't lose
nodes anyway.
It's in dfsGraph... so it must be in the GC?
It's present in pipeline2CFG2, so it's not the print.
I think it's in dfsGC... I'll add a dfsGraphE that returns an Either...
What if I collected all the reachable nodes, then added back a refcount? I don't need to recursively
delete when updating; if I perform an update the new refcount can be deferred to the GC.
That should fix it; you can't wrongly delete a node if you don't delete any, and dfsGC is unlikely to
be the culprit.
If I GC before the first pass I can even neutralize bugs due to incorrect starting in-edges.

OK, I adjusted the GC algo, removed deletions and added a refcount for the entrypoint and now at least
printCFG2 "module {f : Int Unsigned 8 -> Int Unsigned 8; f x := do {while (1) (x=1);return x}}"
isn't blowing up. I was surprised by the double =1s in label 4 but it's because the while cond is
duplicated to avoid an additional jump per iteration, and I don't share constants.

x=(x+1) leads to ill-typed LHS... wut?
f x := do {while (1) (x+1);return x}
doesn't merge when I expected it to.

Full language stack:
CST, AST, IR, CFG, CFG2, stack ops, asm -> bytecode
To add: pointer (first mem) and struct support.

Next step: stack scheduling with DFS op emission.
It strikes me that empty SLCs, if they do not generate any stack ops, can be prepended to a list of SLCs at
no cost - any number can fall through.
On jumpdests: the entrypoint always has a jumpdest, as do jumpi then branches and SLCs with refcount > 1.

If $ret is BOS, calls are problematic from a stack scheduling perspective: every argument word must be
dup'd. I'll switch to TOS.

----

SLC: Need to add strict ordering of writes *and reads* as a simpler version of each new write happening after
previous reads. That would let writes take a fixed number of arguments... but so would each read after a
particular write taking a mem and producing a read proof pN, st all read proofs must be merged into one.
For now, just use strict ordering.

An empty intermediate SLC can't be eliminated if it jumps to itself... but in that case it can be replaced
with a revert(0,0).

Single-SLC SSA: count uses and increment versions at the same time.
x = copy y; rest --does not become a use; instead substitute x for y_n until next assignment to x.
That requires keeping a subst map, since rest is not in SSA form.

CFG ops: delete, insert, modify (implemented via delete and insert).
Track in and out-edges.
Jump->other with in-edges=1 => merge.
For now just naively jump at the end of each SLC; do no CFG optimizations.

If true block becomes block... but I don't need block statements just yet.
When returning nullValue : (), push 0 is still emitted... but that's fine, it'll be optimized away.

Most vital for now: SSA, treegraph, ops => stack scheduling with last-use pop, stack-aware CFG => asm, asm =>
bytecode, JS util funs for deploying modules.
For now I only use SSA for copy elim.

Where to detect and error on repeated uses of the same var in an op LHS? In IR.

After tree shaking of ops in SLCs, the live set may be shrunk... consider an x = y op where x or y aren't used
in the branch.
Algo: for each SLC, tree shake. If the live set of any shrinks, repeat.
That will not eliminate irrelevant vars which are used and modified in a loop.

Currently the ppr only prints reachable SLCs, but in the SSA + copy elim phase I can include only those in
the returned CFG. Contents: (SLC SSAName, versionMap, copySubstMap). In and out-edges?
The CFG being accumulated also marks the SLCs you've already traversed.
SLC merging algo: keep a stack of labels on the current DFS path? No need, in-edges=1 prevents loops.
slc jump-> slc' with in-edges = 1 => merge slc slc' and their maps
SLC bypass: 
 if jump to slc with ops = [] and branch = jumpi, replace jump with jumpi
 if jumpi to slc with ops = [] and branch = jump l in either then or else branch, replace that jumpi branch
  with l
Problem: in-edges may change due to the intermediate node being replaced by multiple nodes that jump to it.

Next: for the name->pre-SSA CFG map, apply processCFG to each SLC in Compiler.
---

Bugs:
Incorrect tuple generation, (w1,w2) becomes w1 >> 256, w2 >> 256 | w1!? It's correct for {1,2}, but
incorrect for {1,w}. The problem is in structLayout: [(256,"x")] becomes a 2-word struct.
divideIntoWords [(0,256,"x")] generates two words - IC see the off-by-one error, the last bit of x is 255.
(fixed)
No implicit returns (fixed)
Note: truthy on one word should just be a copy. Truthy on none should be 0.
Idea for simple ppr: just convert back to CST
Need to add coerce

Elim dup on last use is the single most critical opt I need - otherwise the stack explodes as I keep
allocating new anon vars.
Once I've SSA'd (separately for each SLC), each var is associated with a producing op and multiple
consumers.
$mem doesn't get consumed by mload... but it does get replaced by mstore. All loads which depend on $memN
must be done before $mem(N+1) is produced.
Use naive treegraph approach, don't try to run free ops or partially execute reduces.
For the start layout per node, init for entrypoint and set other starts arbitrarily to the end layout of the
first jump/i to it. That may lead to situations where the first jump could have just done its idiosyncratic
shuffling... FW: given exec frequencies, reconcile stack shuffles of all jump origins.
Layout is tied to op exec order... it's all connected, so need generic opt algo.

Desired compiler-building framework features:
Add stack of context to monad for better error reporting. Anonymous "constructors" for error messages?
Con :: Typeable data => String -> data -> Err
A generic iterated opt-application algo. Local opt guided by heuristics, compared against global consequences.

Dataflow at the IR level: op, while, ifte, break, continue, return.
A write to a var that's not live can be ignored... furthermore, if the var is not relevant to any of the
output vars of the loop it can also be ignored.
Split ops into v = op[n]? Either SSA or assigning a UID per op is then required.
read and written per block are easy to get... dataflow is written => its read.
DF is not a property of code (which may have multiple returns) but a path in that code.
Each leaf will have its own DF.

Prune loops which have no leaves? Problem: functions may stop. Don't prune for now...
Potential effects: mem writes, persistent writes, RETURN.
Revert and invalid are externally observable, so they also count...
You could have a subtyping system to track effects... f1 or f2 : union t(f1) t(f2). FW...

C seems more relaxed about eval order than I thought... maybe I should be as well in future.

How to search for efficient schedulings and opts? Perhaps at random... I only need to pop last-uses within a
SLC for now; use any topological sort of instrs.
Functions can be converted to IR, CFG and then asm individually.
What features does a standalone module need? Exports: main at zero, exported funs, JTs, code arrays, globals.

What's the tricky remaining part? Live is done, SSA is trivial. Live implies >= 1 use, so last-use opt can be
done on a per-SLC basis.
With naive op scheduling, start layout of dest = current layout if one hasn't been set already.
The function entrypoint has a fixed layout.
If the layouts of two ifte dests conflict, introduce an intermediate SLC in the stack-aware CFG.
Opts: eliminate intermediate empty SLCs.

Static pointers instead of mem, storage globals could simplify var compilation - they'd only be locals or
static constants. It would also make costly accesses to them explicit, and make single-value globals consistent
with arrays.

Immediate plan: revamp T, complete CST->AST
FW: introduce UInt, SInt type synonyms.
Implement minimal primfuns. Continue pipeline to CFG.
Vital opt: last-use pop. Drop constant sharing and expansion, dataflow pruning.

Kinds: Nat, Region (used by Ptr), Signedness (used by Int), Universe n / Type, k -> k
But no need for a kind check for now; I just need to check types are *.
Without params, user-defined types are just *, as are all types in function bodies.
Future use of param: objects containing multiple ptrs with a shared region r.
Type synonyms can be expanded without doing a kind check, though do check for cycles.
Require tysyns are fully applied, preventing Y combinator attack; they're macros, not true type constructors.

Perhaps = could be subsumed by ordinary operators... a.b.c += f x --parses as intended
arr !! ix = v --(arr !! ix) = v

Ah: eval ordering matters due to memory side effects. The emitted IR modifies $mem, so it must be emitted in
the correct order. The natural order for the EVM is right-to-left. I'll just use the implicit order for now.

Should struct patterns be able to specify padding?
Oh no: padding size should perhaps depend on all subsequent fields, so bitfield {a,b,c} becomes
bytepad a, bitpad b, bitpad c. Otherwise a bitpadded bit would misalign all subsequent fields...

If the bool and n params of T.Int were :: T, you could have template polymorphism.
That would also let me fit Int, Ptr, Map and user types in a single framework...
(->) :: * -> * -> * isn't an odd one out if it's not reused for kinds.
T = Con | t $ t | Int | struct --True, False, regions are cons
Where should repeated struct names be caught as an error, if at all? 

Op ordering is preserved in parsed text... for now have default fixity mapping.

Move := to E maybe... but none of the memory regions support x = v well; only stack lets you get the old word
for free. For now, just error if it's in an E.

Remember the target: compile
main : () -> ()
main () = return ()
all the way from source. Do the absolute minimum for that.
First step: CST -> AST.

OK, now I've got a basic grammar. Next: import it.
:show paths
:set -iC:\<path to repo>\UniSyn

Is *ptr + 5 => * (ptr + 5) the behavior I desire? Yes, it's hard to see how to avoid it while
having both Haskell application and infix operators; consider f * ptr.

****

For now, just place call continuations in the same SLC? Do all ops before cont split anyway.
AST -> IR -> CFG
CFG: SSA, dataflow pruning, constant expansion, copy elimination.
Pull only used vars. Then SLC codegen, merge into a set to share duplicates?
Isomorphism could let you share multi-SLC subgraphs. But for now do minimal opts; at least per-SLC
pruning is essential.
Note the entry node has a fixed argument ordering.

Important TODO: specify package versions used for reproducibility. Or just use Cabal with specific
versions...
Truffle's been deprecated... test simple EVM C programs in hardhat instead.

Handling primfuns: which do I need?
*_, mathops, logops, bitops
EVM instrs: uppercase (word arguments) and lowercase (nice types, in particular Ptrs)
Forget overloading based on return type for now.

--Use a view pattern for Tuple?
match (p1,p2...) (Tuple ts) ws:
 for t_i in ts: 
  ws_i = split wordsize(t) words of ws, coerce them to t_i
  match p_i t_i ws_i
match Wildcard _ ws = return ()
match (PVar nm) t ws:
 --check nm matches t if it's in scope
 for w_i in ws:
  nm#i = w_i 
Oh, a problem: my Copy op is used for coercion. If I convert uses of x copied from y to y after
SSA, I'll create an ill-typed SLC. No problem, the IR types don't matter at that stage - they're
just for error detection during IR gen. Modulo kinds... words are stored on the stack, Mem and
other abstract types nowhere.
Note pattern-matching of tuples has no runtime overhead.

Struct pattern matching:
{p1,field: p2} = e =>
p1 = e.__field1 or select e 1
p2 = e.field
A separate select operator would be useful, since it's independent of name. Then I can get rid of
the __fieldN hack and have fields really be a Maybe Name.
The type of e is given... but it need not match the type of pattern vars in scope.
Ex: (x,x.field) = (a,b)
writePtr(p,*p) = return ()
writeGlobalField(glob.foo.bar.baz) = return ()
Con p = e --strictly enforce type; constructors are monomorphic.
Check e's tag is the expected value; revert otherwise.
The size of a datatype is the tag size + max size of any element; to keep it compact while keeping
the tag at a fixed location, put the tag in the LSBs?
To add efficient switch/case, I need to extend the IR. I'll at most add newtypes for now?
How to fit case into CFG? switch w [conts] --is a branch

Optimization: if my first C->IR pass produced an IR with higher-level constructs such as
SoftCoerce, I could symbolically reason about it. Ex: softCoerce (struct type 1) {f1=e1,e2...}
=> a modified struct constructor.
A HLIR above IR which has a concept of multi-word exprs? FW...
Sub-word reasoning about fields and mstore/mload could also be useful.
A concept of variable-size values such as array slices...

Pats:
x
(ps)
{fields}
p.field
*e
e1[e2] => *(e1 + e2)
--(ps) and {fields} = e decomposes to (x | *e).field* = e
The region is determined by x or ptr type.
stackVar.field* => sliceStack(stackWords,off,len). Note stack isn't sequential, unlike other
namspaces... though mem could also be split up later.
sliceMem(p,off,len) = e --read, stack slice write, write back
Opt: when a var is set in a dominating node, you know its expr (which may be phi);
x = *p
if(cond)
 p->field = 1
else
 p->field = 2
--could be optimized to reuse the *p on stack
Future opt: delay the writeback AMAP, including across SLCs.
Using IRTs I could figure out the padding per word, optimizing stack slice writes.

******

Woot, CFG implemented! Now what? Eliminate intermediate jumps.
For now, I can scrap usage analysis and assume SLCs have no garbage words. Eliminate garbage at
the start of the function call separately.
Next steps: port CFG back to IR, SSA each SLC, convert copies to noops, generate instrs.
Are the SLCs small enough I can do full search of all op orderings?
Alt: treegraph approach again.
If you have last-consume n vars from a runnable op TOS in an acceptable order, emit it and delete 
it from the remaining op graph.
Mem could be in a separate location.

Recall: a jump into a dowhile adds 1 to the iter cost in return for one fewer branch. 
If !cond, you can fall through to cont.

Is the pasm approach for constructing a CFG better than operating on the AST...?
It's not necessarily the case that all function bodies end with a return (though I do add one
atm): it could also end with if(cond)return () else return ().
Convert each stmt to a SLC, prune later. Empty bodies become jumps.

while(x) could have an empty pre.

Maybe every Branch should also be associated with scope info... that would let you codegen SLCs
in isolation.
If I convert every IR to one or more pre-CFG instrs...
Generating sequential code = ops | jump l | jumpi v l1 l2 | placeLabel l  --is trivial, esp when 
you don't need to consider the stack. How hard would it be to construct a graph from that?
declareLive is nice...
Only when I immediately jump due to a {} is live non-obvious... iftes generate a declareLive.
But when I jump I can follow to get live and eliminate the intermediate jump.

Reconstructing the CFG: it's easiest if else branches also require a label.

If I allocate a new label in every cfg... don't do complex jump elimination in cfg, do it later.
Then I don't need to pass a branch in cfg: pre could jump to an SLC which just performs the ifte.
Pass the start label to cfg.
Jump elimination: how to deal with l: jump l? Check for equality.
Note cycles in the CFG can cause disconnected subgraphs to have refcount > 0; it's reachability
from 0 that matters.
Build CFG from the back, return label and live?
Base cases:
return --top level should always end with it
break, continue
[] --only encountered at end of if's branches

****

Wait... all nodes in a SCC do *not* have the same set of live vars, consider
while (a)
 --x is not live here
 x = 0
 while(b)
  x = f(x) --x is live here

SSA: for each SLC, params at start become a definition and params in labels a use.
Need to compute live to get params...
Are only C vars live between SLCs? And $mem, $ret...
The structured IR is most amenable to liveness analysis...
Assignments within a while body on vars not in the parent scope will not be passed
through at the end. declaration = assignment, but assignment in the following while
body should make the var not live and thus ignored and pruned.
Ex:
x = 0 --garbage declaration
while(cond)
 x = f(y)
During CFG construction, mark with live vars? That requires construction from the back.
Leaf: Return vs => vs
lhs = op args => delete lhs from used set, add args
while(cond)
 body
if v
   th
   el
=> used: union v, used th, el
defined: intersection defined th, el
Does defined matter? Note in
--x not in scope
while(cond)
 x = 1 ...
--in later code, x will either not be used, or be in scope because it's defined without reference 
to x; in either case it won't be in used.
Break, return and continue may mean I need to do liveness analysis at the CFG level.
If I made a spanning tree of nodes...
Getting an initial live var set from the tree would be simple... it's the
back-edges that are tricky.
In a CFG generated from C, cycles can only be strictly nested, corresponding to
nested whiles. There's a stack of possible backjump nodes... as well as forward ones.

live s (ifte v th el) =
 v, union (live s th) (live s el)
--that has the behavior I want...
live s (while pre v post)
live s pre | live s (pre ++ post)?
Why does that work? Because the trace of while is (pre,post)*pre and live is
idempotent. Why? Because it's union with s . delete s.
continue has the loop start's live...
If you can break and continue, you need a stack of starts and ends. It's best to do this while
constructing CFG because then you know the structure rather than having to solve the problem for
arbitrary graphs.
What if I used a fixpoint algo...? Start with continue = {}, get live, then pass that in until
you reach a fixpoint?
Collect ops into blocks earlier? While starts a new SLC, if ends the old one...
I must get liveness from the back... I must do the same for the CFG then.
At the end of a block declaring x, x will no longer be live but might be assigned again.
But that's not a problem, the old x won't be passed on due to scope rules precluding any
use.
Recursively build each block given end label and its live set.
If you encounter a return vs, discard old live and replace with vs.
If an ifte jumps to blocks with two different live sets, it must pass the union of the
two to both... but that's fine, each of them can do the stack adjustment themselves since they
only have one parent.
The backjump in while is tricker... if you want jumps to only include the live vars of the dest,
you must insert an intermediate node with no ops. But that can be done at the stack-aware stage...
Continue doesn't have that problem since it's a single jump.
When doing the node generation for while with the fixpoint approach, you need to discard
the old nodes.
Multiple passes through the graph, marking each with maybe live set and repeating on a backjump
encountered? Problem: all predecessors of a backjump must be marked as well.
If a while takes multiple passes...

The fact database approach: live(l) >= or == ls.
Does while require only two passes?
A symbolic approach: union ls1 ls2, delete ls ls2, live label, exactly ls
liveWhile endLive: first try with loopLive = {}, then use resulting live set
If I decorate the structured IR with live sets, where to put them?
In front of every structured instruction... add an explicit Continue 0 to the end of loops?
If I add a block construct to IR, maybe I don't need to use a list of IRs...

Niggle:
while(voidE)
 body
Here, truthy () => false, so it can be converted to just voidE
Ifte voidE th el becomes
voidE
Block el --it can't simply be transformed to el because 
Note while and if without any cond var isn't representable in the IR.

--In the C, can vars set in cond be used in body? After opts which cache memory
accesses, potentially. ex: while (*p) {f(*p)}. Not for now...
Defined/in scope is an upper bound on live and can be computed in a forward pass.
Tricky case:
x = 0; while(cond){x = f(y); ...}; f(x)
--x is not live in the loop body, but is after it. It should therefore be passed as
a param when branching out.

For now, no constant sharing between SLCs. FW: constant liveness could be tracked...
Detect diff (code length to compute) between related constants such as mask N, mask n, ~mask.
struct.field = ~struct.field => xor with a mask

live (ops,branch):
 defined = {}
 used = {}
 for ress = op args in ops:
  for arg in args:
   if arg not in defined:
     used += arg
  for res in ress:
   defined += res
 return (used,defined)
 
Looking at usage would let you prune unused ops in straight-line code, but not in loops such as
x = 0
while(cond)
 x = x + 1
 ... --because the body branches to while, which uses x
Maybe do a dataflow pass before SSA... but ignore that for now, just get to running code.
Thought: if the loop cond isn't modified by the loop, it can be lifted out:
while(e) => if(e) {while(true)} end

A nice advantage of general combinators beyond reuse: you can split up the problem, abstracting
away details so you only need to care about the shape of the problem you're currently solving
(ex: mapping over a list, or folding over SCCs).

Add default return in seqDefun - it should be the end. $mem, $ret should be in IR scope; args
should be in C scope and arg#1..n in IR scope.

Seq doesn't typecheck after a return, so I'll need a separate typecheck pass for that...

What now? I need to close a minimal pipeline. Complete seq, continue processing CFG.
text -parse CST -desugar AST -seq structured IR -cfg CFG
CFG: treeshake, live, SSA
Elim single jumps.
Ifte jumps with stack processing first -> an additional SLC in a stack-aware lang < CFG
SSA -> CFG in stack-aware lang
Using a simple 50/50 model of branch probability and solving linear equations to get an
equilibrium, I could fallthrough the most frequently executed SLC. Just fallthrough the first one
encountered at first.
Annotating ifs with probability and loops with execution count would require I pass those annots
down. A location annot and an external map location -> info might be better.

First goal: compile f () = return 1 --all the way
Simpler:
main :: () -> ()
main () = return () --or just {}
Should I add info about the function being compiled to the Reader input? That way I could have
a return anywhere, including in an expr. Sounds good... it could make errors more informative as
well.

Graph simplification: jumpi v th th -> jump th
a -> {} -> b => a -> b
ops1 jumps to ops2, which has only one target => replace ops1 with ops1 ++ ops2, ops2's branch
If a subgraph has no leaf branches, revert
map label (parents,slc,children)

Opt idea: consume fishing. Say you want the buried vars (x,y,z) TOS. You have some pushing ops
which you'll have use of later. To get (x,y,z), first swap z to top, then push op1 and swap for y,
then same for x. You might end up just delaying the problem...

How to support compiler choices such as inline or not, unroll, constant sharing, constant
computation, while (cond) body => if(cond) do body while(cond)...
Also, is there a generic way to tag every node with a location and propagate it for error
reporting?

The compromise codegen for while (one branch per iter, no cond duplication but an extra
jumpdest before cond) could be generated correctly by marking the jump from body to cond check
as having a higher fallthrough priority. If the body ends with a call it doesn't matter if it
falls through...

How to represent inlining without local goto? Recursive tail calls can become while (if you don't
specialize). return e becomes __retval = e followed by a goto to the end of the inlined body.

For partial word write at >0, what is most efficient?
store(p,x):
save = mload(p+bytesize) << bitsize
mstore(p,save | x)
--or
mstore(p,mload(p) & mask | x)
--If p is a constant, the former has the same runtime cost but likely smaller code.
Constant sharing makes repeated use of internal constants (bytesize, bitsize, mask...) have an
unpredictable size effect.


If you RETURN, cleaning up the stack doesn't matter...

With support for control flow in exprs, x++ becomes ez:
do tmp = x; x = tmp + 1; tmp

When an IR var is a static constant, you have the choice of keeping it on the stack or duping it
on next use.
0 need never be kept on the stack, since PUSH0 is as cheap and small as a DUP
Setting an IR var to 0 makes it garbage.

Did POP always cost just 2 gas...?

20 Nov:
An overloaded returnPtr(p) that used sizeof(*ptr) would be nice, but requires separate type
inference.
returnPtr : Ptr Memory t -> ()
returnPtr p = RETURN (p, sizeof *p)
If I allocated user-defined overloaded functions on demand...
That wouldn't get me alloc() : Ptr Memory t. But if I passed down info about expected type...
That would preclude coercion when calling an overloaded function.
It would also mess with soft coercion of structs: the overloaded field would be required to
exactly match the expected type.
When to unify for overloading? On assignment (struct.p = alloc(20)) and call.
How to implement overloading? Functions with parameters in type signature and those same params
in the body; for each distinct instantiation, generate a function with the params in the body
filled in.
Ex: 
map : (a -> b, a*, b*, n)
map(f,from,to,n) =
 x = (typeof n) 0
 while x++ < n:
  *to++ = f(*from++)

If exprs can branch and form their own subgraphs, that poses a problem...
Idea: allocate many subgraphs rather than a single CFG. Each has inputs and outputs; the
arguments of an operator are inputs. They'd be independent by default...
The problem would be loops: the statements in a loop are a separate instantiation.
A functional IR where while loops become
loop_f args = if cond args then let args' = body in loop_f args else return cont <subset of args>
might solve it... that unifies ops and control flow?
"Graph doesn't reach end" would become "f doesn't call g"
What would exprs with control flow become?
The simplest thing to do for now is to assume exprs don't branch (even RETURN) and codegen
accordingly.
Otherwise: in the IR, translate branching exprs into control structures. If it never jumps to
end, that's not a problem... in if cond th el I can still define the then and else continuations,
they'll just be tree shaken away if cond is guaranteed to diverge.

Exprs with control flow: return the modified words.

m = mem() --generates an IR var of type mem!
(m1, m2) = split()
Explicit passing of mems, or withMem m {block}
That could let you hint memory parallelism.
A hidden variable __mem? C would have to be able represent it.

cond ? th : el => do if cond then x = th else x = el; x

Maybe I should use HasCallStack for precise error reporting (getting the hierarchy of callers).

Terminology quibble: what I call call soft coerce (from Haskell's unsafeCoerce) is called
conversion in C. Explicit coercion is called cast.

Pointer addition: ptr + x => mask 16 (ptr + byteSize * x). Values are stored byte-padded in
memory. Relevant opt: constant expansion, so ptr++ becomes ptr + k rather than k*1.
Byte-address storage pointers? That could require 2 sloads if a value overlaps a word boundary.
It would also require masking and shifting... but would abstract away the idiosyncracy of storage
vs memory.
returndata doesn't have a returndataload instruction, so *ptr would require scratch memory.
*p1 = *p2 can be optimized to a copy instruction.
mcopy would be a useful primfun to expose; distinguish primfuns with nice types from those that
take words using lowercase vs uppercase?
16b fits the address space of code, at least outside CREATE. But what about calldata?
If you batch entire blocks, 65k might be too little... especially if you batch many contract
creates. Using unsafeCoerce (that doesn't do any masking), you could use pointer operations
on words unsafeCoerced to Ptr. Problem: say you're loading a multi-word value. The internal
pointer additions should be masked? Or maybe they shouldn't be...
If normal pointer addition could silently overflow the pointer (which then becomes a different
ptr when stored), you could avoid masking. Make pointer overflow UB...

Need an exhaustive list of UB in documentation... pointer overflow, unexpected aliasing,
use of bad pointer or function values. Most of it seems to be about pointers...

Masking opt: if you perform repeated additions, just mask once at the end.

19 Nov:

SSA algo: for every write to v, create a new unique v'. It doesn't matter in what order they're
numbered. Every SLC consists of [out_i = op ins_i]; for each var use you must determine the last
version written. For in_i with a single dominating write, use that version; otherwise add a phi
function.
Computing the deps of while with out_i = op_i ins_i: it's a transitive closure.
For each x set and read in a while: put a xIn = phi(xLastPred,xOut)

In the lit, out-branches seem to be part of basic blocks... should I copy that? I don't think it's
necessary. 
Viewing jumps as param'd by the set of live vars at the start of the dest is nice, it makes it
like a tail call. The param can be implicit in my code, since each SLC is associated with a set of live
vars.
Do I do away with phi functions entirely this way?
Allocate a new version for each label param.
Collect goto params (var => version) for each dest and find constant vN = vn. Eliminate the
param and replace vN with vn. vn isn't constant per se, just set somewhere else.
Remember the full scope; the params are just what needs new version counters.
Ex: x in pow(x,n) is constant within the multiplication loop.
OK, use phi functions instead of label params... label jumps with the phi index.
If a var wasn't modified in any branch of the ifte, I can just use the last version?
Generalize that to "since the last dominator". Note all nodes in a function except start have a
dominator.
Algo(start):
 frontier = preds(start)
 explored = copy frontier
 if #frontier = 0: error, we're at start
 while true:
  if #frontier = 1: return frontier[0]
  for each node in frontier:
   for each pred in preds(frontier):
    if pred not explored:
      add it to frontier' and explored
No, consider a -> dom, a -> b -> dom... after 1 iter I keep following from dom... unless I explore
in order of SCC depth. But the algo should work even within an SCC.
Imagine you have a thread for each node in preds(start). Eventually all threads but the first to
reach the idom are blocked.
How to define the depth measure I want? Convert the CFG to a tree, mark each node with the minimum
depth at which it was encountered. Nodes within a while loop will have a higher depth.
ancestors(n) is the set of nodes ancestral to node at depth n; idom is the only element of the
first generation encountered which has only one element.
Algo(start):
 if start.generation == 1: error, start has no idom
 frontier = preds(start)
 explored = copy frontier
 g = lowest generation encountered in frontier
 while #frontier > 1:
  while max generation in frontier > g:
   take node with highest generation
   for p in preds node:
    if p in explored skip; add p to explored
    add p to frontier
    g = min p.generation g
  --Now all nodes in frontier have generation g
 return unique elem of frontier

Relevant functions: idom, potential predecessor nodes since idom
potential_ancestors(node):
 let d = idom(node)
     expand n = preds n less d
 transitive closure of expand on preds(node)
transclos rel s =
 ret = s
 frontier = s
 while frontier nonempty:
  new = union map rel frontier less ret
  ret += new
  frontier = new
Each SLC has starting live, modified vars, dependencies, live at each branch.
Return is a branch. ret is consequently live in any CFG node which may return (some are
guaranteed to diverge).
Minor opt: if an argument word (including return) is guaranteed not to be used, you can just
replace it with GAS in a call. Only if the argument doesn't modify state...

Opt: if you repeat the cond code in while, it becomes
if(cond):
 dowhile {block} (cond),
which requires only one branch per iteration. That saves 9 gas per iteration; the size of
cond and iter count determines whether it's worthwhile.

How to deal with temp vars? In an individual SLC, consider the live vars in the end branch to
be a use. Then anon vars will have a last use within the SLC and can be popped then.
Problem: let's say you have ifte cond l1(args1) l2(args2), where args1 and args2 are distinct and
l1 has multiple jump targets, and the layout at the ifte is not args1. You must emit shuffling
code, but you can't necessarily fall through to l1 - you may have to jump to it.

Codegen for call: after the CFG phase, split SLCs at call sites and codegen. That could create
opportunities for code sharing, in particular if the return cont is short.
Comparing tree-shaped CFGs is trivial; comparing cyclical ones requires an isomorphism check, but
since they need to have the same shape that's also easy: just map labels in t1 to t2.
What if one tree is equivalent, but contains some redundancy?
Ooh, that might let you un/roll loops. FW...

Kinds of IR vars: word and virtual (mem, gas, storage etc).
What does CALL touch? storage, mem, returndata, maybe gas. No reasoning about external contracts
means no external_state gets touched.
call(...,ptr,len) can really only modify ptr (writing off the edge should be UB).
Hold on, can you use call(...,ptr,inf) to zero any number of words for just 100 or so gas?
Or is there a penalty for words copied...? There should be.
If you call origin (an EOA) with 0 eth you get a hot call for free. It's only worth it if you're
zeroing ~30 words, however... is there a real use case for that?

When a SLC ends, it continues to a SLC with a live set (vars without versions). The target either
has a layout specified or does not; if it does, emit swaps to achieve the same layout. Otherwise,
set the current layout as the target's (removing any old versions of vars if necessary).
Would dups and pops help as well? It may not be possible to always consume on last use; if so pop.
Dup would help if you had garbage in the target's layout.

Goal: compile "x=1; return 2" correctly. Get SSA working ASAP so I can complete the pipeline for
a minimal program, forget all non-minimal features until I have a pipeline to debug them with.

17 Nov:
Phi functions... can they be replaced with just vs = arbitrary at the start of each SLC, where vs
are all the nondeterministically modified vars?
You also want to track the dependencies... including on branch values.
The start of a while may also require a phi, since it doesn't have a single parent SLC.
Don't treat calls as branches for the purposes of CFG/SSA? That would let you reorder or
eliminate them. In future I may want to remember that I can place the return continuation anywhere
rather than immediately after the JUMP, but I don't need to for now. Actually I do need to, to
exploit the free pseudo-inlining opt.
Why pseudo? Because full inlining lets you specialize and opt the ops in the parent function.

() is falsy - why? Because truthy v = or (words v)
An ifte on a multi-word expression where each is expensive and could be computed separately
could be optimized into multiple iftes, the first on a subset likely to be true.

while {outs1 = ins1} var {outs2 = ins2}
Note while becomes 2 SLCs; the cond is guaranteed to run.
What are the affected vars and each of their dependencies? Compute it at the CFG level: all
phi vars are all the live assignments made since the last dominator.
The dependencies of each var are pre-loop vars, plus the cond var(s).
if {as = bs} var {cs = ds} {es = fs}
Affected: as, cs, es.
Dependencies: ...?

For now, take and return mem in every call. Later, identify those functions that don't use mem
and remove mem effect from static calls to them.

Making call an op doesn't change whether I need multi-var returns, but it does mean I need
multi-word returns.
Leaf branches: tailcall, return, branching primitive.
while(1){} should compile to a noop since nothing can depend on it.
CFG-level rule: replace a -> {} -> b with a -> b.

Branch types: jumpi x l1 l2, jump l (count ret?), branching primitives.
Return is a safe dynamic jump, so treat it specially.

When placing bytecode SLCs, one can share identical sections. There's a conflict between code and
load size and runtime efficiency: if you adjust slc1 and slc2 to unify them (for example by
reordering vars, changing a dup to a push to not use a const which only appears in one of the
two, or changing a pushed constant to a param), you can share the code/load in return for
potentially increasing runtime cost.
Opt: empty SLCs which just jump to cont can be replaced with cont. Note that doesn't apply if the
end layout is different from the start.
Branches with constant conds can be replaced with jump; do that and the jump following (and SLC
merging) iteratively.
Sometimes merging may be inefficient if you could otherwise share a suffix, but save that for
later.
Does it matter much which branch of an ifte is the one placed immediately after the JUMPI?
Once code Merklization is rolled out, spare bytes could fit a small branch.

Thought: does the stack layout (var -> index) need to be the same for the end of two SLCs
jumping to the same destination? Well, garbage words should be considered identical.
Constants and shared subexpressions should be converted to vars and always shared in the SSA
form.
Whether to move a var assignment into both branches of an ifte to improve stack layout is a
nontrivial decision: it may improve layout, but it increases code size.

For computing live vars, divide the CFG into SCCs, forming a DAG. Each SLC has a set of used SSA 
vars; the live vars of a SCC is simply the union of the live vars of the nodes in the SCC and
all lower SCCs.

Note variable declarations where the value isn't used until the real assignment shouldn't have a
runtime cost.

16 Nov:
OK, struct construction is implemented...
What next? seqS, seqBlock, seqDefun.
SSA; my generated code has lots of copies.

What should the coercion scheme for assignment be?
Maybe same as for function application.
softCoerce: int -> int, struct -> struct only
Use the same for application; add general struct coercion.

Could I do without SSA? It makes write elimination and constant propagation really easy...
First make CFG...
A simple thing to do: for every var that might have been assigned, v#new <- arbitrary.
Live var set at end of SLC (without versions) can be computed during CFG.
Write elimination: every var becomes a tree; v1 = copy v2 => follow copies, replace with
arg var or op.

15 Nov:
Computing struct from field words: associate each word with a shift value (which may be negative
or 0).
For each word of struct:
take up to 256b of fields, shift and or them together.
Each word of field has an unpadded bitsize <= 256; multi-word values can be divided into such
words, with the first having a size < 256 if the field isn't n*word in size.
Accumulate struct words, starting with the first field word;
 track offset.

14 Nov:
Constructing CFG:
call, return, ifte, while
Each transition has a cont: call, return, jump, jumpi
Doing SSA only within straight-line segments has a downside: an opt might involve using an old
version of a var that stays live. But live vars are computed backwards (and can thus only refer
to pre-SSA vars), whereas stack layout is computed forwards.
However, constants that remain on the stack can be reused. Binding them to a var will keep them
live...

Call SLC boundaries can occur deep within exprs; first convert to word ops.
f(a,b,c) => w1,w2... = f(a,b,c)

*****

Time to hack

First compile to word-level with structure preserved. E,S => ops.
Divide into CFG of SLCs, but don't specify targets.
Solve equality constraints by arbitrarily using first stack order you encounter.
Ex:
s1 -> while e -> end
When creating the loop label, use the end of s1. Record the expected layout of labels in a map.
Problem: if you allow return (and later break, continue and goto) in a while or ifte, you
can't rely on e.g. the first branch of the ifte giving you the initial layout of end.
No matter: use the CFG. If there is no path to end, DCE it.
If there is a path, use the layout you obtain from the first path you encounter.
That is, the end of the first node explored (SLC) which goes to N determines N's start.
The constraint on the first end is determined by the vars that need to be live; they're the union
of the used vars of all reachable nodes.

1: Convert to word ops, with structure preserved
--problem: because I SSA after CFG I can't prune or reorder function calls here.
Construct CFG; use greedy first-predecessor-becomes-start approach to avoid phi functions.
SSA within SLCs (note they don't necessarily have ends yet), but after traversal they should.
Generate bytecode for SLCs.

Even with no moves across calls, constant sharing should work pretty well. I can also constant
propagate in phase 1, or by preserving constant information across SLCs.
I've overloaded the term SLC too much...

Modified treegraph ("elastic tree edges"): if a const is used repeatedly, place it ALAP.
Place ops backwards from root? First just DFS, or use my treegraph.

******

Maybe optimizing state accesses via UB is undesirable... it could trip up devs like silent
underflow did in Solidity. No matter, it's not prio 1.

Note: function calls can return multiple words, but ordinary ops don't!
Call and return is a branch, so I only need to consider ops which return 0 or 1 words (mem
isn't a word, it has no runtime representation).
If calls break up SLCs that precludes reordering them to take advantage of dataflow, at least at
that level. But if I have a pre-breakup step where the dataflow of calls is explicit...
(mem,w1,w2...) = call f args.
Note mem' : Mem r = mstore(mem : Mem r, ptr, v) --introduces the desired dependency of mem' on v!
When I add more fine-grained mem tracking, functions should take minimal mem regions.

If I also use a consumer graph, I can look at the consumer ops of the top var to see if I can
greedily perform that op.

Let's say every SLC ends with a final branch op. How to deal with the prelude to while?
Constraint on ending: all subsequently used vars must be on the stack, but they can be in any
order and garbage may be included. Constant sharing can be done across SLCs.
Distinguish word-level IR with control flow from division into CFG of SLCs.
If the last statement before a while is a call...
If a C-level var is live, does that mean every word of it is? No, consider struct.field.
ifte is the only example of two forward branches to the same dest; use the first branch to
determine target stack layout?

if e then b1 else b2
--can be reduced to x = e, if x ...
OTOH, while cannot, it's divided into
loop:
x = !e
jumpi x end
body
jump loop
end:
The stack layout at the start of the while is constrained to be equal to that at jump loop,
modulo var updates.
Determine it greedily... do the same for end.
Fix the e eval so it only pushes one word... note that any truthy word lets you opt away pure
ops.

DFS placement of trees in stack order would work well for t1 + t2, for example... or would it?
Consider <a bunch of stuff>; return (x,1). Now if there are any mem ops, the return will be
dependent on them... but DFS means you push 1 (which won't be used for a while and messes up
subsequent <a bunch of stuff> evaluation).
The maximum overhead of duping on last use is 6... modulo other ops having the wrong words TOS.
How to limit cost of search...? Maybe first choice is most significant.
Limit total, do BFS.

5 Nov:
Problem: the target and initial stack of internal SLCs can be freely reordered, but their value
matters globally if you can select them to reduce code reuse.

New approach: split into actual SLCs and impose constraints between their targets.
ops
call f args
rest =>
slc1 = ops, last = call f,args,ret
ret -> slc2,
slc2 = rest
end of slc1 = f,args,ret,locs
start of f is fixed... unless I introduce multiple variants to allow different solutions to
constraints! That's for later, once I have a working algo to extend...
start of ret = returned value, locs.
Locs is flexible!

Are tail calls always optimal if possible? Consider if the args are already above the stack but
above a bunch of locals... that's fine, just swap back and pop.
x,arg1,arg2...argN ---reordering would cost n. But ret would need to be on the stack as well.
Using empty spaces you could fix dests to be at given constants, but that's quite esoteric...
implement simple opts first.

4 Nov:
Reducing structured statements to single ops, problem: when you have e.g.
block1;while(e)block;block2, the stack target of block1 is flexible.
That's not a problem, the entire function body is part of the same SLC after all.
The while is treated as a single op; block1 and block2 can be reordered around it.
Recursively emit instrs for the loop; the target is whatever layout has been created so far by
the previous instructions, modulo var version updates.
Whether to share loop-constant exprs by computing them before entry is a nontrivial decision
dependent on expected iteration count.

What are the properties that "virtual" ops must satisfy? Ex qualifying: if, while, commutative
and associative fold on words.
For fold or, whenever any two last-use words are TOS you can emit an OR.

Is treegraph really optimal? Whenever you have last-use vars on stack that correspond to a
runnable op (that is guaranteed to be run), you should run it. That requires deleting the op from
the treegraph. What if doing so requires a dup or swap? If it can be done at minimal cost
(given you'd have to pay at least as large an overhead later) and it shrinks the stack, run it.

How to deal with gas? Let the user fix its placement by passing an argument (that isn't DUP'd)
and using its value: x = gas(e).

3 Nov:
OK, got it; implementing.

Code placement: assuming aligned 32B chunks are warmed at a time, you want to pack code that will
be executed given the current code is being executed within the same word.
Prioritize by exec probability.

Thought: GAS is mutated on each instruction. How to place it? Perhaps give it an artificial
dependence on all previously assigned vars. No, that falsely makes those vars multiple use.
Alts: just prevent CSE for gas and assume its result will be wonky; split SLCs at gas.

2 Nov:
While loops don't introduce a dependency between all of their input variables and all of the
output ones... consider while(n--) x++. x depends on n, but not vice versa.
Note the e in while is computed using a series of instructions.
When you have an output var = a + b + c... and any two last-use summands on TOS, greedily add.
Opt: when repeatedly adding and masking, lift the mask to be applied once.
Greater than word-level precision (eg fields in a struct) could be useful for eliminating false
dependencies.
At the end of a while loop's block you must restore the stack layout to the entrypoint... meaning
you need to swap back any modified vars.

Trivial compilation: dup every var use. That risks placing vars out of range of DUP*.
Alt: eagerly consume last uses.
Say next op is op(x,y*,z*) where y,z are deep in the stack. Compute x, move z to pos 2, move y to
pos 1 and dup x. Read that paper!

Reading treegraph paper... a difference from the IR may be that function calls in the IR may
generate multiple output values.
Mem introduces dependencies which aren't on the stack.
I see... they enumerate to find the best treegraph ordering.
How to reduce the cost of enumeration at the expense of compiled performance? Optimize locally,
limit number of alternatives explored.
Something missing from the paper: a multi-word target state. Returns partway through a function,
requiring popping of locals.
Ops with >2 stack arguments, e.g. calls, MULMOD and CALL.

Idea: for a shared dep, find the smallest subtree that contains all refs to it.
Alt: find first use in DFS, place there and then dup. But if it's not the first arg in push order
you'll need to swap it... unless you run the remaining code that uses it first.
That the target is multi-word is a complication.
On a definite last use of a var, you should be able to avoid a dup and pop.

Subexprs with a single use should be placed at the use site... modulo dup range limit.
Return has target retval,ret - not locals,retval,ret!
Do DFS with consideration for shared vars from the bottom word of the target?
Different schedulings of the same SLS may produce different stack orderings.
Problem: invars, SSA ops, target. Ops must be placed in a topological order (modulo recomputation)
and each has a precond on the top of the stack. It's weaker for commutative ops...
Count #uses of vars. Each var corresponds to a tree of the initial vars, modulo memory writes
which should not be repeated.

Greedy generation: when you can pop, do. When you have last uses of an emittable op on the stack
in the correct order, emit it. First, prune unused ops.

swap x 2 costs as much as dup and pop but leaves less garbage on the stack (potentially avoiding
dup out of range). However, it may mess up the stack, disrupting later ops.
One input, e.g. iszero, mload: if not last use, dup; else if not TOS, swap.
Two inputs: if all last use and in correct positions, exec. If swapped, swap back.
(y,x) => swap.
If neither, swap x 2 to put y at pos 2, then swap to put x at pos 1.

Constant expansion isn't always the right choice given code size constraints. Looks like EIP-2926
isn't in effect yet, so code load can be ignored in the short term.
Cross-call constant lifting opt: replace k with param_k in function, add k argument to all calls;
recursively add param_k to callers.
Global/memory lifting opt: replace mem with word(s) which are passed and returned.
Is it worth caching storage in TSTORE? No, hot storage read/write costs the same.
EVM problem: given preheating, the 2300 gas stipend now lets you perform storage writes!
It also lets you perform TSTORE writes regardless. Has it been removed?

global memory allocPtr : {wordpad (Ptr Memory Byte)} 
malloc : UInt 16 -> Ptr Memory Byte
malloc (n) =
 {p} = allocPtr
 p' = (Word) p + (Word) n 
 if p' & (1 << 17)
  then revert()
  else allocPtr = unsafeCoerce p'
       return p
The bounds check must be done on every malloc because if you overflow pointers you can modify
allocPtr. That's if the attacker has control of allocation... otherwise you could do a check at
the end.

If you allow scratch writes off allocPtr, the value of newly malloc'd memory must be assumed to
be arbitrary rather than zero.

Local globals: for e.g. dispatch, you could declare globals within the scope of a function.
Too complicated for now... need to generalize and automate with a good IR.

1 Nov:
The basic problem is this: I have a dependency tree of ops with potential sharing and a list of
initial variables. How do I reach a target stack state most efficiently?
Recall there's literature on that, what was the paper called?

Problem: returns in an if or while. Those correspond to ifs/recursion in the let?
return, call etc correspond to functions applied to the word list.

31 Oct:
Ah, in direct compilation I can't just have locals on the stack... I also need to track the stack
offset. Can locals be interleaved with anonymous words? Yes, if you allow block exprs:
(block {x = 4; localreturn 1}, 3) --3 will be on the stack, but it's not a local.
Note anon words aren't garbage; distinguish between them.
(x,_,y) = e --here the second word is garbage, but it can't simply be popped immediately.

The complexity of managing garbage vs non-garbage words perhaps justifies an IR after all...
preserve structure, greedily decide placement.
Each call starts with the args in standard calling convention order.
Which var use is the last depends on how you order...

(x,y,z) = call(f,args) --without LSCs calls are IR ops. Maybe divide into SLCs at a lower level.
Distinguish tail calls?
End state: each live var in locals is a word on stack; garbage may still be on the stack.
Using push instead of dup removes a dependency on a constant var; sometimes that may be the right
choice. It's always the right choice for push0.
Pass a mem slice into each memory-using op; should malloc create a new slice?
Overwritten memory writes can be omitted, allowing you to eliminate function calls.
mload,mstore(b,ptr) --only depends on/affects the lowest-order b bits

struct eval could be done in field order, in fact that might be most efficient: shl and or.

How are call and return distinct? call ignores locals, but return expects exactly none; it's like
a structured (intra-function) jump with a constraint on the local set.

30 Oct:
Is there any use of jump except while backjump? Structured gotos when I add them... and if then
else.
mem's range should perhaps be part of its type, or a constraint...
Mem * can always be constrained to Mem {field.*}. It must also be able to represent ptrs in args.
Explicitly split pointers at the C level?
For now, just always pass entire mem.
Note mstores may write outside their given range as long as they save and restore the clobbered
part; represent partial word writes in the IR?

Do two different SLSes ever go to the same SLS via a structured jump? Yes, in if then else.
If you assume functions will return, is there any point to not popping garbage from terminated
blocks? Yes, they may contain a useful value.

Do any ops return multiple values? mstore returns only mem, but call returns sto,mem and success.
The IR contains no stack shuffling ops: dup, swap and pop.
Good, because dup is overloaded.
Declaring the return type in Ops is good because it's not trivial: *multiwordPtr becomes
w1 = mload(multiwordPtr)
w2 = shl *(multiwordPtr+k)
Stack layout as a function of size: first grow to the left (downward), when the first word is full
grow to the right.

A dynamic call to an unknown function always takes mem and sto.

29 Oct:

OK, the IR also needs to track locals (which args are a part of) and treat calls specially:
cont = RETURN (stop is just a synonym), revert, call args returnlabel, jump, jumpi
call must end with locals on the stack and args above them; an ordering constraint
Since args is part of the cont, locals = those expected by the return label
tail calls ~ returns; locals preserved = {}
Local info: [Name], LocalsID --for equality constraints
Call args have the same. Why a list? To hint the default ordering.
f() in a S is sugar for _ = f()
Opt: If you inline, you can eliminate wasted pushes

Constant sharing opt: if you know a let var is constant, use the var instead of a new push.

Preserve structure (if and while) while converting to word and SSA form?
CC: ret,args
Word shuffling overhead to get to retv,retaddr from retaddr,retv: 3*|retv|
For () -> a, for sufficiently large a it might be worth storing retaddr in a memory global.

Ignore pointers and anti-aliasing rules for now.
Future: mem has operations m1,m2 = split(regions,mem), m = merge(m1,m2)

28 Oct:
extern function ~ extern immutable
A stack of IRs could make development easier rather than harder... the compilation work can be
divided into multiple stages rather than combining the complexity of all stages into one.

subroutine type : vs : t_is -> continues to expr --(args?)
Calls can be implemented via unification with continues to and AHP; JumpDestOf lab has a single
value.
A subroutine could have multiple continuations expr(args), cf case.
Separate case and casePtr?
tagOf(dt) :: Tag dt --with variable sizeof
case ptr of
 ->Cons <-x <-xs -> ...?
*x *xs wouldn't work, that should set what x and xs point to.
What if you want to nest * access?
case interpreted as generating new var->location bindings rather than assigning new variables
could perhaps solve it...
funArgs := ref --binds new vars in the ref
caseBind *ptr of
 Cons x xs -> ... --x and xs become locally scoped memory variables
Simplest: just have normal case, set existing vars and alloc new ones.

Each t_i is the i'th word of a t. Types: Ptr r a, subroutine...
SLS instrs: k, x = op(xs)
To address: memory ops! Explicitly pass around subsets of state to make independence explicit?
If you know a memory slice is constant, you can replace it with constants and constant expand.
An advantage of SSA: once assigned, it remains constant.
Opt: 
don't pass unused words
don't dup args, instead passing more of the stack and leaving it to the callee to handle it.
Partially inline a function, e.g.
f(struct) =
 x = struct.field
 ...large body...
If struct is large, it can be worth doing the indexing in the caller.

SSA still lets you use the same value multiple times, e.g. the same mem. That lets you replace
known read values with constants.
Note if you pass mutable state then all the ops are pure.

Pull-based instruction placement: start from final branch and tree-shake. Side effects can't be
shaken away unless you symbolically reason about mems to recognize noops.
Each var can be associated with an expr that you can symbolically reason about.
Ex: x1 in scope; x2 = x1; x3 = x2 --eliminate intermediate assignment.
Ex useful thing to infer: whether x is constant, which parts are constant.
How to represent partial mems? Assume pointers are non-aliased? A set of refs; ptr[ix] is assumed
to be a subset of ptr. Then each malloc becomes disjoint for free.
Aliasing becomes undefined behavior.
Would arrays with static size as fields be worth supporting? Not really practical on the stack
except for bytes... for TCI data structures I need dyn-sized arrays.
Structs are an acceptable substitute.

Remember which labels are SLSes; any SLS referenced by a placed SLS must also be placed.
If a label has only one caller, you can inherit static info about its call values.
Both branches of an ifte must take the same stack context.
If the cond is a constant, you can convert it to a straight jump.
Symbolic reasoning about vars can be used to get a constant dest; that's a generalization of
jump label.

How to support lambdas? Just lift them, then they can be tree shaken away.
dispatch is a big case; case *s.pc of Instr1 args -> ...; ... can be optimized to load tag and jump
to it. The args are then at s.pc + k. If you already loaded a *s.pc word, you could maybe get the
args off the stack more cheaply than repeated mloads.
But that can only be done for a single case; hint which one? 
Let the compiler spec tag size and values.
Type synonyms can be eliminated in an isolated step: first unfold the set of synonyms to
Map Name T --without any synonyms, then apply the mapping to all types in the module.

Any function which modifies memory becomes res,mem = f(args,mem)
Then SSA transform!

A monad for choosing alternative compilations and enforcing constraints would be nice... []?

jumpi l x --add another label and end the SLC so you can negate x
mem' = mstore k v mem --mem is of type MemoryRegion
Require updated regions are always returned.
Simple start: just have one mem at a time, pass in by default.

Maybe I should use a module system rather than rely on labels... I have all the data I need.
Simpl: one instance of globals per module, no support for "new instance".

Could you automatically convert allocating code in a HLL to code which takes arrays to alloc into?
Ex: map (f :: a -> b) => map (f :: (Ptr a,Ptr b) -> ())

27 Oct:

What types of continuation do SLSes have? Static and dynamic.
Consider if(e) block;rest --block has cont rest... unless it returns
What about non-returning calls, such as STOP and RETURN?
f(3) --when applying an f, do a special coercion for function application.
Permit coercing ints to ints, but nothing else?
Also struct coercion; ignore irrelevant fields.
So map(f,{hd,tl})

I'll need to extend the type system with datatypes...
NewtypeCon x = nt --should be zero-overhead
Specializing case for C: just case on the top-level con at first?
Need local blocks and returns... is e --local return of e
localContinue, localContinue n --depth
A SLS you only fall through to doesn't need a jumpdest

case *xs of
 Nil -> block
 Cons x xs' -> block
--If the constructor argument is placed leftmost, knowing the tag of a datatype in memory gives
you a pointer to its argument.
sizedWrite(ptr,dt) --uses the minimum number of bytes to store
Custom, variable-size tags would be nice for instructions...

case *pc of
 Instr1 args -> ... pc += dynSizeof(*pc) --constant
--Aliasing is important to whether memory writes affect pc->tag.
Compile data case from constant case?
Tag_Datatype could be an enum.

Inlining:
f e becomes
do pat_f = e
   ...
   localreturn x --convert return to local return
Opt: if you pass (x,y,z), on the first use you can just dup it.
Gen: x -> y; y -> z => x -> z, where -> is flip (=)

Subroutine:
expects pat : t on stack
continues to: index of pat? --a ref
Can mutate and return locals? Explicit return works.
invoke sr (e) --automatically fill in relevant return address
What if it was in memory, such as a branch register? Would that ever make sense as an opt?

Ah, if you have a dynamic instance of a function and a more efficient head SLS with a jump to it,
the dynamic SLS could just do some stack shuffling and then call the efficient version.
The global calling convention for each function type could also be fiddled with...

Why do I need an IR? Structured programming doesn't expose all the SLS labels.

Don't place jumpdests by default at labels; only place if some SLS makes a non-fallthrough goto
to it.

Start by compiling naively directly from EVM C; no IRs. That way I can identify practical
concerns for optimization, especially if I start writing opts at that level.
Try doing type checking simultaneously with codegen.

When deciding whether to inline, I could look at (discounted) exec count vs load and create cost.
Hint branch probability via annotations.

Q: should assembly really handle placing labels? That's conventional... but the codegen monad
needs to handle global alloc.
Global regions: memory, storage. Immutable is just a label. Calldata?
Storage global behavior: pack like memory; storage pointers are byte-addressing.
Then objects may overlap storage words... but avoiding that is the programmer's responsibility.
Useful command: padding for globals.

23 Oct:

OK, what's the simplest features I could support to complete the pipeline?
Pipeline: String -> BNFC CST -> AST -> type inference -> IR -> bytecode

Minimal types: int, a -> b
E: x, 3, f(x)
S: ref = e, return e
ref: local
Int still allows multi-word values... but start with just one-word?

Functions are a subset of immutable exprs; f p = e can be desugared to f = \p:T -> e
Scan from entrypoints for lambdas, place.
Separate entrypoints from functions due to no return?
Functions marked as exported ~entrypoints with a stack context including a return address.
Any function can be goto'd from a block with identical stack effect...
In the normal calling convention, the return address is a valid continuation... how to express
that in a type system?
scont, result
ReturnAddress a --requires stack context a, allows return
Note locals can be eliminated or reordered on the stack; establishContext (...) could force
a particular context. This should be in the IR...
Variables, including globals, are part of the storage context.
"A copy of x is on the stack" /= "x is on the stack", because the latter determines x's value.
Well... the (full) storage context is a bijection between the leaf ref-value mapping 
(language-level environment) and actual storage.
In case 1, modifying the value on stack to no longer be x would change the storage context.
How to simplify AMAP? First expose only functions, don't use a tricky IR.
No reasoning about memory, just perform the read/writes.

Externally set immutable values aren't known at compile time.

First: no opts at all, nothing fancy.
Each local in a separate contiguous series of words; its position remains fixed.
x => dups
var = x => x; swap and pop
a + b (words) => b;a;emit ADD
f(e) --this is why I need tuples

Padding per field: align to nearest word, byte or bit
Code type: (ts) on stk => (ts') on stk, goto label (where label may be one of the ts)
It could also be constant.
If the cont is known you can eval it further... (ts) (Code ts ts' l) => (ts') l
stop() could be detected and prevented at the type level.

f(e); code becomes
f(e) goto cont
Given the label to cont is one of the arguments, you can fill it in automatically.
ret :: Jumpdest code

That's all FW, for now use a fixed calling convention.
First, no stack shuffling: just use a trivial layout.
ret,x,y => x,y,ret:
swap1, swap2 --6
Without changing the order in which you push return words, the swap cost is 3*|words|
The layout at any time = [v:t], including both locals and arguments.

structType#{field:val,...} is equivalent to (structType){field:val}
someInstr(s) =
 parse args off s
 dispatch(s)
ImplicitParams ?interpState : t would simplify... but first write an interpreter vanilla style.
Pragma: autogen JT and instructions from an iset command with instruction specs.
Ex instruction spec: function, argument spec (immediate, stack...), return spec
Ex instruction type: add(memstk,imm): push to memstk
Could it compile to vanilla EVM C?
A 1-indirection JT:
Context: s : S
i1:
return f1(s)
...
invalids...
dispatch(s) =
 goto(jt + *s.pc)
--jt :: S -> ret?
You should be able to goto/tailcall functions with the right return from a function with that
return, adjusting the stack as necessary.
Alt: return dispatch(s)
inline dispatch
dispatch(s) =
 return dispatchJT(s,jt)
inline dispatchJT
The JT must be placed.
x = parse(t,s) --Could be implemented with a hygienic macro rather than &ref arguments.
parse(t,s):
 {x = *(Ptr Memory t) s.pc; --locals in hygienic macros need to be renamed
  s.pc += sizeof t;
  return x --returns become local returns
 }

Singleton t = {wordpad __field1: t}
Field syntax: [padding] [field:] e
(a,b,c) is equivalent to {wordpad a, wordpad b,wordpad c}
(a) isn't representable except as {wordpad a}
Another useful struct sugar: bitfield{...}
New locals don't need type signatures, just infer and coerce if necessary.
xs = (list) {hd,tl} --should work... but don't coerce structs at first.
Coercion scheme for (struct1) struct2: for each field in struct1, if a field with that name
exists use that; otherwise use the same position. Recursively coerce.
If neither exists, set the field to 0.

With trivial layout:
x => dups
x = e => e, swap and pop
a,b...e1,e2 => 2 swaps, no problem
primfun call => custom
f(e) => ret, e, f, jump
That's simple enough!
For now, only support tuples and single vars in lhses.
if(e)
 ...
end 
 ... =>
jumpi !truthy(e) end
...
end: 

In the IR, ifte could have two SLS dests

Argh, I seem to have lost the code where I played around with Generic/Data

Note for interview: EVM C and Asm have strengths and weaknesses compared to Ecomp.
Ecomp is a shallow embedding which supports low-level control and the definition of codegen 
combinators, but doesn't allow analysis (so making object files, using structs, type inference
and optimizations etc aren't possible).

FW: caching object files on-chain and combining them into smart contracts.
For that you need on-chain linking, preferably in EVM C.
Loading and linking could be optimized by storing nonzero regions, interleaving codecopy with
placement of given label values.
Write in reverse to a

TODO support code blobs in the language, return and instantiation of them.
Support placing a pointer at the end for instantiation? __END
A named end per module?

For naive compilation I don't need an IR, but I may in future.
What's the most pressing need for it? I'll discover that when optimizing, but maybe custom CCs.
******

Goal: use parsec to make a C-like syntax for EVM C.
Ideally it would be Haskell-like and general to the point where I can use it for other stuff, but
practice is enough.

e ::= num | var | (op) | \pats -> block | e e | e op e | pre e | e :: t
--e.field subsumed by e op e?
--:: subsumed?

s ::= e | if e then block else block | while e block | lhs = e
lhs = var | *e | lhs.field | e[e]

decl ::= defun | type | data | global

--------------------------------------------------------------------------------------------------
EVM C objectives:
A fast but convenient language for programming ETH smart contracts, in particular batchers and
interpreters.
Low-level enough for speed.

Features:
Structs, datatypes, static types (at first monomorphic using C rules)
Pointers and function pointers, maps
Globals
Typing rules:
Invariant: every leaf expression has a known type, so combinators can look at the type of their
arguments.
Integer literals have inferred type based on size
Structs and tuples have inferred type based on fields (opt: eliminate coercion operator when
you assign e.g. x :: {bar: Bool, foo: Bool} = {foo: true, bar: true}

Interpreter support: JTs
InstrPtr iset --as a type
Autogen dispatch and parsing code from functions
Param location pragmas: on interp stack, immediate...
Generate user-level bytecode from code
If you fuse or make variant instructions (actual iset auto-generated?), that compiler could opt.

Potentially performant feature: local returns, gotoreturn(label,e)
A normal goto is gotoreturn(label,())
A normal label is ::nm:: ()
When returning, pop vars which go out of scope; push 0 for vars which enter scope.
break(e) is return to end of loop; continue should take no arg?
Stmts should have a type?
Reaching the end of a block ~ returning 0 to the top-level label.
Issue: ::l:: = while (e) ... --where does the label go to? The end, otherwise it'd be
::l::; while (e) ...
Opt: if you goto(l,e), don't bother pushing e.
Jumps to labels could be dynamic; then the stack context of the label must be recorded in its
type.
Could you implement Duff's device that way? OH: at least 1 JUMPDEST per label.
You'd need in-code jump tables; if code can be spaced regularly, you can optimize them away.
f(a) :: b ~ goto f(::ret:: :: JumpDest {} b, a); ::ret:: :: JumpDest ctx b
In the callee, ret will have the context {}, expecting a b.
The callee's type context at the start is ret and args. How to differentiate top-level blocks
which shouldn't fall through?
If f continues to ret, it may goto any code that also continues to ret.

Instr functions take and return an iStt. They could also return a different type depending on the
iset's purpose.
a,(b,c) -> (a,b,c) is a zero-cost operation.

From YUL:
Objects
Generate object types, maybe overload optr.immVar = e
p = allocMyObjectType()
Mutate it (including by adding dyn-sized data), RETURN it
Linkable modules
Object types with statically known imports?

Desired opts (do later):
The usual: DCE, constant expansion, loop unrolling...
EVM-specific: lifting to stack, stack shuffling

dispatch via tail calls could let you vary the interp scheme within an instruction.
Recall: &v --on-stack references to a v which let you mutate a ref outside the function's scope.
Implem: pass in each arg as a normal value, add them to the return if you mutate them.
&a -> b --is a distinct type from a -> b, with a different calling convention; you must swap back
the modified value after the call.
Example: _a,b = f(a); a = _a
That could be used for parseType(&st), for example.

To use for:
TCI with Merkles and VSCs

***************

Idea for a general syntax: a typeclass that determines which combinators common syntactic
elements such as integer, op etc correspond to.
To use OperatorTable need to create the table when parsing?
Simplest sol: just don't use general infix ops.

Supporting annotations in syntax: you should be able to annotate decls, stmts and exprs.
To get fixity data for operators, I could do a first-pass parse where fixity is ignored and
"infix* nm fixity" declarations are collected.

*****
Using BNFC to quickly generate a CST... perhaps that forces me to use curlies instead of
alignment. However, Beata's MSc thesis is about supporting that in BNFC.
Ah, but the intro shows that can be done in a limited way (limited how?)

How to support arb-named operators in BNFC? Record the list [term-op], term, resolve later based
on fixity info.

BNFC syntax extension: patterns instead of vars.
LHS: tuple, struct, *e, e[e], var, Con arg.
I need to use GH again... but I'll skip it for now.
I don't get why (e) fails... do I need to add a special rule for parens?
Yes, of course... there are syntaxes where it doesn't make sense to add ()-rules to everything.

Idea for a BNFC extension: just set precedence for rules so you don't have to mess about with
appending N to every type and arg.