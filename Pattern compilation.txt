p = e =>
let x = e
in ecase x of
    p -> x

End result of case compilation:
atomic ::= local | *e
p ::= Con {field: atomic} | atomic

Initial pat:
_, local, *e, e[e]
p.field, p ! e
Con {field: p}

_ converted to new local
ptr[ix] desugared to *(indexPtr ix ptr)
...but does that need to be done before type inference?

Handling accessors:
Con {field: p} (.field | !ix) is an error?

let local.field = e => local = setField e local
local.a.b = e =>
tmp = local.a.b
local = local{a=local.a.b{b=e}}

!ix => setIndex ix e

(*ptr).field => *(ptrField ptr)
(*ptr)!ix => *(indexArrayPtr ix ptr)

Remaining:
local, *e
Con {field: p}

Problem:
case x of
  ps -> conts

If any p is a Con, convert to
case x of
 Con1 locals -> cont1; --here we case on locals
 ...
local and *e are infallible, so any pats occurring after one in ps can be dropped
That can be generalized to a dominates relation, >.
atomic > Con {}
atomic >= atomic'
Con args > Con args' iff one arg > arg' and the rest >=
Con1 does not >= Con2, they're disjoint. disjoint a b <=> not (a >= b || b >= a)
Iff Con is the only constructor, Con args is infallible iff all args are.

The state during decision making is es, not e. The choice of which e to case on first is not trivial
(it influences code size), but I'll just always choose the leftmost relevant one.
Each leaf is a (ps,cont) pair. That could also be represented using a tuple.
When is an e relevant? When there is at least one fallible pattern that can be excluded.

Problem: the order of patterns in the source-level case matters. One should case on the leftmost fallible
pattern of the first case.
Allocate an enum type for each multi-constructor datatype (boxed or unboxed), case on that and bind the vars
using let x = dt.field. Consequence: all locals are bound by lets, cases don't bind vars directly.
Note the es under consideration are locals, so a [Name] can be used.

A consequence of case-bound vars being block-scoped: the same var in different cases must be of one type.
Make tagOf a universal function?

Result:
A decision tree of case local of Con1 locals -> ..., with lets on atomic patterns as the leaves.
Writes to user-visible locals and ptrs are delayed until a particular case succeeds.

Step 1:
Redefine Pat datatype, desugar to it
After desugaring (when all globals have been collected), substitute all globals g for *g.
Con {field: p} must be handled before (x | *e).field = e...

Each Con is associated with a list of fields
Con args =>
 require #args == #fields
 return $ Con $ zip fields args

p = e => let x = e; p = x in x?
By allowing Pats in let lhs I can incrementally desugar.

Note: with named record types pad/align annotations are not required in expr record fields.

In *e += k, e should be computed only once. That also applies to local ! ixe.
*e (.field | !ix)* becomes a single ptr... but that can only be detected after all globals have been
collected!
Solution:
Preserve pattern form verbatim in Desugar.Desugar; add constructs for += et al.
Then: desugar g to *g, Con args to Con {field: p}.
Then: assignment to ecase, e/case to decision tree on enum tags, &_ desugaring, 
atomic (.field | !ix)* compilation.
Does the latter really need to be done before type inference? Yes, because *calldataptr = e should fail the
type check.
&_ should be simple and dependent on syntactically verifiable local expression. All it really needs to
capture is &global and ecase xs of Cons x xs -> &x; do not provide a signature for it in Prim.
To prevent definition of addressOf, rename it to &_.
&_ desugaring converts exprs to exprs, so there should be no problem doing it before or after the last stage.
Forms accepted by &_: *e (.field | !ix)*, local --the atomic patterns.
Consider only the immediately preceding, definitely occurring assignment to local; do not support
foo.bar.baz = *ptr; p = &foo.bar.baz.
Instead, you'd need to do v = *ptr; foo.bar.baz = v; p = &v.
Don't recompute the expression! Need to decompose exprs into subterms stored in locals before applying &_...
Note (*foo).bar.baz expressions must be desugared to a single *ptr before &_ desugaring.

For writeup:
Hindley-Milner type inference allows you to write user-level polymorphic functions, but the tradeoff is that
you sometimes need to help the inference along: result types can't be an arbitrary function of argument
types as in my earlier monomorphic type system. That's reflected in the distinction between [ix] (pointer 
indexing) and  !ix (unboxed array indexing), and in future custom read-write functions for maps.
Storage maps don't need BigPtr results; you could use the hash word and append a byte to it to make a
BigPtr.
*_ : ptr a -> a?
Infer the class type from the instance types via type scheme union? That would allow BigPtrs as an optional
extension to deref and writePtr.

Since *ptr is in lets, writePtr can be defined as
writePtr (p,*p) := ()
local (.field | !ix)* need not be defined using local = ..., typecheck and optimize it as-is?

BCon args =>
 let ptr = alloc 1 :: Ptr r a
     *ptr = StructBCon TagBCon ...args --here calldata alloc will fail
 in coerce ptr :: Datatype params --where params includes r
Need to alloc new tyvars as well as locals.

Each non-union datatype must have an associated union, and each boxed constructor has an associated struct
datatype.
Union datatype => no constructor has arguments
Struct datatype => only one constructor
FW: associate unions with a value type and values for constructors. Only allow tag decls for union DTs!
Tag decl structure: type, con => value
Default: minimal UInt type that fits, values: 0..n.
Naming convention: data TagDT = TagCon1 ... for non-union datatype DT, 
StructCons for boxed constructor cons.
Expose them to the user, allowing them to make a tag decl for Tag<their non-union DT>.

Separating out global, constructor desugaring etc into separate phases after Desugar.Desugar is a mistake...
Instead group decls first. Desugar.Desugar should be the only Desugar module used by Compiler, and should
recursively perform all the desugaring stages (including pattern decomposition).
Dependencies: 
 g => *g depends on the global decls set. Note affected exprs may exist in globals.
  Affects: defuns, globals, statics (now deprecated)
 Con args to Con {field: p/e} depends on datatypes.
Add a <region>allocPtr global for each region (including immutable ones).
Allow immutable region globals; staticdata => code global. Staticdatatype => a datatype code global.
Desugar cons in globals uniformly to static alloc calls.

First:
Group decl types, desugar g => *g
Remove static. 
Overhaul global expr validity rules?
 Valid: k, -k, unboxed con, &global (string is converted to that), boxed con with region Code,
 unboxed array, coerce.
Add type params to Con! Check region (and therefore all global value validity) after type inference.
Boxed con region could be checked after HM rather than expanding boxed allocation immediately.
bdt.field assignment must also be checked...
Alt: desugar .field to *(coerce bdt :: appropriate ptr type).fieldStructCon

Expose default field names? anon<field index>Con
By doing so I can also expose the field names of the struct datatype of the boxed struct.

Patterns remaining after decomposition:
*ptr, local (.field | !ix)* in let
If I allowed (*ptr).field* I could just check ptr's type. bdt.field (in patterns) must either be desugared to
*ptr or treated specially.
Enum tags in case

*****************************************************************

case x of
 Con1 fs -> ...

Each DT (including BDTs) should have a .tagDT field.
bdt.tagDT => *(coerce (bdt :: DT params) :: Ptr r TagDT)
bdt.field => *(coerce (bdt :: DT params) :: Ptr r (StructCon params)).structConfield
So I can apply & to xs.hd, xs.tagList as well.
Note the tag and ordinary field are treated differently, so desugaring needs to be done on a per-field basis.
A field is either a UBDT field (no need to distinguish between tag and ordinary), a BDT tag or ordinary BDT
field.

case x.tagDT of
 TagCon1 -> ...
   --the matching for all patterns with that top-level con

Oh, Con {field: p1, field: p2} can be used for as-patterns.

Which cons to inspect first is not a trivial decision, so doing the decomposition in a desugaring phase is
unfortunate.
Could I add value region info to HM instead? I can use the generalized pattern idea instead!
Con {field: p} => Con {fieldPtr . *_ : p}
Hold up... I can convert each pat -> cont to
(Cons and locals) -> let binds in cont --without deciding case order.
I can then associate the locals with additional region info... but it needs to propagate across
let x = y, .field, !ix.
Note locals may be reassigned...
let x = xs.hd --should set x's region.
The same variable may have different regions/last-assigned exprs in different case branches.
Every variable is bound to an expr *in the event a match is successful*.
Con x = e --becomes an ecase with a single case.
An expr may be assigned to a local only when there is a single branch. What about scope?
That distinguishes missing cases from _ -> revert(); in the general case f(e), always-reverts can't be
determined.
Alt: instead of a case, Con {field:p} = e becomes let x = e; _v = require(x.tagDT == TagCon) in p = x.field.
Observe that exprs in lets may diverge; they should be strictly evaluated.
Don't let case-bound vars escape the e/case? Block-scoped mutable locals and &_ really complicate 
things...

If e.field is converted to *(fieldPtr(e)) in pat and e... that doesn't solve BCon {field: p}...
A composite pattern (fix ps . Con ps | var)

What if I desugared case on boxed cons to
let ImplemCon ptr = dt
in case *ptr of
    Con1 ... -> ...
and allow heterogenous-size datatypes in memory?

Maybe & would have a cleaner definition if &bdt was also defined... but what is it a pointer to?
And what is the meaning of (*xsptr).hd? Desugaring BDT fields to ptr->field makes that obvious.

fieldPtr(bdt) ~ &(bdt.field); it can record the work needed and type info without obscuring info.

Do I need a separate let construct for statements? SE (let bind in e) should be sufficient...
The fact that lets don't have a local effect or scope is tricky...

Note locals aren't persistently associated with an address... it's reassigned on each bind.
Recursively associate locals with regions for type inference purposes:
r, x => x:r
_, BCon @params ps => get r from params, pass to ps
r, UBCon @_ ps => pass r to ps
_, _ => nothing --non-local atomic patterns have no region effect

x = *ptr; --&x will return ptr, but x = 3 will not assign ptr

During compilation, x = *e => let v = e, x = *v, v used for &x until next x reassignment... but I can ignore
that for now.
Type checking also doesn't require explicit constructor tag access during case... though tag and struct
datatypes should be autogenerated. Indeed, they're necessary for typing their use.

Struct-like DT => default tag type is ()
Union-like DT => it's its own tag, produced by coerce. trueCoerce, which raises a compiler error on word
length mismatch?
Allow tag value pragmas only for union-like datatypes. Tags should follow the same anti-cycle rules in
unboxed datatypes as other fields. The leaves should be primitive types... Int?
With con-specific fields I could have {hd:a,tl:b} be valid syntax... but that would require the tag field is
treated inconsistently with the others. Boxed constructors would still have to be treated specially...
better to eliminate that first (translating to a narrow waist) and then attempt to simplify.

Padding and alignment pragmas can be ignored during type inference; they're useful after monomorphization.
Pattern decomposition is also best done after mono.

It's decided: I convert &local to pointers after type inference.
& on explicit derefs can be converted before inference.
& only occurs in an expression context, so I can desugar xs.hd to *(...).structCons_hd and rely
on that.
Valid & arguments: *e, local, valid.field, valid!ix

p++ => let
 ks = let-bind all es in p
 old = p with ks in place of es
 new = inc old
in old