Now I face the hard problem of designing Core...

Phase 1:
Monomorphized module -> Core (deterministic)
EVMC is converted into Core variants in multiple stages.
Rewrites:
Pattern decomposition:
pattern Cons x xs => ImplCons ptr st StructCons x xs = *ptr
Nested patterns => a tree of cases and lets.
Exprs in patterns (p[ix], *e) must be computed once and bound via a let. They should only be computed once
the constructor skeleton has been matched.

State-passing transformation: a single S type = a tuple of all the relevant state types:
each region + ext (the rest of the BC)
gas(e) --depends only on e and G, an arbitrary series of numbers
The only guarantee of gas(e) is that it's executed after e and all gas calls are sequentialized.

mallocPtr as a word-padded memory pointer with a guarantee that it points to free memory.
writeMPtr (MkPtr off) v m = writeMem off lenv (serialize v) m
if cond then x = 1 else x = 2 end --returns an x?

f :: a -> b => f :: (a,S) -> Return (b,S)

Phase 2:
Core -> Core optimization (nontrivial, needs chooser parameter that uses choice API)
If Core functions could be independently compiled down to word ops/Stack, it would be easier to backtrack
based on feedback from that level.

Nontrivial decision: case ordering.

Memory model:
The memory map Mem is a type with operations get, set, split and separating union.
Also new k len?
The Core semantics could be nondeterministic, allowing an arbitrary value to be given for *ptr in
ptr = malloc().
Otherwise, they should be pure; EVM-based context should be passed explicitly.
Calldata is a wrapper around a bytestring... but what is a bytestring exactly? It's a magic repr-less
datatype, just like Calldata bs.

data Return a = Revert Bytestring | RETURN Bytestring | Return a
--Invalid can be considered equivalent to revertValue()
How to deal with gas? It should not force a sync of all memory ops and it should be possible to reorder code
around it... within limits.
while (cond)
 {g = gas();
  s
 }
--Ideally gas() would not be reordered out of the loop

If alloc :: () -> Ptr r a, that would be much cleaner than alloc n... Ptr r a would imply the reachability of
an a, with no unknown n parameter.
Define a separate allocDynArray :: Short -> DynArray r a? Then live data could in principle be inferred from
the values on the stack.
ptr[ix] is an unsafe operation... instead use indexDynArray :: (DynArray r a, Short) -> Maybe (Ptr r a)?
Or unsafeIndexDynArray. Unsafe operations should ideally be generated rather than written by the programmer.

set off len v (m1 ||| m2)
p-->v :: Mem
set ptr v (ptr-->_ ||| rest) = (ptr-->v ||| rest) --modulo the side effecting set which should be emitted

Rewrites:
1) Pass as small a map as possible to any code which uses a map.
split (ks1,ks2,m) = (m1,m2)
2) Specialization: if a function takes and returns a value unchanged without using it, specialize to f'
which doesn't take it.
If a function is guaranteed to return a particular constructor (e.g. Return a), specialize it to return an a
and simplify the cases on its result.

Note optimizations which use alloc should be possible.
Add a bound parameter and exhaustion callback to it to safely use small regions + guarantee no silent
exhaustion? Optimize away when possible.
An exhaustion callback would allow auto-generated intelligent management of spare memory. To add per-callback
state you need a closure...
How to integrate closures with a sound memory model? Perhaps "OO-style" GC callbacks a la the STG.
Could I go all the way and actually implement GC while ensuring adequate performance?
The machine would not be spineless (the EVM stack must be used for unboxed perf); basic blocks would need
their own GC cont.
GC would then traverse the stack, copying it into a buffer in memory and then restoring it.
Restoration could be incremental, with the return cont fetching and restoring a frame before calling it.
Hey, that gives you coroutines for free!
Preserving subpointer sharing is tricky...
Proposal: each pointer includes a pointer to its root alloc entry, which contains a GC callback.
GC operates at the granularity of mallocs.

Overhead: if each GC'able frame must have a GC cont, it must either be in the code (so any potentially
exhausting operation may trigger it, at which point you explicitly branch to it) or before the jump address.
The latter prevents fallback or makes it more costly (min overhead 6: PUSH2 addr, POP), which is undesirable.

Adding full dynamic GC support is unlikely to be worth it given the overhead... but limited, local GCs and a
static model that has a concept of reachability might be.
Ex: n = sum([1..n]) --here the list pointer will not escape
If the alloc ptr is guaranteed to be ascending you could GC freshly allocated values...
Copying GC with sharing requires replacing values with IND cells... but that conflicts with precise control
of DS layout.
When in GC mode, you'd need a write barrier that detects pointer writes of fresh values to old addrs.
No complex GC for now, just O(1) allocPtr reset for code where no allocated data escapes such as
sha3 and call.

Real Core body: 
trueMain () := main(); stop()? 
Then the continuation can be optimized away if main is guaranteed to diverge.
If main has only one call site it can be inlined and specialized.
The allocPtr could also be part of implicit state; modifying it via raw pointer ops is UB ofc.
Keeping allocPtr in a static address in memory is a reverse form of the stack lifting opt.
By making malloc's effect on allocPtr and the initial memory map value white-box you could specialize main
to place static allocs at fixed addresses with known initial values.

Dealing with CALL: each returned returndata could be treated as a separate value a la Mem; reads to it must
be completed before the next overwriting call.
Old returndata pointers should not escape that... tag pointer values with reachable scope?
The Solidity approach is to simply not expose returndata pointers... but that's not efficient if you do
extended processing of parts of it, e.g. interpretation/copying to disparate locations.
A static lock of returndata would be easy to implement... any *rptr to be accessed after it's broken must be 
rescued first.

Typing the CALL boundary: could a RETURNS a monad be efficient? Alt: gather additional data about functions,
e.g. what type they RETURN (stop ~ returnValue (), or any 0-size type).
data RETURNS a t = {RETURNS t}; --the t allows further annotations; RETURNS f would check whether the
property can be inferred for f (easy for a static function; possible using local context for an expr).
Deconstructing RETURNS@[a,tau] t = e --would add ContainsReturning a t to local context.
coerce f = e --pass all the props to local context.
Problem: EVMC types are fixed-size. OR ARE THEY? sizeof is just an overloaded primitive, it needn't determine
the whole language semantics.
An UnboxedDynArray could have an undefined sizeof and be referrable to only by pointer; it could still
support dynsize and *a = *b.
Similarly, Core could well support case on Return despite its halting values lacking a bytestring
representation - it's just that only cases that implement bind are compilable.
Unrepresentable types don't even need a different kind?

Another problem of typing the call boundary: the return type depends on the calldata argument passed... but
main has a single type. Recall WhatsApp's attempt to type Erlang OTP using GADTs. Fixing a single type for
calldata is a simple matter.
With variable-size types supported and an unboxed append for them, complex but efficient memory-safe data 
structures would be feasible.
Unboxed append would require dynSize or dynOffset.

For now don't need to worry about the call boundary... but variable-size datatypes would be nice.
No need to support GC for them.

Critical opts: inlining, tail calls, specialization, CE/DCE, local GC when no pointers escape.
Bytecode-level: fallthrough, code sharing, string sharing.
Additional: shared subexpression elimination, symbolic reasoning about memory.
Drop the additional... I also don't need a full memory model for scratch mem opt... just check no pointer is
returned or assigned.
Symbolic reasoning about memory would let me stack-lift the alloc ptr...

To capture aliasing/non-aliasing info I must add tracking of facts to the optimizer.
Once I do so, Disjoint (p1,p2,p3) could be introduced as data Disjoint t = {Disjoint t}. I'd then need an
additional "fact check" post-mono where facts are required for construction and produced on deconstruction.
Could linearity and uniqueness of a pointer be encoded? Uniqueness implies non-aliasing with every other
pointer.
A linear value may only be placed in a linear container...

************************************************************

What's the simplest Core I can get away with? Start from there.
No opt choice parameter, run deterministically. No symbolic memory reasoning, but make get/set operations
explicit.
Add primitive functions and types fname#, TName# (not referrable to by source)
All exprs referred to by var; allow lets to bind infallible patterns such as (arg,S)

return x => Return@[t] x
Determine whether statements are guaranteed to return (they contain no halting constructor, nor break/
continue); convert them to lets.
while cond s => let (scope',S') = f (scope,S) if well-behaved.
Efficient compilation:
jump check
body:
check:
...
jumpi cond body
--cont
f (scope,S) =
 let (c,scope',S') = <cond>
 in ifte c (scope',S') body (\x -> x)
body (scope,S) =
 ...
 f (scope',S') --if well-behaved
Efficient compilation ~ simply assuming body -> check has a higher exec count than start -> check and 
therefore check should be after body rather than start.

A Core program is a single pure expression.
Constructors:
letrec, let, case, var@t, lambda
Con?

Any subexpr may halt, so Con {field: e} must use bind.
From Haskell Core: constructors become functions (no problem, I can inline them); case remains unexpanded
(modulo nested pattern decomposition) until STG.

What is the minimal state I can pass?
Blockchain context: blockhash etc
Caller, callvalue
Gas (mutable)
Regions: memory, storage, tstorage, calldata, returndata, code
Extstate

Specialization:
Production spec:
If a function f : a -> b always returns Con t, you may replace calls to it with Con (f_spec a),
where f_spec : a -> t.
Integer literals and symbols may be considered constructors for that purpose.
Consumption spec:
case Con e of ... --replace with the let ... in the appropriate branch
Replace ifte with case on Bool? truthy is most efficient when it returns a word... but
ifte (truthy x) can be optimized to that.
truthy could in theory support a dynSize datatype... but I won't.

(range1=>bs1 ||| range2=>bs2) => the fact that range1 and range2 are disjoint.
p = alloc() --implies p is unique
So creation as well as destruction imply facts!
split(range1,range2,m) = select range1 m ||| select range2 m

DynArrays should have their size tag inline rather than in the pointer; that ensures an ordinary Ptr can be
used to refer to it.
UnsafeDynArray a --could lack both dynSize and sizeof. Ptr r (UnsafeDynArray a) replaces Ptr r a as the unsafe
option.
data Buffer a = Buffer {maxsize: Short, actualsize: Short, content: a}
dynSizeof pbuf := return pbuf->maxsize
copyBuffer(from: Ptr r (Buffer a), to: Ptr Memory (Buffer a)) : ()

First just define the operations (including on memory), convert to pre-CPS form.

Could I get away with just using Haskell Core? One difference: my Core is monomorphized.
I don't have any reason to param by b... yet.

local.field = e => local = set_field# e local
Local assignment => a let to a new var

Introduce a partial map type Word => Byte?
Note Storage# needs word indices.

Fateful decision: I'll support nested patterns in Core, deferring determination of one-level case ordering.

Start by supporting a subset...
Append a return null() to each user function definition; that ensures the Return# a is well-typed.
Minimal subset: locals, return.
Each local corresponds to a Core variable.
x = e => x<n> <- coreE e
Context: locals : Name -> Id, scope: Expr, lastLoop: f.
Variables are not inherently on stack...
copy<r> off len off' <r> m =
 setMem off len (get<r> off' len <r>) m

*p = 1; *p = 2 --result: mem' = setMemPtr p 2 (setMemPtr p 1 mem)
That can be simplified to setMemPtr p 2 mem.
The v_1 and v_2 variables to which 1 and 2 are bound are dead (not live), so they can be removed from scope.

Core eliminates:
 nonlinear control flow, including stop/revert/evm_return
 side effects: local assignment, region writes, gas, extstate

Issue: the global variable pointer params can be statically known, so they should be converted to constants.
If &g occurs in a tag expr, the global must be fixed.

Representing constructors as functions: use Append and coerce#?

From phone notes:
Return# ret a --is a RWSE monad!
R: immutable context (blockhash, calldata...)
W: logs
S: mutable regions, gas, extstate
E: revert, RETURN, Return, Expr#

Values represented using coerce#'d Bytestring#s; the consumer in truthy x (where x is a variable because the
A-normal form transformation has already been applied) can result in different concrete implementing
operations (still in Core!) based on whether x is on stack or loaded from a region.
Note: as in System FC, coerce# must preserve representation (but I don't need to check that...)

Globals could be represented as p = alloc()?
A statically sized buffer would allow alloc() of a buffer to store dynsized DTs.
Sugar could be used to treat the buffer as an ordinary global...
Dynsized DTs would be vital for safe parsing off a typed calldata pointer!
Fixed-size arrays could have bounds checks by default; optimize them away when possible.
data AtMost n = AM Short --associated fact: AM x => x <= n
safeIndexArray : (Array len a, AtMost len) -> a

Dynsized DTs can implement grammars. Note that since it uses a prefix parser, garbage in a buffer won't
corrupt a new dynsized value written to it!
Taking a page from Agda:
data Array : Nat -> (a: Type) -> Type where
 Z : Array 0 a
 S : a -> Array n a -> Array (succ n) a
Better than main: using the GADT trick from typing genservers to expose
protocol : Implements MyAPI, where MyAPI is a user-created type.
The calldata type can then be fixed, but what about the return...?
Problem: monomorphization doesn't currently support that.
I'd need local type equality constraints (a ~ Int) within the program:
type Implements api = forall a . Ptr Calldata (api a) -> Ptr Memory a
--No more arbitrarily typed evm_return or calldata!
data MyAPI a where MyMethod1 :: Int -> MyAPI ()
protocol calldata =
case *calldata of
 MyMethod1 args1 -> ... --here a is ()
With protocol + dynsized types, I'm much closer to supporting the Solidity ABI.
SolArray a becomes WordPad (Ptr Calldata (UBSolArray a))
UBSolArray a = UBSA {sz: Word, contents: UnsafeDynArray (WordPad a)}
dynSizeof p = 32 + coerceInt (p->sz) * sizeof (P :: WordPad a)

Should function return be a monad constructor...? I could just use tail calls instead.
Tradeoff: while with a return in it doesn't become a bind. But that bind introduced complicated control flow
anyway...

From mail:
Operations such as *memptr = e; then become (after simplification) something like let mem' = setMem ptr e mem,
and side effects can be reasoned about symbolically!
For example, setMem ptr 1 (setMem ptr 2 mem) can be simplified to setMem ptr 1 mem. If no one else reads ptr 
between the two assignments, the first write can then be eliminated!

Linear data/types such as the mem map support many reads, but only one write at a time.
Furthermore, reads must be done on a fully evaluated var; that enforces the above rule, as setMem is
ultimately compiled to side-effecting code and value dependency enforces ordering.
Note the mem type is not necessarily unique: it can be split, though that consumes it.
Core itself need not be aware of linearity.
Could the same rules/check be applied to pointers?

The mem map must be partial Short->Byte, otherwise it's not possible top split it.

A problem: if Append et al support dynsize types, what to do about padding? The tag can still be in a fixed
position... when loading to the stack, you load the max size and the padding is last!
Only when you perform truthy do you need to mask it.
A type which supports neither size nor dynsize such as UnsafeDynArray prevents access to fields after it...

Idea: instead of manually wiring the control flow, build it from a stack of monad transformers.
Expr -- RWSE which houses the CALL-global branches: revert and RETURN
WhileT t m --effects: break, continue... similar to a Memo monad that makes recursion explicit.
FunCallT ret m --effect: function return
I can't have curried functions, so extending a -> b with an additional c param must => (c,a) -> b
I can prototype the transformer stack in Haskell.
Code can't implement values, only continuations (a,R,S) -> Result (b,[Log],S)...
So is it an arrow, not a monad? Arrows have rec...
Must I use CPS from the start?
x <- m; ... isn't that different from m (\x -> ...)
Note I can compose functions (e.g. (***)), but only if I can statically evaluate the result to a new lambda.
Then a monad might be feasible... the bind rhs ultimately gets compiled away, as do the function 
transformations done in the monad stack (lifting etc).

runWhile :: (m Bool) -> (s -> WhileT s m s) -> s -> m s?
continue :: s -> WhileT s m s
break :: s -> WhileT s m s
lift :: m a -> WhileT s m a
Should the cond be (s -> m (Bool,s))? There's already a scope which m and the body can access...
But its type changes, and we want to explicitly bind the result of a while.

Perhaps protocol should be an explicit monad...
Or the EVMC function type could hide a RETURN parameter.
A function which doesn't RETURN could be forall a. <returns a>, true quantified polymorphism.
I can't just fix a particular return type for all functions, since protocol must support several
different return types.
Purity and linear control flow by default, explicit effect transformers? That's getting a bit too exotic for
devs used to Sol...

Recall from notes: lens p.f es = e (where e must be a valid pattern).
That way I can conveniently assign to indices of Array, DynArray, UnsafeDynArray etc and build them
in terms of each other.
data Array len a = Array (UnsafeDynArray a)
sizeof (arr :: Array len a) = knownNat@len * sizeof a
Boxed fields => lenses... unboxed fields could be slices.
Index of a UDA is not necessarily safe...
lens uda.indexUDA ix = uda.slice (ix*sizeof a) (sizeof a) :: a
Exprs in patterns make lenses strong...

s;
*g = garbage() --could explicitly mark g as usable scratch in s.

Closure conversion: compilable Core may contain no closures. HOFs such as (***) applied to static lambdas are 
simple to eliminate. Values from monadic binds cannot be closed over either; the context passed must be
extended with them.
if/case must ultimately be Scott-encoded.
let x = e
in if x > 5
   then x
   else x+1
In the initial Scott encoding, the two branches can initially take () and close over x; their argument must
be extended with their free vars before they become compilable.
If the monad stack is in CPS, statements such as while explicitly take their cont; the top-level bind 
evaluates to an expr which contains all the info it needs.
Nonlocal control flow encoded using function return, break and continue must be eliminated for compilability.
Do so via specialization? case on a WhileResult passes in continuations; letrec can be converted to a
fixpoint combinator?
Since the letrec-defined values are static, the letrec can ultimately be lifted.
To convert them to function pointers, all closed values (except the letrec functions themselves) must be
added to params.
WhileT takes two conts, FunCall one. What about Expr?

If I start with CPS where all state is threaded through every computation, how to reorder within a do? Give a 
pure def of independence.
f . g is commutative iff r1 . g . r2 . f . r3 ~ f . g, where r1-3 just perform fixed tuple permutations.
Note some closures are compilable - the \x -> f x k in nested tail calls has a stack representation:
f k.

case x of
 Con1 args -> cont
=>
caseN x (args -> cont), then scope is prepended to args.
Why caseN? Because a subset of the constructors may be excluded.

(a -> b) => forall ret cd . a -> FunCall ret cd b b?
Calldata could be another type parameter.
Explicit monads: while as sugar for a monad transformer call that lifts every statement but break and
continue.
User-level closures that must be simplified away?
Still need to support lenses.
Explicit effects: context read/write are effects. Better to use Eff than trans?
There isn't a single memory map... you might want to stack several.
Core parameterized by ret and calldata is preferable to protocol :: Implements MyAPI, since it allows an
EVM return from deep within the call stack.
Hmm... all the regions could have typed APIs.
Memory: a struct of globals + allocPtr : AllocBuffer.
Could the allocPtr point to an "infinite size" buffer datatype with an alloc operation?

C isn't well-suited to exposing all this type info... but I also want to automate the handling of it, so
that's not that big a problem.
However, fixing the calldata and returned type for protocol is highly desirable.
FW: Ptrs tagged by mem?
A linear type system using indexed monads?
Where do lenses fit into the type system? They have a get, set and an m (which may be polymorphic to allow
for "at least effects eff").

Note it should be possible to return types such as UnsafeDynArray... returnWithSize?
Does a return monad open up for longjumps, current function returns deep within a nested call? No, the
unknown depth of the stack gets in the way.
It might enable non-returning functions which don't take a return address.

Keep main for now... I need a simple prototype before considering extensions. Non-typesafe calldata and
RETURN is a minor blemish that can be mitigated with a protocol pattern. An explicit single entrypoint,
allowing shared computation and efficient dispatch, is a consolation prize.
Implement while and return using letrec and tail calls.
A single RWSE a monad remains, with REVERT and RETURN as the only control flow constructors.
With a single monad, lenses become simple again.
(a -> b) => MkFun ((a,R,S) -> Result# (b,[Log],S))
CPS: forall t . (a,R,S, Result# (b,[Log],S) -> t) -> t
data Result# t = REVERT# Bytestring# | RETURN# Bytestring# | Expr# t
bind res f => (coerce# res) f
*Every* expr gets converted to CPS, then most functions are optimized away to lets.
Monad reordering must be done by splitting the input after conversion to lambdas.
Core doesn't care about eval order, but eventually it must be scheduled.
An explicit consuming lambda, linear stores? Actions: create, consume, borrow.
Make a pessimistic approximation of consumes, tag functions with consumption status per argument.
p->field borrows p...
A dynamic store for unique pointers would be cool.
p1 -> Con {field: p2} owns p2 => you can borrow it, but it can't escape once you return Con.
A Rust-like type system which maintained linearity by construction rather than having to reconstruct it from
ill-behaved code would be very attractive, but isn't suitable for a prototype.
More FW: Rust-style unsafe.

add# :: Word -> Word -> Word --no Result# => success guaranteed
x <- w1 + w2 => let v = add# w1 w2

With closures at the Core level, there's nothing stopping me from currying primfuns... if their partial args
are constant that'll go through.

Closure conversion: a lambda without any free vars is compilable.
If it has free vars, it may only be called statically; its context must be passed in and its modified
locals returned. Proceed depth-first. Note that enables e.g. forArray (\n -> x += n, arr), but you
must inline the closure's user! Nice, combinators replacing what would be language constructs in e.g.
JS.
User functions could be curried, though then they'd face closure elimination failure errors; better to
just uncurry.

A CPS-converted while statement must take all its potential continuations: the return continuation k
and the statement end continuation (break).
The continue continuation is the function itself, which should be in scope via letrec - not passed as
a parameter.
Note the Core monad doesn't capture locals.
It may be simpler to just compile directly; that avoids the need to pass continuations to the next stmt
rather than just using the letrec f.
Proceed backward to assign names to fs?

DCE is easier before CPS, though it also has to be done afterward.
Compilable form:
letrec {f args = (let varTup = opE in)* branchE} in main
opE ::= k | f | g | opF varTup
branchE ::= RETURN# v | REVERT# v | f varTup | case v of alts

.field, .field= need to be functions/primops
How to distinguish linear application of a single argument, mem? Make it a separate application and add
LinearFun#.

The memory model could assume tags aren't modified: a Maybe Word and a Byte pointer then won't conflict.
Instead of special treatment of char*, add a RawPtr type that can alias anything?

*************************
Scratch memory use is based on two opts: first, reset the mallocPtr after alloc/dynAlloc, then simplify the
resulting mem' by recognizing writes to the same key.

To support dynSized or size-less datatypes, need dynAlloc.
To parse multiple instruction scripts (load, user, store), use size-tagged instruction buffers or increment
a PC?
next : BDT params -> BDT params
next(bdt) :=
 case bdt of
  ImplC1 p -> coerce (inc p)
  ...
Overwriting the tag of a pointer to a struct type is UB, so you can assume the tag remains constant and
thus next(instr) after/within a case on instr can be optimized.
coerce 0 :: Ptr Calldata t --asserts that 0 points to a valid t.

Assume no tag overwrites when optimizing (otherwise Byte aliases practically every datatype).
Then referred types (t) = flattening union of referred-to fields with recursion on pointers.
Note multiple regions may be referred to!
Need support for word-sized storage and tstorage slots in semantics.
**************************

Is *mptr = v for a multi-word, non-wordsize type optimized by using scratch and mcopy?
Consider *p = short.
old = mload p & mask --12, code-intensive...
new = short << 240 --9
mstore p (old | new) --9, total 30

mstore k short --9 or 8, depending on whether scratch is at 0
mcopy p 30 2 --15, total 24

The same trick works in reverse: mcopy to scratch, then load off constant offsets (cost 3) rather than ptr+k
(9).
The OH of mcopy is 12 + 3/w; even when writing a word-padded value mcopy will be cheaper for #words >= 4.
This shows how important single-word values are: they're about 3 times cheaper to read and write from the
stack than a short.
That's assuming you read the whole value and not just fields.

On-stack field writes are also expensive: (newfield << k) | (struct & mask) --15 in the best case where both
newfield and struct are consumed.
Would it be worth scratch mallocing frequently written struct locals?

xs = range(1,n) :: List Memory Word --allocates only new list cells.
The uniqueness of the reachable set of xs should propagate up the call stack.

****************************

Pre-CPS:
RWSE monad: use the monad laws to flatten nested dos and propagate return or error.
The monad does not take a cont, instead it uses constructors.

k => let x = k in Return# k
f :$ x => do
 f <- compile f
 x <- compile x
 f x
local => the appropriate Core local

The monad does not handle locals; statements return the local scope. Exprs return the local scope in
addition to their value! When a var is passed through unchanged, eliminate it. Note they'll be added back
where appropriate during closure elimination!
ifte i t e =>
 locals' <- case truthy i of
  0 -> compile <locals> e
  _ -> compile <locals> t
Note the ifte can be compiled in isolation; breakup into tail calls has not yet been performed.
truthy can return a Word for efficiency. truthy of a type with no valid falsy bitpatterns can be optimized to
1.
case e of 0 -> a; _ -> b --can be compiled to a JUMPI.
while is analogous but uses an ifte.
while cond s scope = do
 (w,scope') <- cond scope
 case w of
  0 -> return scope'
  _ -> do
   scope'' <- s scope'
   while cond s scope''
--Don't actually define a single while function, instead use a letrec whileN.

Patterns are fallible or infallible. case takes at most one-level fallible patterns; convert to
case x of
 Con1 locals -> (...).
Infallible assignment forms: p = e, p += e, p++. Subexprs in p must be let-bound first, as must e.
Note in case *p of Con1 locals -> ..., the deref of the locals can be deferred; locals are not inherently on
stack.

Add special support for tuple pats and exprs in Core? x or a nested tuple is the only acceptable arg to a 
function or lhs of a monad bind.

No expr modifies the scope (dead var elimination comes later); only var x = e or block can modify it.
(local = x) scope => Return# scope[replace local with x]
local (.field | !ixvar)* = x becomes local = (...) local x; add an expr construct for (.field|!ixvar)*?
State: locals, local => core var.
var x = e --only makes sense in a block; if standalone convert to SE e.

In p->{a,b,c,d}, p->a etc should be known not to alias each other.
More generally, *p (path1) should be known to be distinct from *p (path2) if they're distinct; both should
definitely alias all their superiors.
a ! b: they definitely alias
a ? b: they maybe alias
a . b: they do not alias
How to represent the alias matrix compactly? A tree...
! is transitive, but is (?)?
A* ? (A,B)* ? B* --Answer: no.

getNthPointer : List r Byte -> Ptr r Byte
--If it doesn't unsafely produce a pointer or alloc, the returned pointer must alias one of xs' reachable
pointers.
The same is true if the elem type is A st reachable A includes A*.

Pre-CPS compile subset:
The statements; e ::= (x = e) | x
return x => return# x?
Alt: a FunReturn# constructor?
Recall: pre-CPS Core# ret a ~ (R,S) -> Result# ret a
Result# ret a = Return# (a,W,S) | FunReturn# (ret,W,S) | revert, RETURN
The W (log) supports only emptyLog# and appendLog#
Don't type the log API for now.
A function call will either diverge or FunReturn; a -> b can then be converted to
a -> Core# b ()
call f x =>
 s <- get
 r <- ask
 case (f x) (r,s) of
  FunReturn#  tup -> Return# tup
  revert, RETURN -> propagate
Core cases should be assumed to cover all possible cases; add _ -> revertValue(errmsg) to each EVMC case.

Patterns in the function lhs must be matched in the monad context!
f p := s =>
 f = \x -> do 
  scope <- <match p x>
  s
p should support nested fallible patterns, triggering revertValue(errmsg) on failure.
Disallow (ix,p[ix]); all exprs should be in the null scope.
Convert nested fallible pattern into a skeleton using only locals.
Match local pat takes a list of local, pat pairs and matches the leftmost vs the top-level constructor of
its pat, either failing or producing new local, pat pairs. Once the pat is one level deep, the locals should
instead be stashed for return.
case e of
 (Con ps | k) -> ...
 _ -> ...
Can be implemented as ifte v == k; ifte (v == 0) t e can be optimized further to ifte v e t

The monad hides mem etc; get and set ptr become monadic actions.
The monads should be expanded in a later stage.

For writeup: the tricky part is adapting Core to an imperative context where there are a bunch of inbuilt
effects + the data repr is low-level (in particular pointers exist and functions are pointers, not closures).

*************************************************************************************************************

Always compile calls to f (args, \ret -> cont). That eliminates the need for a FunReturn constructor in Result.
Convert f : a -> b to f : (a, b -> Core End) -> Core End; that eliminates the need for reintroduction of polymorphism.
n => let x = n in …
return e => x <- compile e; k x
A let binding is compilable iff its rhs is not of type Core t; that corresponds intuitively to straight-line primops.
ifte : (Word,a) -> (a -> Core End) -> (a -> Core End) -> Core End
A closure is compilable if it's of the form \ret -> f (Pair ret rest) and it's the last argument of a call.
\ret -> f' ret rest can always be converted to compilable form. 
while loops must take the locals they modify as their recursive argument; additional locals can be added later during closure elimination.

Core takes (R,S), but which parts of state should be returned depends on the Result.
Result a = Return (a,[Log],S) | RETURN (Bytestring,[Log],S less volatile state) | Revert Bytestring
Volatile state: memory, calldata, but not gas.
Is there any pressing reason not to just put [Log] in S? Not optimization; logs are stored in a list rather than a multiset, so logging
statements cannot be reordered.

Modelling CALLs in the semantics: if programs are assumed to have a single given type ~ main:()->() and the mapping addr=>f is known,
the Core monad allows you to reason about the callee.
A CALL will never Return... perhaps the whole program should be modelled as such a CALL rather than just a main invocation.
The real code is ~ main();stop(). If main is only called there, it can then be inlined. The true main takes no arguments (i.e. just an
empty tuple) and no continuation.
Even more concrete: main (\() -> RETURN# emptyBytestring#) :: Core# End#
Memory initialization can also be made explicit, allowing it to be optimized.
That requires a concrete implementation of the alloc and writes.
region g = e => g = alloc(); *g = e
That allows dynamic allocation in global exprs, e.g.
arr = dynSizeArray(…)! The global pointers need to be let-bound and ultimately passed into the main expr.
Note that the actual global addrs can then be subject to GC optimization.
Implementation of globals in Core allows generalization, e.g.
let p = dynSizeArray(…), where p doesn't need to be stored in memory.
You could even access calldata etc!
Problem: how to deal with unsafe dynsized objects during GC?
What does alloc() guarantee? No interference from safe code will be
observed by safe access to the returned pointer.
Code is assumed to be safe when optimizing; p[ix] and coerce can violate
safety.

Pointer values from alloc are arbitrary; only &(p->field) and p[ix] force a
relation between two distinct pointers. That enables reordering of allocs to
preserve the semantics.
Assume p[ix] won't overflow p; don't mask it (it's already unsafe...).

The code mapping is fixed; the overall Core expr for a contract is then
\calldata gas ... ->
  let codemap = …
  in letrec fs = …
  in runCore (main (\() -> stop#)) calldata gas …
That code map can be represented as setCode p1 v1 ||| …, enabling unused ps
to be optimized away. If only a field is used, it should also be possible to optimize that. The final optimized expr should be scanned for setCode to
collect label-bytestring pairs, analogously to setMem (or the lower-level
functions chosen to implement it) emitting side-effecting instructions.

Code generation:
Collect letrec bindings, then proceed from the main expr; place bytecode
there directly.
Result: a true main BB, a f/lambda->BB map and a codep->v map. The 'main'
function is treated normally if not inlined.
Placement: first true main; all offsets assumed to be 16b.
BB + codep placement is nontrivial due to fallthroughs, case jump tables
(which can be implemented efficiently using jump tbl + tag << k) and some
static values being placeable in .text because they contain no PUSH* bytes.
Containing PUSH* isn't fatal, as long as the bytestring either 1) contains
enough subsequent bytes, 2) is padded.

Recall: if tags are a multiple of 16, case can be implemented as
jump ((push tbl >> tag) & 0xffff) --23 gas, not counting tag retrieval
(another 6 for sub-word tags in unboxed datatypes).
Default to that in Desugar.Datatypes when there are few enough constructors.

Region model: the memory and code maps are divided into ranges which may be
freely reordered.
(p,mem ||| p->initValue) = alloc# mem, where p and initValue are arbitrary.
Problem: how to reorder if the entire mem map must be passed?
How to record freeness of p:len?
Lifetimes for static GC: the lifetime of a pointer written to an object with
lifetime L is at least L. Consider sum([1..10]); the list should be possible
to GC, but the Core will turn into a number of sub-funs. How to propagate
lifetime info across funs?
Note valuable opt: if a pointer will not be read again, writes to it are a
noop. Stack lifting can be achieved by symbolic reasoning: p->serialize x
~ v = x. Memory vs stack is just an implementation choice either way.
Full-program global lifting is made easier by first let-binding global pointers, passing them as part of defunctionalisation and only replacing them
with constants if they're given a fixed address. How to do so temporarily?
HM with template poly can capture (L1 >= L2) if you use concrete nats for
lifetimes. There's a relation to ST, though I don't have the necessary
rank-2 polymorphism.

Loop unrolling is inlining. Consider:
for(var i = 0; i<32; i++) accum += byte(i,word)
Using constants instead of shuffling both accum and i is much cheaper;
it also avoids a jumpi.

x.field = e => let x' = setField e x --that doesn't require specifying
whether it's on stack or in memory
--Assuming p points to x
*p = setField e x => p->field = x
Which is cheaper? If field is sub-word and you have x on the stack...

Core shouldn't care about lower-level details, but opt hints for LL codegen
would be very useful. Ex:
pin g/f at k;
one-byte g/f.

(p, p->arbitrary) = alloc# (allocPtr->v) s.t. p new
Need to transitively bubble up new so [1..n] refers to fresh memory.
Should allocPtr be separate from mem?
Every alloc call should be a distinct expression to prevent sharing opts,
but it should be possible to eliminate sequential dependencies between them.
Split the alloc ptr, never consume it or return an updated version?
(a1,a2) = splitAllocPtr a should be called whenever an alloc is done.
alloc# and splitAllocPtr consume ap.

Initial simplification: default tags only (0,15..) or (0,16..), support only
calldata, memory and revert/return.
Consumes x e --tags e (e.g. mstore, call) with consumption info; disallow
parallel consumes, schedule any exprs which use x before e.

Treat UB (unexpected aliasing, invalid bitpatterns) as a first-class outcome
in Result#?
Q: When should invalid bitpattern be UB? If the tag is invalid on case, when
jumping to an ill-typed function ptr. Deref of an invalid bitpattern need not
be UB; it's triggered on use, not creation.

Mem can be a single value initially. Allocs introduce new mems which must be
merged.
Note: with a code cost of 0, any while loop will be inlined until space runs
out.
case_N :: (tag,a) -> (a -> b) -> … -> b

Do I need to let-bind word constants? That would make replacing
*memp = *p with *copy p memp len harder.

Should every statement take a continuation rather than bind?
Perhaps polymorphism can be avoided if the continuation for Core a is
(a -> Core End), the same as for function calls.
Indeed, each statement can be treated as an ordinary function; instead of
the parent function's return cont it's passed a closure usually containing it. Then there's no need for an error monad; CPS is made explicit.
type Fun a b = ((a,R,S,Cont b) -> End)
type Cont a = (a,R,S) -> End
Note the return continuation is not a Fun a b; it takes no return cont.
data End = Return ([Log],Sto,TSto,Ext,Bytestring) | Revert Bytestring
R = {calldata,caller,value,code,blockhash,origin...}
--Also include allocPtr and gas in R?
S = {mem,sto,tsto,ext,...}
The Core type of e :: a is then (R,S,Cont a) -> End, ~Fun () a.
Except assignments must also be passed...
Locals which might be updated must always be passed; that's related to phi
vars in SSA.

Current special vars: mem, cd, ret, allocMem

Track all live mem maps, merge them when passing to a non-stmt fun.
Future refinement: for f (\ret -> …) where f only uses some of the maps,
pass in their union; the cont lambda can remain closed over the rest. 

Merging the mechanisms is nice... reminiscent of my old approach w/ CFG.
Algo: for s;rest, next <- the rest cont, then compile s w/ next and return it. Pass the entire scope to next.
Special cases: 
var x = e; rest --here the scope is extended
continue; rest --the next is ignored; the while loop takes a fixed number of
memory params, so new allocs must be merged into them.

* Allow only x, tup in function LHS, enabling zero-overhead binding.
Each function is of form 
f (scope,r,s) = …
case e of {Con {field: p} => s} @ next =>
 letrec n = next in
 e scope @ \v ->
  let t = v.tagDT
  in caseTag ((t,v,scope),r,s) {
   Con1: \((v,scope),r,s) -> {p1 = v.field1; …; s} @ n
  }
Semantic footgun:
case xs of
 Cons True xs' => … --will revert given Cons False ys
 _ => …

Since the unboxed repr of BDTs can now be one datatype, require each
constructor has a unique tag.
Best case: 0,16... --29
Second best: 0,1...
 jump (JT + tag<<3) -> jumpdest, jump k, jumpdest --36
Worst case: binary search
jumpi (k < tag), jumpdest --19.5 per branch, less 3 for the final one

ifte i t e (scope,r,s) @ next =>
 letrec n = next in
 i scope @ \(v:scope,r,s) ->
  let w = truthy# v
  in ifte (w:scope,r,s) (t @ n) (e @ n)

f x (scope,r,s) @ next =>
 f (scope,r,s) @ \((f,scope),r,s) ->
 x (scope,r,s) @ \((x,f,scope),r,s) ->
 f ((x,mkCont next scope),r,s)
Placing cont in scope ensures the lhs of top-level functions matches those of
statements.
Bind cont to $ret.

return e @ _next =>
  (e @ \((v,scope),r,s) ->
   scope[$ret] v) --takes (scope,r,s)

Simplification: pass everything (S,R,[Log]) as state.
Stages:
1) No effects whatsoever; the final cont \_ -> stop# doesn't need memory.
2) mem, allocMem
allocMem should be split at every point where it might be used, but never
passed on. Ditto for calldata (less the splitting)
Runtime values (vars in scope) are never consumed; whether an application
consumes mem must be estimated pessimistically.

Stage 1): Nested tuples as arguments, e ::= f,x,tup,k; p ::= x,tup
s: if, while, case eliminated
SSA: x = e => push e, statically rebind x.n (where x.n is the newest x to
be declared).

alloc@[Memory,t] (((),cont),(ap,mem)) =>
 let (ap1,ap2) = splitAP ap
     (p,mem') = alloc#@[Memory,t] (sizeof@[t],ap1)
 in cont (p,(ap2,mem ||| mem'))
Treat ||| as an infallible pattern constructor in lhs? Then inlining alloc
should reveal mem is passed through unchanged.
Problem: if Nil should allocate no more bytes than necessary, either
*p = e needs to use dynSize or BDT alloc needs to use unsafe alloc and store
ops.
Nil@[Memory,Byte] repr on stack: {TagNil,0x00_00_00}
Naïve load and store require shifting.
Disallow types without a fixed sizeof for now, enabling all to reside on stack in principle. That forces reliance on unsafe pointer indexing for
dealing with dynsized values (e.g. bytecode), restricting when GC can be done.
data Object = … | OArray {sz: Short, buf: Array 0 Object}
Restriction: if you do a raw alloc (OArray + content) and coerce the pointer
to Object* and it escapes, you can't GC the "unused" part.
coercePtr : Ptr r a -> Ptr r b --Full coerce : a -> b should not be considered a pointer escape, but coercePtr should.

Global allocation: convert all alloc#(n,ap) with fixed ap from root to fixed
addresses. Constant let bindings outside the defun letrec should be allowed?
What about static scratch allocations? Inlining of main may expose new static
allocs.

pointed(p) : Mem <= reachable(p)
If p[ix] is used, the original allocated pointer must be large enough to fit
p[ix].

I need to use tuples in Core, but C treats them as Append (WordPad …) … 

x,y,z,ret => ret,x,y,z code:
swap1: y,x,z,ret
swap2: z,x,y,ret
swap3: ret,x,y,z
Because there's only one cycle, swapping is cheap.

dup1 (x,scope) = (x,(x,scope))? No, values may be multi-word.

Pure primfuns are relatively simple:
add (((a,b),cont),env) =
 let x = add# (a,b)
 in cont (x,env)

Letrec in compilable form should only work for constant expressions.
Globals need to be converted to constant pointers, but their identity info
needs to be preserved; letrec solves that.

Local scope is constant within Es, allowing it to be treated as monad state.
State: scope, env.
All statements but var x = e; also avoid modifying scope.
By restricting function lhses to x | tup, the params can be assumed to be on
stack with the same repr as locals.

How to enable variable calling conventions with Core?
An obvious one: noreturn. Limited effects: no mem etc.
How about no halt, guaranteed return?
GReturn env a b ~ forall c . ((a,(b,env)->c),env) -> c
With f x implemented as f ((x,cont),env), you could call GReturn Env normally
in ordinary code.
For nonstandard env you'd need to overload the effectful operators or use new
ones.
Could you overload stack push to use memory instead?
First things first...

Key opts: inlining, dead var elim, passthrough detection.
How to detect
f() := return f() doesn't use $ret? Functions form a call graph that can be
SCC'd. No return in the subcall implies no return in f... classify fs into
definitely and maybe returns. If f returns, it must be provable that it can
from the definitelys (assuming favourable cases/conds).

Tail call opt: f ((x,\(v,env) -> $ret (v,env)),env) => f((x,$ret),env) by
eta reduction. Which empirical opts fall out naturally from LC opts must be
documented for the writeup...

Important to note for inlining: the ultimate effect on code size depends on
the effect of specialization on the inlined body.
Loop inlining: inlining from the call site differs from inlining in the body;
the former produces constant indices for for(var i = 0; i<k; i++), the
latter produces a longer loop body.
Limit number of nested inlines of a single fun to prevent a 10-iter loop
from taking arbitrary code size?
Symbolic eval useful for loop unrolling: commute, assoc of sum to enable
constant expansion.
f x = while(cond)s => f ((x,cont),env) = letrec g = … in g ((x,cont),env)
After lambda lifting of g, that becomes f = \arg -> g arg, which can be
eta-reduced to f = g. At that point occurrences of f can be replaced with g.

Not making constructors C functions (instead Cons :: (a, List a) -> List a in Core) seems the right call; they're straight-line, pure and don't halt.
On the deconstruction side, Core sees fields and tuples, and has access to
case funs. It does not need to be aware of tag values or data reprs, modulo
optimizations.
case fun type: (Pair tag scope, Env) -> 
 [(tag, scope -> End)] -> (scope -> End) -> End
Constraint: the case tags are constant. The default tag is essential,
otherwise ifte can't be implemented as a caseTag.
Special case: case n of 3 -> … should discriminate on n itself, not its unit
tag. Should every type even have a tag..?
data Fun a b = Fun (CoreFun ((a,CoreCont b),Env) End)

In case (t,cont) of caseTag tag, it can be assumed tag == t.
That should enable opt of dynSize xs within a case on xs (relevant for interp) by symbolic eval of mem (if (xs.unImplList)->tagList remains unmodified, the == constraint should propagate to its region of the mem map).

Name info: should I do an initial pass where I replace Name with
g, f, x.n? Con and index (.field | !ix) could also be tagged with a param to
SEP. For simplicity, all names could take a [T] type annot after HM -
including locals.
Is it worth making that distinction in Core?
Just use the monomorphized output I have... functions and globals are
translated directly, locals given an index.
Invariant: every compiled E has the arg pat (<scope>,<env>); ditto S (though
var also updates the scope).
x @ next => ix <- current x version, next x.ix
Only when an expr has sub-exprs (:$, Con) does arg vary.
Prior to optimization, scope always has $ret last.

Top-level function LHS: ((args,$ret),<env>). The args are the C args with index 1.

When inlining and specializing, a graph of mutually recursive functions can
be specialized at once; that's necessary to ensure the specialized function(s) can recursively call themselves. The top SCC is the minimum
recursively specializable set.
Only do simple one-step inlining first; when inlining f = body, rename all vars bound in f except f itself. That should be done after lambda lifting, so everything but the basic block is free in f.
Invariant during lambda lifting (done after globals have been letrec-bound): no letrec-bound lambdas close over anything but higher-level
letrecs (including themselves). That ensures naive lifting is safe.
Use mkCont f scope instead of an explicit return lambda.
Core primitives such as mkCont do not need to be in the letrec, but e.g. stop should.
Inlining (\(a,b) -> body) tupvar: \(a',b') -> body' <- rename lambda; let (a',b') = tupvar in body'. Nested tuple let-binds are zero-cost for values on stack, since they just compile to stack word renaming.

Static alloc of globals and static allocs from main: track concrete alloc ptr, replace "leftmost" (alloc#@[Memory,t] len ap) in terms of
split path with a constant allocated off the ptr. Further inlining of main and its callees may reveal more static allocs, but only if ap
has a fixed path from root.
Global alloc should use alloc# directly since there's no return cont set up before the main call.
writePtr# could also be used directly. Then code is generated outside a lambda...

OK, enough planning; let's go.
Replace data Fun a b def?
Pair is now the wordpad primitive, wordpad is Pair Unit.
Permit arbitrary patterns in C function lhses; implement as
\x -> {var pv1, pv2, ...; p = x; ...}. That requires scanning p for vars, but
also optimizing away the dead null()s in the declarations.
Yet another reason not to have primitives in letrec: they might appear when
implicit ops in the C are expanded!
So I need a separate primfun set from Prim, since Mono will prune functions
not visible in the source.
It should be possible to inline primitives definable in Core such as
stop (_,_) = Return# emptyBytestring# --compile to STOP when encountered
Note any deref of a zero-size type should yield emptyBytestring#, so
evm_return(0,p) or returnPtr(nullp) should compile to STOP.
For (scope,env) to get the right runtime repr, env must have a size of zero.
However, that does not imply that there's only one value of type env, since
toBS and fromBS should not be defined on it.
Invariant: for all supported types, (toBS . fromBS) ~ id.
I'll use 0,16... as the default tag when there are no more than 16
constructors. Why not 0,15..? Using 16b ensures the case jump addresses are byte-aligned in the code, 
Bool therefore needs an explicit tag to ensure iszero etc return valid Bools!
Fortunately there's no runtime overhead to using 0,1 since case should compile to ifte then anyway.
No... datatypes with 2 constructors should always default to 0,1... no need
for a tag decl just for Bool.
TODO fix default tags: 0,1 : Byte for 2 constructors, 0,16.. for 3-16
(using direct JTs), 0,1.. for >16 (using indirect JTs).

Append a b = <ab> --requires the result of ab be saved on the stack for both
a = v.fieldAppend1; b = v.fieldAppend2.
While exprs in the patterns a, b (e.g. a ~ ptr[ix]) should be evaluated before <ab>, if a ~ Nil a binding attempt will involve a deref.
Keep a single scrutinee TOS during complex pattern-matching?
Complex => not just x | tup. Unit-like types and enums don't need a scrutinee either, though they could be viewed as having a unit scrutinee.
Nested fallible patterns require an ifte (tag == k)… but that's fine, it should go after the one-level scrutiny.
case xs of
 Nil -> cont1
 Cons <x> <xs'> -> cont2
=> v <- eval xs, coerce to Ptr r (ImplList r a)
--boxed should imply tag = ()
caseTag (v->tag) {
 Nil: cont1, 
 Cons: 
  ixs <- deref v --scrutinee should remain on stack
  <x> = ixs.hd
  <xs'> = ixs.tl
}
That should be optimizable if e.g. xs' is _; p->field (the Core op, not C)
should be convertible to deref# len (p+offset_hd).
More generally: slice off lenField (deref# lenStruct p) ~ 
deref# lenField (add# off p) --if lenField <= lenStruct
mload (p+k) >> k --18;
(dup << k >> k) --15
3 gas can be saved by coalescing loads, but a further 6 can be saved if the
sub-word field starts or ends at one of the word boundaries.
Should Core be aware of that?
It would be very nice to back-compile word-level IR to Core to run word-level
and Core opts in a loop...
Note x and xs' are patterns; the <x> = v.hd is branching.
At each scrutinization of a sub-pattern, push the value as a new scrutinee.
Function: matchAssign p v --not e!
p should contain only var exprs; eval and push the exprs first.
case v of
 Con1 fieldes -> …
 Con2 fieldes -> …
--In each ConN case, retrieve the field values and matchAssign them in
textual order.
Extension: since I'm already committed to O(n) matching on the fallible
skeleton, might as well transition to the next case with the same con.
The exprs in the fallible patterns should then only be evaluated once the
entire skeleton has been validated; push all their vs and match in order.
onStackArray[ixVar] can be assigned using a JT, but that's at the word level.

Some datatypes are tuple-equivalent; their explicit let bindings should be
optimized away to renaming. Important opt: if x1 == x2, no need to keep both
on stack.

Storage globals can also be alloc'd (their addresses will remain constant,
after all), but their initial value can't be assumed to be 0.
The storage alloc pointer should be set on creation rather than on each call;
if it remains 0 then don't write to it.
Explicitly store the malloc pointer in memory, with an added assumption that no other pointer aliases it?
Preserve both identity and exact value info for data and function pointers;
incrementally place code so that the remainder can use the known offset for
optimization. Backtracking refinement search for opts could be worth doing; the top few decisions could have a lot of impact.
Cache exec counts, retain while deciding which basic block to place.

TODO: add a step before Core which converts f generalPat := s to
f arg := {generalPat = arg; s} in C.
TODO add a Maybe T type tag to PWild so I can avoid allocating a false local
while keeping pattern types unambiguous post-HM and mono.
HM certainly doesn't need the false var, since it only ever gets used in the
wild itself.

Now that I've replaced WordPad with Pair as the word-padding primitive, type
checking has ofc been broken... need to redefine AST.DTs .Pair, tupleT,
tupleE.
No such constructor WordPad... redefine it as (a,()) instead.

Unlike statement Core funs, expr Core must deal with multiple non-locals on stack.
Ex: (e1,e2) --while evaluating e2, 1 is already on the stack.
However, that's not a problem: the local names can remain the same.
A bigger problem: values are on the stack in reverse eval order, so
a - b must perform a swap in the general case.

Generalizing static alloc: multiple allocs within the same basic block
(which can grow quite large thanks to inlining) can be converted to
p + k, with only one allocPtr store at the end.
Implementing alloc without an allocPtr global: detect allocation by looking
at whether the ap parameter changed in the cont vs lhs.

For writeup: using LC and describing EVM effects in terms of pure capab passing enables opts to be grounded in a well-studied theory; opts such as tail call fall out for free. 
Making the continuation in calls explicit distinguishes between straight-line
and branching code, as well as enabling opts when a cont is never used.

Now I need to specify exactly how statements behave...
Compile blocks of stmts?
The next cont should be a closed lambda; it gets pushed down in
compile a;rest. a takes rest @ next as cont.
The next of the body of a while block is the loop function.
SE e --pops the v, so the cont needs a different lhs… spec only the rhs in
compile?
Pattern matching: in Pair p1 p2 = e, split using a let:
e (\tup -> let (v1,v2) = tup in cont (vs...scope,env))
Note that has no runtime impact once the intermediate f is eliminated at the word level.
For both Pair and nontrivial Cons, recursively match on the fields (keeping
the original scrutinee on stack while doing so).
Match effect: e:scope => scope.
Handling boxed cons in Core makes sense, since doing so in C requires
declaring a number of useless intermediate vars.
Perhaps not implicitly declaring new vars in case bindings was a mistake...

() => push Unit:()
Pair e1 e2 => push e1, push e2, \(e2,e1...scope,env) -> Pair es:scope
--where the order of the es depends on field order; replace missing fields
--with null#@[t]
ConRecord con ts nmes => (v1,v2..) <- eval tup es, con@ts (v1,v2,...) …
letrec tagCon = …
       Con = \vs -> fromBS (concat $ map toBS $ tagCon:vs)
Alt: treat Con as a primitive
Add a Pair constructor to Expr without type tags? That avoids the need to
track an O(n^2)-sized list of type tags in the scope.
Invariant: the cont application should be ANF. Letrec-bind each next?
If ANF includes primfuns, it might as well include integer literals (which are akin to primfuns).
If mkConf f scope must also be let-bound, that makes stack-compiling calls a
little more difficult... but it might as well be permitted in ANF since it
only occurs in conts.

Relevant simplifying combinators:
pop scope next = \(_:scope,env) -> next (scope,env)
bindE "$varname" (compileE scope e) (\v -> …)
Perhaps split out scope effect.
Also need to track Maybe (break,continue) conts.
How to do this more elegantly? Monomorphized C expressions and statements are
polymorphic in scope and break/continue. If $ret were in a separate slot from
the locals (local,$ret,env), polymorphism in ret would be easy as well.
I could do an initial traversal which annotated each stmt, expr and pat with
its scope. Issue: &_.

The next lambda is never a closure, so it should always be letrec-bound to avoid code duplication in e.g. ifte, case. Its scope should also be recorded; it can't always be found from inspecting the block given the next.

Solving the f :$ x circularity conundrum: f knows its own type, so it knows
the type that should be attached to the next Name to make it an Id.
Can/should I avoid eDepth using fresh names?
(e => cont) lhs next =
 next' <- new name
 (lam,t) <- e lhs next'
 v <- new id : t
 lamCont <- cont (v||lhs) next
 return $ letrec next':cont lhs t = lamCont
          in lam
Aside: ifte@[argsT] (w,t,e,args) is directly stack-implementable.

It should be possible to make a codegen monad for S and E.
E: locals and version map remain constant. Common pattern:
x1 <- subExpr1; …; f x1 … xN
S: only locals on the stack; locals remain constant except in var x = e; rest.
Every subexpr has a next; it's not necessarily unique, consider a && b.
continue e, pushScope e.
letBind id e
setLocal nm e
v <- m --pushes v to scope in cont

SE e =>
v <- subExpr e
popScope
Each S/E takes and updates a scope and may call a cont...
What happens if a substmt doesn't use a continuation? That happens for return, break and continue. 
Each subExpr adds another layer to a stack of expr transformers... leaf could ignore its >>= f. While loops should still work because ifte
should catch the leaf.
letBind can be a within-BB expr transformer. Have a BB submonad?
For writeup: Armstrong said first make it work, then make it beautiful... but if the problem's too complex it must be made (more)
beautiful before it can work.

Allowing let x = x + 1 shadowing would simplify setLocal but perhaps necessitate a SSA pass.
Let's say I don't deal with names in the monad, instead accessing args via index...
vs <- consume n --modifies scope to remove them
push (Con vs)   --let anon = e; push anon to scope
v <- letBind expr t

continue =
 s <- getScope
 n <- getNext
 leaf $ App n s --report next and scope type
--?

Block:
s <- getScope
mapM compile stmts
setScope s
continue
--foldl1 bind stmts `bind` resetScope
foldl allows you to add statements one at a time
A compiled stmt should report its next name and resulting scope (if any).
s1;s2 scope =>
(mcont,lam) <- compile s1
case mcont of
 Just (nm,scope') -> do
  (mcont',lam') <- compile s2 scope'
  return (mcont',letrec (nm:contType scope) = lam' in lam)
 Nothing -> (Nothing,lam)
Effect: Scope -> Either Expr (Name,Scope,Expr)
That's a monoid... it doesn't need monad functionality?

ifte could take two monads.
ifte (w:scope) mon1 mon2:
 e1 <- mon1 <> fixScope scope
 e2 <- mon2 <> fixScope scope
 if neither continue, ifte does not
 if either do, the next scope is scope
 assemble into ifte e1 e2 (w:scope)
Using a single $next (that gets shadowed on each nested letrec) would simplify stitching branches together.
Alt, for branch ifte th el scope:
 --with conts th,el = n1,n2
 --cont ifte = n
 ifte (letrec n1 = n in th) (letrec n2 = n in th)
An arbitrary next should be generated for each Core monoid.
It needs to have access to the pool to give unique nexts to ifte…
Split the pool to avoid sequentializing it. <*> would fit that, if not for
the return constraint making it not an applicative.

Call is pretty simple: it still has a next, it's just in a mkCont.
With the exception of call, Core with opEs and branchEs (desirable to prevent
let x = stop# in e) will be quite similar to CFG.
However, the distinction may hamper uniform application of LC-specific opts;
perhaps two levels of Core would be desirable. OTOH, it should not be possible to break that structure and adding more structure to LC might even
help optimization...

bindNext exprResult exprBuilder/expr

jump :: (Pair (Cont a) a, Env) -> End
type Cont a = (a,Env) -># End
--End fixes the problem of typing jump; all CALLs must end, so that must be
a continuation's type. C calls are jumps.
Fun a b ~ Cont (Pair a (Cont b))
call :: (Fun a b:a:Cont b, Env) -> End

Scope: {e stack: [T], locals: [T], names, vermap}
pushWord, 
BB: \vars -> letrec ks = kEs in let vs = opE in branchE
branchE ::= jump f arg | jumpi f1 f2 arg | caseTag tag2f arg

v <- bindE e --keep v->stack var stable across the continuation boundary...
A block combinator for the monad to scope anon locals?
A single monad for both inter and intra-BB code?
loop <- label --as in Ecomp, but typed!
Structured patterns => combinators
call => withCont (\cont -> …)? That cont would become break in while.
Need to specify the expected scope.
Exprs continue to cont, but if they branch (call, ecase, &&, ||) then their
return type is already known.
With optional branching, stop() could be compiled directly.

TODO for write-up: a succinct, elegant explanation for the distinction between straight-line code (opEs) and branching code (branchEs) and why the
data structure should enforce it.

There's a correspondence between the low-level representation of the C1/Ecomp
monad and the high-level representation of Core:
x <- op v1 v2;
branchingThing;
--can be translated to a functional form.
Optional branching that's transparent to the monad would allow
x <- compile (EInteger n)
… --where the compile doesn't force a transition.
Aha! Pass vars on the stack (in order of decl) iff they cross a branch
boundary.
So what does n <- e mean when there's a scope underneath?
How to detect var liveness? It escapes if it's used or passed.
Use Ecomp-style monadic combinators that assume valid structure.
block m...
val <- e --an immutable value, constant and passed if used across calls
var <- declare e --a stack variable which may be mutated.
x <- e --may either branch or be straight-line...
But both (let v = e in) and (e where next =) are both transformations!

case e of
 Con ps -> s --the cont for tagCon will have a (Con vs) TOS
--However, all statements can assume no anonymous es on stack

Need combinators:
v <- compileE e
--Should the es necessarily be pushed to scope or should I handle that in a
defunctionalisation step? Always pushing is simpler, then the cont is always
a lambda.
v <- newVar t --not bound yet!
letBind p v
v <- op o val
ifte, while, break, continue
block, declare nm e, getVar nm, setVar nm
compileE e --returns a var and pushes it to the scope; it should clean up
--pushed subexprs.
assign cPat --pops the scrutinee
p = e =>
 v <- compileE e
 w <- dup v
 assign p w
 return v
assign (Con ps) v:
 checkTag Con v
 for fields in Con:
  assign p_i v.field_i
--The last .field op can consume the v...
e always takes a next; specify the type to retrieve it
n <- next scope
l <- label scope
jump l --passes suffix of current scope; only works if expected is a suffix
placeLabel l

withNext scope --should hide the enclosing next

leaf <> _ = leaf
branch <> leaf = a leaf
branch1 <> branch2 = a branch expecting branch2's cont type
It might be simpler to construct the CFG first, treating calls as ops, then
CPS the CFG (splitting BBs containing calls).
Lambda-lift all BBs immediately. As before, liveness analysis can be used to
determine which vars belong on the stack. Pass live vars in an arbitrary but
deterministic order (TODO arg ordering opt). By order of declaration would be
preferable to alphabetic...
An initial unshadowing pass can give each semantically
distinct C local a unique name.
Exprs become a series of ops, with the exception of && and || which become
iftes.
Note case and if have live set = the union of all branches.
(v,env) = call f x --becomes f ((x,mkCont next scope),env), 
where next expects (Pair v scope, env).
No user code explicitly modifies env... save it for later?
Use declared scope and explicit setScope instead of live.
Patterns: expr (pushes 1), block (0), ifte (pops 1), assign p (pops; = dups
a copy).
case e of Con ps -> s => block $ do
 x <- expr e
 t <- x.tagDT
 caseTag t [("Con", --t has been consumed here
  assignCase (Con ps) --x is consumed here
  block $ stmt s
 ]
No user code in the structured IR can diverge; that's delegated to functions
(stop, revert etc). The pre-CPS CFG therefore only needs jump, jumpi,
caseTag and return. Call becomes a jump with scope = (a,Cont b) post-CPS.
if cond then f(x) else g(x) can be optimized to avoid a jumpi->jump, so it's
not the case that interprocedural calls only use jump post-opt.
Change from old structured IR: ops aren't word-level yet.
Implicitly declare on assignment to var not in scope. Scope should be a
(stack,env) pair from the get-go.
Bind labels in the CFG.
(b,env) = call f a --scope must be defined at each point in a BB for it to be
possible to split the BB here.
Pattern-matching:
Con ps (fallible or not), (x | *e) (.field | !ixE)*
Special cases: Pair, Unit.
The tag check for (Con ps = e) is branching; that can be translated to
tag <- dt.tag
neq <- tag /= tagCon --tagCon is a letrec'd Const
if(neq)revert()
--revert needs no $ret; it should be possible to avoid cleaning up the stack
and jump to a specialized (jumpdest,0,0,revert) directly.
Possible solution: if a cont is never called, you could mkBadCont scope to
ignore the stack. Alt: ignore scope :: ()
A function which never returns may still make use of the stack; in that case
it may be best to pop instead. Stack depth can be statically known for the
non-recursive skeleton of the call graph from the main call; note static tail calls preserve that.

a && b --either returns null or b; the TS should reflect that
ret = null --just used for scope; gets optimized away
if a then ret = b else ret = null
The structured IR must provide scope information, but need not care about it.
A nice advantage of it: by breaking up exprs into a series of IR stmts ending with var:t := op, the type of the next cont becomes obvious.
var := E irs?

First: unshadow locals. That must be done in C rather than IR if declarations
are implicit in IR. The scope rules will be duplicated...

All execution occurs in the context of a function call; there's no such thing
as a naked expression.
Then perhaps it makes more sense to reintroduce a CFG-style map and have the
true main be a function in it.
Format: Map Var Const. Change: labels are typed and in scope
across functions.
Const = Lam (Scope,[OpE],BranchE) | ...value consts
Since that's so close to the CFG repr and the stack is known at each call in
the structured IR, there's no point in having a separate CFG stage.
Con: porting opts from LC becomes a little less straightforward.
The IR needs $mem etc if primitive deref, writePtr (used in patterns) are to
be represented correctly.
Param the CFG by op type (first RHS, then OpE).

Does Core need to contain sufficient info to compile the program?
No, datatype tags and field offsets can remain abstract. Global offsets and
static data can have undetermined (label-based) addresses.
Code pointers are necessarily abstract before code placement.
case needs to specify the set of possible values; the set can be restricted
further by local context:
case bool of
 False -> 0
 True -> case bool of
           --here there is only one possible value

New construct: Label Name T --an undetermined value, not part of the Core
semantics.
L -> {0x01,0x02,serialize (L+k)} would let you add (external) linkable code to the code map. jump (MkFun L) ((),env) --lets you jump into it.

trueMain passes the code map... pass it as a (|||) union of (lab,len,content).
It might as well be defined using a normal series of lets; after per-BB SSA it can easily be reconstructed.
Do not include .text (the lambdas) in the code map, that breaks the lambda
abstraction and interferes with opts.
Mem map: representing mem fragments of dynamic allocs is not a problem:
they're just (ptr,len,content) with a dynamic len (and arbitrary content).
alloc# (len,ap) = (ptr,len,content) where ptr doesn't clash with any
existing pointer and content is arbitrary. There's a conflict between keeping
the implem abstract to make use of its properties (no conflict, commutativity) and using a concrete implem with its own specific opts
(e.g. coalescing alloc ptr bumps in BBs).
Simple implem: add allocPtr:Word(?), bump alloc all potentially escaping ptrs
in BB, scratch alloc only those that die within a single BB.
Implement *_ in terms of coerce# (get<R> off len r)?

Const format: label, k:len, coerce# (?), ++
Con x y z = coerce# {tagCon,x,y,z} --except for Pair, which inserts an
Array len ZeroByte.
Arithmetic based on labels/consts could also be allowed, modulo cycles.
Consts must be convertible to a sequence of bytes and arithmetic functions
of labels.
For now, restrict to label and bytes; keep funs and globals (which must point
to consts if they're Code) in separate maps. No need to define addresses for
the globals yet, but the memory globals need to be assigned.
Global rules:
 memory --may have initializer
 storage, tstorage --must not
 code --must
 any other region: forbidden
Enforce the above in Desugar.
Static values are simply anonymous code globals with a set value.

To compile case, need to explicitly store tagging scheme (Nil, N1, N16, Custom) rather than just a Name => E map.
Store tagging scheme and the full tag set info in DTInfo rather than ConInfo.
Update BDT scheme:
data List r a = Nil | Cons {hd: a, tl: List r a} region r =>
data List r a = ImplList (Ptr r (ImplList r a));
data ImplList r a = ImplNil | ImplCons {implList_hd: a, implList_tl: List r a}
ImplList's tag scheme is that given for List (either default or custom).

TagScheme = Nil implying no tag field at all is an improvement; giving
Array, Int etc a tag was ugly.
Support integers in patterns; matching is just an equality check.
Core would benefit from conversion of tags to linkable data: a mix of bytes
and labels. That would enable case => ifte opt when the non-_ case has a zero
tag. For tags containing labels (functions and code pointers), case must be
compiled to a series of iftes with no guarantee of disambiguation.

TODO update Cons x xs, xs.hd desugaring to fit new boxed datatype convention.
I mapM Maps in a lot of places... TODO define and use a single combinator.

Changed MonoS to record a tagScheme per monomorphized datatype.
Debugging:
main _ := Nil --fails
 The boxed cons weren't added. Without a con tag, UBCon and BCon now have the
 same fields. I'll replace them with a single Con constructor that takes an
 additional boxed :: Bool.
 Fixed, but now it complains r is unbound... is that correct behavior?
 Interesting... main _ := alloc 1 passes.
 Both Nil and alloc turn into TyApps, and tyvar defaulting should be applied
 uniformly to unbound tyvars of a given kind... what could be going wrong?
 "main:()->(); main _ := f(); f _ := return Nil" works...
 It's not r that's unbound, it's the kind var... I must not be binding the
 kind of typeOf Nil to Type when I introduce it? I wasn't doing so for any
 name from tysigs… fixed.
main _ := True --has a custom tag scheme; the default should be sufficient
 fixed
"main:()->(); main _ := case Nil of {Nil=>return (); Cons _ _ => return ()}"
Does not include ImplList in exploredDTs… but how could it, when the BDT
desugaring is done in Core? I could add the Impl type to explored when a
boxed constructor or field is encountered.
It also mentions List Memory Word and List Word Memory in exploredDTs, a clear bug (List Word Memory is ill-kinded).
main _ := Nil does not mention List Word Memory, so it must be the case
triggering it.
It's specifically the Cons case, and the bug is present in the HM output.
(PConArgs "Cons" (Just [uint32,Memory]) [ --I must incorrectly reverse
somewhere... Nil uses PCon and is correct.
main _ := ((Cons _ _) = Nil) and (Cons{}) = Nil both apply typarams in the
reverse order.
Strangely,
"main:()->(); main _ := (Foo{} = Foo ()); Foo:Region->Nat->Type->Type;data Foo r n t = {Foo ()}" does not.
Pair _ _ = Pair () True also types correctly.
A clue: Cons _ _ = Cons () Nil --types the expression incorrectly!
Aha... Cons : a -> List r a -> List r a. When quantified, its parameters
will indeed be [a,r]. But monomorphization assumes the con params are the
datatype params!
Solution: add a params list to tysigs, modify quantify.
TODO: cache kinds as well. That might prevent bugs or restrict expressive
power. Consider copyProxyParam : p a -> f a.
Param ordering: in order of first appearance for explicit tysigs, in order of
datatype params for constructors.

Converting tysigs to proper schemes means I must delve into the belly of the
beast: HM. Have I used quantify messily?
TODO general code cleanup: go through TODOs, separate out HM.DTs and
its fundamental funs (runHM etc).
Get HLS working, use that dependency graph printer to find where to break up
the oversized modules.
Oh no: goSig is used for classes as well! It needs to take a scheme, which
perhaps means the repr of class funs needs to change.
It's apparently fixed... but I really need a proper test suite for each stage.
main _ := (Nil.hd) fails: no such field hdStructCons. That's a quick fix...
Need to add a  BCon => TyCon mapping in addition to field => BCon.
BCon {field: e} => ImplTyCon {unImplTyCon: 
 allocValue(ImplBCon {implTyCon_field: e})}
Problem: allocValue allocates a Ptr to ImplTyCon, wasting space when allocating Nil. OTOH, that means you can safely modify xs.unImplList to
ImplCons.
allocDynSizedValue ImplNil --could be optimized by inlining and simplifying
the case. Alt: allocFixedSize (k,ImplNil)
bdt.field => *(bdt.unImplTyCon).implTyCon_field.
Normalize PConArgs to PCon? That requires a Con => fields map... might as well pass DTsInfo instead of multiple views of it.

Restrict alloc to memory? Storage deserves a dedicated word-aligned API due to its high warm-up cost per slot. It also needs byte-precision refs into
the entire address space (Word,Byte).
Byte vs word-aligned alloc is not a trivial choice... word-aligned alloc could well be more expensive for multiple small allocs. Ideally a custom scheme specialized to each individual contract would be expressible...

dec for pointers := return indexPtr(p,65535)… if I add non-bounds-checked
pointer indexing that'll become a bug.
Use unsafeCoerce to avoid masking, assuming no overflow? Corrupt pointer
values extending beyond their 16b range is a major footgun, but alloc overflow is a footgun regardless.
No one Prelude can meet the perf vs security needs of all contracts...
fortunately, configuring the stdlib is possible.
A flag function f : () -> Bool; f () := return ... could be used to configure
choices such as 1) safe pointer addition, 2) allocPtr overflow checks.
Inlining + DCE would let you use it as an ifdef.
Solidity's choice to make every datatype one or more whole words must've saved the devs a lot of pain...

Argh: if a BDT's Impl datatype has a non-Nil tag scheme, xs.tagList should
desugar to *(xs.unImplList).tagImplList. xs.tagList should also be assignable, but Cons{tagList: p/e} should not be permitted.
.tagList should be fully desugared away, so no need to put it in tysigs.
Add Boxed implTagScheme as a new tag scheme type?
Then I can record .tagList as List's tag and rely on the scheme to clarify
what that means.
case xs of
 --Both ImplList and Nil/Cons should work here!
The constructors overlap... and whether to inspect the tag depends on the
patterns used.
Add Boxed implTagScheme, add BDT tag field to field info.

Implement a concrete alloc in Core; it needs to mutate allocPtr while also
returning a new memFrag. allocPtr should be assumed to be disjoint from every
other pointer; the globals and their child ptrs are disjoint for free, but
it needs to be disjoint from every alloc'd pointer as well.
Use reallyUnsafe* variants to encode no alias? The side effect on allocPtr must be observable (while affecting nothing else) to enable stack lifting and
symbolic opt of it.
getMem off len : Bytestring#
deref@[Memor,t] p => getMem p.unMkPtr sizeof#@t
Every Core primitive must have a # appended to prevent confusion with user
functions.
BB effect: stack, map => stack', map'; alias info encoded as ||| on map.
Types do not in general have a single fixed-length Bytestring# length since
mkCont breaks that assumption... but in every other case they do.
Track which Var (if any) is mkCont; there should only ever be one.

alloc (len,ap,mem) =
 (ptr,memFrag) = alloc' ap
 mem = mem ||| memFrag
In Structured:
ap <- newAP --Track ap variable; becomes (ap',ap) = splitAP ap
It's important that's done in Structured to ensure there's a fixed skeleton
of splitAP ops per function body, preventing any sequential dependencies.
Need to split to pass a new AP into each substatement (consider loops)
(a1,a2) = splitAP ap;
stmt; --pass a1
--continue with a2
That should be translated into an additional zero-sized parameter for the
substatement which is passed through unchanged! It can reside in the scope;
reordering it out of the way is zero-cost.
(a1,a2) = splitAP ap is equivalent to (a2,a1) = …; if one ap is unused the
split can be eliminated.

TODO desugar BCon{field:e} to use allocValue (for now).
TODO use BNFC's generated print function for ppr of errors in desugar.

Q: Do I really need to define alloc in Core? If I had a rule "Pointers
created with MkPtr n are assumed to point to free memory", its allocating
effect could derive from that.
However, that doesn't capture its intended commutativity, so I will define it in Core.
A flush()/sync() primitive could signal all writes should be committed before
further progress.
allocPtr should perhaps be the only memory global assumed protected from
pointer writes.

Now to desugar Con, Con args and Con {field: e} for unboxed cons to
ImplCon (allocValue (ImplCon{implTyCon_field: e})).
Error on missing or repeated fields?
Enforce full application of constructors, boxed and unboxed... that requires
unrolling apps into f xs, recursively desugaring f and xs, then checking
whether f is a constructor.
If it is, fully apply it and then roll the remaining arguments; fail if there
are too few. Iff the constructor is MkFun then overapplication is OK.

case e of
 MultiCon1 fs -> …
 _ -> …
Can be converted to an ifte; for a trivial constructor you can just prune the
_ case.

For documentation/writeup:
There are a number of C types which are deeply entangled with the compiler
because they're fundamental to the language.
They include:
Fun --used to implement functions
Ptr --used to implement pointers, entangled with the memory model.
The unsafe operation ptr[ix] is necessary for dealing with dynamically-sized
data; I say data and not types because the type system currently can't
capture types which don't have a statically bounded size.
Pair --the word-padding primitive; its runtime representation doesn't use the
default byte concatenation rule used by other constructors. It has zero-cost
construction and destruction. For that reason, tuples and vars are the only 
valid Core patterns.
Int --Int s len is a primitive, having a non-default representation.
All other datatypes are represented as the concatenation of field types; Int
is at the bottom of that hierarchy, unique in that it's nonempty but has no fields, indices or tag.
Int Signed is the only type compared using SGT and SLT rather than the
unsigned GT and LT.
Array is unique in that it has indices instead of fields; indices are
accessible via the infix ! operator.

Separating the algo to report more errors at a time: depending on decl type,
each can be desugared separately.
Unrelated SCCs and funs with signatures in HM can be inferred separately.
Use Applicative to represent parallelism a la Haxl?

TODO ensure I use ConRecord rather than Var for constructors everywhere.
TODO eliminate PConArgs.
Wow, converting to mandatory ConRecords and ImplCons made the list of tyvars
massive for main _ := Cons 1 2 (which correctly type errors). A sign of a bug
or performance issue?
Cons 1 Nil errors, while Cons {hd: 1, tl: Nil} passes...
Cons Nil 1 passes, but its output is not equal to Cons{hd:1,tl:Nil}!
TODO add unit tests which test the equivalence of programs written with sugar vs without.
I'm forgetting to reverse somewhere. Ofc they're not equivalent, I haven't
desugared properly in P.ConRecord yet!
f 1 2 also desugars to f 2 1...
I'd introduced a bug into roll; fixed.
Next: proper desugaring in P.ConRecord. Done; now to eliminate PConArgs.
Need to add PArray. Done.
Supporting &&pointed = e would be nice... then I could write
&&(Array (a,b,c)).

Now: support PArray, remove support for PConArgs. Done.
main _ := "foo" triggers TypeSignaturesLackBindings {"$string1"}…
Now I get rigid unification error: Ptr, Array. Note: in check signature of
$string1, not in main!
"a" => deref $string1, which has signature ([],uint1[1]) after desugar…
memAllocPtr has signature ([],Word) after desugar, but surprisingly fails in
mono!
Why do locals work but not globals in mono...?
I convert globals to TyApp nm [], but I don't expect that in mono!
Rather than add a global set to HM and generate TypedVar for globals, I'll
check for globalness. TypedVar now refers exclusively to locals; I need to
recognize that in Structured.
String still fails...
memory x = True; main _ := x --works, but adding x:Bool causes it to fail.
The issue seems to arise when a global has both an initializer and a 
signature. I had forgotten to report back that a global r x = (e :: t) must 
be of type Ptr r t, not t.
Now I get RIU 1 32. Ahh, I need to add fromWord to the string expr. Now it
typechecks!

Remember: Const serialization must handle fromWord.
Format: fromWord k, -fromWord k, Con {field: e}, f, &g (which includes string and is represented as TyApp g [] at the Mono level), array.
Serialization: fromWord@[_s,len] k => len raw bytes (TODO warn on too large
literals, it could be used for malicious Ints which are surprisingly equal).
UBCon: cache serialized tags per monomorphic datatype tycon params.
Normalize field order and fill empty fields with null bytes (use Maybe or
permit null()?).
If the type is Pair, word-pad both arguments and concat.
If it has a tag, fetch its serialization and prepend it; concat the rest.
Array is just concat.
f, &g become labels.
Boxed cons and strings are interesting: BCon {field: e} requires allocating
a new global; the serialization is the label to that global.
"foo" desugars to *($string<N>). &"foo" is a Ptr Code (Array 3 Byte), but
"foo" itself is an array. To enable inline strings, permit deref of code
global pointers in static exprs as well... note that can lead to exponential
blowup: code x1 = 0 :: Word; code x2 = (x1,x1); …
Code is the one region where initializers which refer to other globals are
safe... but need to prevent cycles.
Restrict all initializers (code and memory...) to consts.
Problem: memAllocPtr must be initialized on each call, sto on creation,
tsto on first call of txn. Without alloc for sto and tsto, boxed datatypes
for those regions can't be created.
Solution: a memOffset() primitive rather than introduce a source-level
concept of labels? External labels would require extending the source
language, but would be very useful for FFI and constructor arguments.
imported nm : MonoT
exported f = e : MonoT
Exe mode: exported main : () -> (), main => stop placed at 0.
Library mode requires either separation into text and data segments, with
data placed last (complex) or up to 32B of padding if the last byte of data
is PUSH32. The latter is preferable since it makes code objects easy to
manipulate on-chain; sort code globals to minimize padding.
A source module becomes a set of decls; the static environment is a mapping
module path => such sets. It would be desirable to cache the parsing/decl
grouping and mount a conceptually immutable environment during compilation.
Split Prim and Prelude into multiple variants + config modules, don't import
them by default?
Code objects are then contiguous and linkable, exporting base-relative
and constant labels and importing constant labels.
Each region requires a base, but all bytecode modules must take the same
<r>Offset.
The code object API could be auto-generated: place in memory, specify
imports, return/compute exports.
Stick to exe mode for now. No need to expose memOffset or support labels, just set memAllocPtr's initial value in the prologue.
Permitting initializers only for code globals might simplify matters.

Store smaller-than-max constructors such as Nil and Nothing unpadded in code?
Then reads may contain garbage. I'll allow it for now.
Code layout: main BB, BBs, data, padding.
If JTs were first-class then they could be shared among several caseTags.
Place them inline for now...
Const/ defines the serialization scheme for constants, which must be
consistent with the layout of dynamically created objects.
Note if I only generate exes I don't need to worry about data padding.

I should perhaps explore every global and function, deferring pruning until the Core primitives are added.
I currently do mask pointer addition in indexPtr… which is necessary for dec's definition (adding 65535) to work. TODO replace with unsafeCoerce,
word ops. Done.
Problem: I want to inline indexPtr, unsafeAddPtr, inc/dec etc on pointers
while preserving aliasing info. Inlining of other functions may also reveal
aliasing info. Solution: recognize pointer ops when inlining.

Moving more of the primop logic to source: if I had a toWordTuple op, I
could define &, | etc at the user level. However, that requires knowing sizeof during monomorphization.
Bug: data Foo = {Foo Bar}; data Bar = {Bar Foo} isn't rejected... I need to do a cycle check in datatypes.
If the well-foundedness check is done on a finite set of monomorphic types
it's simple... but how difficult is it to do on abstract parameterized
datatypes?
Datatypes can take and apply n-ary tycons as arguments and refer to m
different combinations of them + constant tycons.
data Foo a b c = Foo (a, Foo b c ()) --loops, but has finite size.
If n/2 and 2n+1 could be encoded I could use Collatz as proof of termination
checks being at least nontrivial in practice.
A datatype can deconstruct its value argument if you pass it the
component constructors, see:
Foo : (Type -> Type) -> Type -> Type
data Foo f a = Foo (f a, a)
Simple solution for now: check the finite, monomorphized set; I need to do 
that before I serialize tags and initializers.
Inputs: DTsInfo, exploredDTs; result: monoDT => repr. Best to get the full
repr all at once, that way field lookups etc become easier later.
Sizeof and tag serialization need to be done at the same time to get an
accurate repr.
Each field/index path has an offset and surrounding values. Values to the
right may be relevant for opt, consider (Short,Array 30 Zero): writes to the
short can be done without a restore by left-shifting it.
That is if I can assume that if a pointer p :: Ptr r a exists, then p must
already point to a valid bitpattern.
That would imply pointer writes of single-value types are noops... but my
current definition of alloc/allocValue breaks that assumption!
I could make a function allocValid(a,n) that places n instances of valid
bitpattern a using log(n) mcopies…
Proposal: split allocation; allocRaw returns an UninitPtr r a.
writeUninit(up,a) makes no assumptions about the old value's bitpattern; once
you've initialized a up it's safe to coerce it to a p with initPtr up.

Relevant queries: sizeof, offset of field, sizeof elem, flattened repr
(allowing concatenation of fixed regions).
Once sizeof is determined, I can fix the global addresses of the non-code
globals. Compilation is a multivariate opt problem... global addrs interact
with code opts. Ideally one would be able to try multiple solutions, fixing one var at a time and use a heuristic to tree search...

Ah: need to include padding bits with indeterminate value in repr to ensure
they're masked out when performing equality checks or comparisons.
Plan for remainder of pipeline:
Don't go for every possible opt, just the minimum required to get working
code. Scrap: opts based on datatype structure. Keep: inlining, it's essential
for reasonable perf. Defer: pointer opts.
Compute minimal repr info per monoT (sizeof, field offsets and lens,
serialized tags).
Structured => Core => opt => Stack => asm.
Structured doesn't need const serialization... what does Core need it for?
case e of ZeroTaggedCon args -> …; _ -> … opt and sizeof inlining. The latter
is big enough to justify repr/serialize first. Hacky first solution: just
cache sizeof for each monotype, rely on DTsInfo (and special rules for
Array, Int and Pair) to get field offsets.
Disallow a mix of Cons and ImplList in case (for now).
Disallow array index of local arrays larger than 1 word (unsupported for now).
Now: serialize tags and initializers.
Core/structured: expose .slice(off,len):T directly?
getMem p len m .slice(off,len2) = getMem (p+off) len2 m
.slice(o1,l1).slice(o2,l2) = .slice(o1+o2,l2) --lens ops

The valid repr assumption of Ptr may mess up not only p->field = e, but *p=e.
That's because writes to types with a single valid bytestring pattern should be a noop. For that reason, memory global initialization needs to use raw
memory writes.
The total size of memory globals can be computed after Sizeof, giving you
the memOffset initializer for memAllocPtr.
Scrap sto and tsto alloc?
Memory global layout: memAllocPtr at 0, initialized globals placed
contiguously; initialize with a single codecopy in prologue.
Issue: Mono's mentioned should include <r>allocPtr if you use alloc@r.
I can solve that by mentioning the vars in the placeholder bodies; fixed.

When there are no memory globals and only scratch alloc is done you should be
able to alloc off 0. What's the maximally general basis of opt rules which
enables that? Scratch alloc: if alloc'd pointer does not escape, read but
don't bump allocPtr. If allocPtr is read but never written, replace reads with memOffset. Then if there are neither reads nor writes, drop the global
and set memOffset to 0.

Should Nil be padded with arbitrary bytes or zeroes? The latter would make
comparison cheaper, but code globals larger. A word list only has one Nil, but it would cost 6600 more gas to deploy padded...
Note a codecopy to init memory x = 255 :: Word is massively inefficient, let
alone memory x = 0.

TODO bytestring value deserialization, in particular for parsing return/revert values.

OK, now pipeline2serialize runs... but it's revealed bugs in earlier stages.
1)
ghci> (\(_,_,_,sers)->sers)<$>pipeline2serialize"main:()->();main _ := x; code x = Cons 1 Nil :: List Code Byte"
*** Exception: Compiler error: tycon "Byte" not in scope despite FIKS
CallStack (from HasCallStack):
  error, called at .\Typecheck\HM.hs:780:18 in evmc-0.1.0.0-inplace:Typecheck.HM
2)
ghci> (\(_,_,_,sers)->sers)<$>pipeline2serialize"main:()->();main _ := x; code x = Cons () Nil :: List Code ()"
*** Exception: Compiler error: missing sizeof for ("ImplCons",[Code,()])
CallStack (from HasCallStack):
  error, called at .\Const\Serialize.hs:477:16 in evmc-0.1.0.0-inplace:Const.Serialize
1) Must be due to tysyn subst missing globals. Fixed.
2) Indicates the arguments of monomorphic DTs aren't considered mentioned.
I'll fix 2) tomorrow.

fmap(\(_,_,_,si)->si)$pipeline2serialize"main:()->();main _ := D;data D={D};tag D=UInt 4 where{D: 0xdeadbeef}"
Right (SerS {sersAllocPtr = 1, sersCodeInits = fromList [], sersMemInits = fromList [], sersTagSchemes = fromList [(("D",[]),(["D"],Custom uint4 (fromList [("ImplD",(TyApp "fromWord" [Unsigned,4] :$ EInteger 3735928559,Serialized {serLength = 4, serSizeof = 4, serContent = [Left [222,173,190,239]]}))]))),(("Int",[Unsigned,4]),([],Nil)),(("Unit",[]),(["Unit"],Nil))], sersRunQueue = []})
--Strange... why is there an ImplD when D is unboxed? It's a bug in
Desugar.Datatypes; fixed.

ghci> str="main:()->();main _ := x;code x = Cons True Nil :: List Code Bool"
ghci> fmap(\(_,_,_,si)->si)$pipeline2serialize str
*** Exception: Compiler error: missing sizeof for ("ImplCons",[Code,Bool])
CallStack (from HasCallStack):
  error, called at .\Const\Serialize.hs:477:16 in evmc-0.1.0.0-inplace:Const.Serialize
Ofc, ImplCons is not a real datatype. Somewhere in Serialize I'm using the
constructor rather than the tycon to look up size. Fixed.
code x = Nil :: List Code Bool --$anonCodeGlobal1 has the correct value, but
x = ImplNil $anonCodeGlobal1 is incorrectly given the serialization 0 :: Word.
I was incorrectly left-padding arguments in non-Pair datatypes; fixed.

Potential bug in Mono: will (null() :: DT) result in DT being mentioned? It
should, since I need repr info at that point.
I can't just compute it for all datatypes since they're parameterized...
If any expression returns a DT, it's mentioned. That's a problem: how to get
the full set of returned types with my current algo?
Looking at function return types isn't enough, consider
f : a -> (b -> D). (b -> D) is of fixed size, but returns a D.
But for f to return a valid g at runtime there must be a function which
returns a D...
Also don't forget EArray! Done.
In library mode external functions would be able to pass externally
constructed values, at which point the "mentioned" criterion becomes more
complex - I might need to apply Mono after Structured (where every subexpr
is bound to typed vars) instead.

Note: it's not a problem that N1 uses Int; you'd need more constructors than
could possibly fit in memory to exhaust that space.

To use an open set of modules in stdlib I need to parse them and recursively
fetch imports. Each module consists of a set of decls (preferably grouped by
type) and a set of dependencies.
Rather than just import Prim and Prelude by default, I should add the stdlib
modules to the module namespace. I'll keep it as it is for now...

Nasty trick: I could use datatype padding in memory as scratch.

Surrounding fields with constant values (eg tags or padding) can make pointer
writes cheaper: if you know the exact value the old surrounding data should
have, you can add it without a load. That's particularly useful for tuple
pointer writes. Aside: TODO add syntactic sugar for nth(n,tup) =>
tup.snd. … for exprs and patterns.
Preserve that info when taking &(p->field).

Storage API:
If you have a small number of fixed-size arrays, you could use
small constants to distinguish them like this: indexSlot = (index << 8 | k).
Maps could simply use 248b of the hash as the index. Note large arrays that
take up a significant fraction of the address space make maps that simply hash their key to get the slot unsafe.
Is preventing storage slot overlap when possible worth it? That requires
padding, which in the worst case (17-byte values) wastes half the space.
This is a choice that should be exposed to and made by the user, since it
depends on the use case. Note if you're frequently loading multiple adjacent
slots then Merkles might be preferable.

TODO prune the error datatypes to eliminate obsolete errors, starting with
DError.
TODO warn on potential UB: setting tag fields to arbitrary values, access out
of range, accessing the field of a constructor which lacks that field.

A desirable reordering: Cons f() (Cons g() Nil) =>
x = f(); y = g(); Cons x (Cons y Nil)
By performing the (assumed non-inlinable) calls first, the straight-line
inlinable code is grouped together. That would alloc the alloc pointer to
be lifted, saving 2 load-stores.
Write effect: oldAllocPtr -> {00,01,y,pNil,01,x,pCons}
That could be optimized: with pNil and pCons simplified to oldAllocPtr+k,
they can be disentangled from the write effect. Stores can be done from the
back to avoid saving and restoring bytes that will be overwritten anyway.
In the best-case scenario, the entire thing would fit into a word.
If pointers are assumed to never over/underflow, pointer fields can be
incremented by adding a left-shifted k.

The initial test suite can be composed of unit tests, but I should also write
QC generators for valid (or deliberately invalid) code.
"If program is otherwise valid but has X error, the compiler should report it"

Generalizing BDTs: some fields could be sugar for *(dt.field).field2; that
could be used for method tables. If the tag were a pointer to an info table
that could support GC, loading, linking etc in an interpreter.

Note constant expansion of letrec'd globals is analogous to inlining (less the beta reduction).

Appel CPS:
Impose restrictions in the Core datatype stack directly?
1) Any let-bound expr should be in ANF: primop vars or var.
2) letrecs may bind only constants: lits, cons or lambdas
3) cont exprs are primop var/tup or f var/tup
Stop should only occur in a branch expr, add in an op expr
A BB is a series of let-bindings followed by a branch expr

PRIMOP differs from my primops in that it takes a cont
p34: val arbitrarily...
"
As is traditional in a continuation semantics, we have a type “answer” standing
for the result of the entire execution of the program. We don’t really need to know
anything about the structure of this type.
" --analogous to my End, except I know End's structure
p55:
"
However, two references (mutable cells) are considered equal
only if they are the same cell (i.e., at the same address). This simplifies the testing
of structural equality, as cycles need not be considered—every cycle in an ML data
structure goes through a ref.
" --A consequence of strictness, though one could in theory build cyclical data structures without refs in a safe language using black magic (pointer-
manipulating combinators that prevent GC).
"In summary: Polymorphic equality is no fun at all." --I wonder how that's changed for ML since '92...
Unboxed updates are distinguished; in EVMC, one could just track the pointers
a value potentially contains to track pointer escape.
p57: "Note that case expressions do not bind variables in mini-ML, as they do in ML." --analogous to my caseTag and the STG
Shame I can't use the "small constant is definitely not pointer" trick; all
(memory) pointers are small on the EVM.
p66: F (the CPS conversion function) takes a meta-language cont that takes
the produced expr...
Difference: I don't have closures.

Consider a simple stmt and expr language:
k, e+e, (), Pair, return, x = e, var x = e, SE.
I can experiment with and compile Core further, separately from the C->Core
stage. Datatype repr is tied to C...
Need a Core.PrimFuns module to store them in one place and avoid typo bugs.

Initializing storage in a contract on creation:
import StorageGlobals;
main () := {
 <init storage>;
 modp = alloc();
 *modp = m;
 returnPtr modp
};
//Defines a code global containing an appropriately sized type
module m {import StorageGlobals; main () := …; ...}

*****************************************************************************
Next: Structured IR.
Structured decomposes stmts and exprs into Core ops while maintaining
structured control flow. The work of decomposing the structured code into
Core basic blocks is deferred.
In my old approach, I used pull-based live rather than push-based decls to
determine which vars should be passed to CFG (now Core) branches.
That has the advantage that vars bound to subexprs are cleaned up for free,
but OTOH dead locals must be aggressively GC'd.
Would declaring live after every op work well? The main issue with that is
that live behaviour is complex in assignment and case.

Variable order at the Core level isn't what determines perf; word-level code
should be able to split multi-word vars and store them non-contiguously.
GCing a dead word var requires either a pop or swap, pop; the latter risks
putting the words in a bad order. Taking into account order requires having
a concept of "bad order" based on estimated cost of code as a function of
order, as well as support for reordering functions.
Focus only on optimizing Core for now.

First:
Adapt Structured input to serialize output.
Change: the Module has already been unshadowed.

Before I can adapt the structure of Structured I need to adapt Core.
Use a newtype of (Name,[T]) for Core primops; opE = push | primop.
Make behaviour as explicit as possible at the expense of terseness.
If I put funs and globals in a different namespace (accessible through a
StaticVal opE) that would simplify scope and type checking.
Push Constant, Constant = lit | named
Should I distinguish between fun and global names? How about fields and cons?
For compilation to word-level code, Core requires defuns, globals, inits and
datatypes.
Con, setField and getField (on locals) can be primitives; I can use
unsafeAddPtr (with additional aliasing annots?) for p->field.
Invariant: all types are fixed-size, so global pointers refer to a fixed
region. alloc'd pointers OTOH may refer to an array of dynamic size.
p->field, *p!ix and p[ix] should be assumed to never cross a global/alloc
boundary.
Length-tagged array allocation requires coercion (because the tag is of a
distinct type from the elements), but that should be fine.

Distinguishing between fun names and let-bound names isn't a trivial decision; while it clarifies behaviour, it also messes with LC rewrites.

There isn't a fixed set of state vars; splitAP creates two new APs, and
alloc@[r,a] (len,ap) creates a new r slice.
To simplify the algo, splitAP whenever an AP could be needed, then prune.
stmt => (ap,ap') = splitAP ap; stmt; ap = ap' for new ap'

Core primops must include:
Integer literals
Label push (functions and globals)
Field get and set
Constructor application
Other primops (deref, arith etc)

Keep global and function values abstract.
How to model alloc in a way that ensures it's both commutative and
implementable?
*allocPtr = arbitrary(ap)?

If trueMain initializes memory and code globals with consts, it needs a
concept of consts. Const push can be a primitive which subsumes function and
global push.
Any expr in const form (c ::= +-k | g | f | UBCon{...}) could then be
converted to a single const opE. Note it's not necessarily a push, consider
*memPtr = const.
To keep global pointers abstract, Core modules need a pointer set.

Code map ops: ptr->const, m1 ||| m2, empty.
If a pointer p is never accessed, p->v ||| m can be simplified to m.
FW: if only static offsets off p are ever accessed, it can be split into its
accessed fields placed out of order.
The code map ops will need to be scanned later.
Wouldn't it be simpler to retain the original code global mapping...?
Since functions already have access to letrec-bound names, I can just
letrec-bind it.
deref@[code,a] (p,code) => v
Memory globals still need to be initialized... including memAllocPtr.

Start with an inefficient but simple effect model: just memory, get and set.
No memory splitting, it's a single token; no AP.
allocRaw?
Note reads of consuming ops must be scheduled before the consumer.
No need to pass code as a resource, it's let-bound.
$trueMain takes calldata and nothing else (gas &c ignored).

Bytestring# is necessary at the Core level for representing revert.
Value ops reduce to bytestring ones.
Problem: stack words are always a multiple of 32B, so they don't represent
bytestrings perfectly. Core needn't capture that, but word-level code would
benefit from having a concept of "bs stored in ws at offset o".
Problem 2: bs = *p; *mp = bs may be separated by several basic blocks, making it more difficult to optimize to a copy.
Symbolic eval and propagation of arg value info across BBs could address that.

OK: bytestring get/set, no AP, concrete allocs.
id#

Deref in patterns will only trigger an error after monomorphization?
Need to detect *immutableP = e.

Q: do I recognize EArray as a const? I should.

Should I have a null# primitive separate from Stdlib's null? It doesn't hurt... and it simplifies opts relative to a more general coerce-based opt.
If a coercion results in a valid bitpattern (e.g. coerce# 0 :: Bool), it can
be replaced with a const of that type.
That's particularly useful for optimizing derefs (which involves coercing
the Bytestring# result of getMem).

There's redundancy between tuples encoded directly as Values and ones created
with MkCon; simplify away the latter.
Issues with scope (more or less than necessary, ops getting lost) are a certain to be a source of bugs.
Need to make sure I'm tracking every subexpr evaluated + keeping track of
them across branch boundaries (in particular within pattern eval).
And do I produce enough Declares? emitStmt produces one...

BDT case compilation:
case xs of
 Nil -> s1
 Cons x xs' -> s2 =>
case *(xs.unImplList) of
 ImplNil -> s1
 ImplCons x xs' -> s2
It should be possible to choose intelligently between fetching part of a DT
(deref . GetFieldPtr) or fetching the whole thing, then slicing it
(GetField . deref).
A good heuristic for the former: if the value to fetch >> the expected number
of bytes read (i.e. the tag and any fields).
Simple implem: do so on a per-BB basis, so the tag gets fetched before the
Core-level case.
FW: coalesce reads of adjacent fields, either directly at the word level or
by coercing to a struct pointer.
UB case compilation:
v <- e
Get list of canonical cons, map each mentioned Con{field:p} to assign v to
Con{field:p} without the tag check, the rest go to default case (revert if
none is specified). You needn't do any matching on revert, since it can't
affect the outcome.
Note case e of Con fs -> s; d -> dflt => 
ifte (e.tagField == k) s {d = e; dflt}.
That also works if one of two con tags is truthy and the other is not.
Problem:
case *p of
 Con1 x y -> s
--Is the DT itself on the stack and passed as an argument? That's determined
by scope in Structured. Core opts must be able to recover structure and
move coalesced derefs to earlier BBs?
That should be possible by looking at the subset of the CFG that's static.
Beyond trivial inlining, you can also inline looping code by specializing
$ret to a fixed cont. You should be able to reverse the stashing of the
original ret and scope in a mkCont… and deduplicate pushed arguments using
them.
You can avoid the default behaviour (fetch entire *p) by explicitly fetching
(bdt.unImplTyCon)->tagImplTyCon in Structured, but that doesn't let you choose approach later.
Note: I can't eval the patterns before the case is matched; analysis must be
done on Pat!

case rules:
1)No conflicting cases:
 At most one case per top-level con
 No cases after default
 --Consequence: you can determine which case to execute using a single
 JT jump.
2)Either no boxed cons or all boxed
 That could be refined: ImplTyCon p could be treated as a default.
 No default case on a BDT involves dereferencing p.
If there is only one case p, convert to p = e; {s}
Valid form: (BCon* | UBCon*)[default]. FW: integer literal patterns are ~
UBCon.
BCon ::= boxed con where the ImplDT has > 1 canonical cons;
UBCon ::= unboxed con where the DT has > 1 canonical cons;
the remainder are defaults. Note it's only the top-level con that matters:
NilCon () and NilCon True are both defaults.
Ignore whether custom tags are equal at this stage.
If a datatype has only one con, omit the tag check even if it has a custom
tag scheme.
If a case statement starts with scope S, each case in it should have scope
v:S, where v is the scrutinized value (regardless of whether the case is
boxed or not). The body of the case (after the matching) has scope S.
The difference between boxed and unboxed nontrivial case is whether it's v's tag or v.unImplTyCon->tagImplTyCon which is branched on.
FW: more general integer overloading, boxed integers? No pressing need...

There can be no revert# op because it's branching; default cases where no
infallible pattern has been specified must call a revert function unless I
add revert to Structured. If I use revertValue() I must mention it in Mono,
inline and optimize it to 0, 0, revert. The zeroes need not become push0;
there might already be a 0 on the stack. I'll use revertValue for now.
The need to mention every implicitly used function is likely to be a rich
source of bugs; can't I avoid it by deferring mono? Issues: mono on demand
would be complex, interleaving concerns; the mono output is used in
serialization.
Inspecting whether case has an unspecified default in mono is overkill;
simply always mention revertValue() and prune it during Core opt. Mono is for
instantiation, not tree-shaking - even if it closely resembles the latter.
Note: case on a BDT also mentions deref of the tag and every field!

Nice: since overlapping cases are forbidden, case on arrays can only take
a single case, meaning it'll be converted to ~an assignment.
With no literal patterns, integers are also treated the same way.

Note: once I begin pruning dead cases (e.g. in a specialized fun), cases
which would not become iftes during Structured compilation can be converted. It's therefore not appropriate to perform case => ifte conversion
at that stage.

On Core vs C primitives: all C prims need to be mentioned (?), Core prims do
not (and cannot be, they're straight-line ops and not functions).

Even a subword local array may straddle two words, as it may be in a field.
An index may also straddle two words.
Implementation of general local array get/set therefore requires a JT.
Storage pointer access also requires branching... can I optimize that at the
Core level?

x+=5 produces an error value in desugar: Prelude.read: no parse.
Presumably I got bitten by my hacky read . show conversion for op; TODO
convert it to a proper case. Fixed.

A vital application of reordering: pushing noninterfering exprs in reverse
textual order. Ex: in f x, f will ultimately be ToS - evaluating it first
will require a swap (O(n) in word size of x).
In f (a && b), the branches of the && could be split, with f pushed in each.
If A -> B1,B2; both -> C -> D, when is it worth copying and inlining C?
If you must eventually jump, there's no use in delaying it. However, if D
terminates...
Need to consider code size budget; use a heuristic size when optimizing.
Make most profitable exec => code size trades (given exec intensity at each
BB) until there are no more that are profitable or fit in remaining code
space.
If heuristic size is an underestimate and I run out of space later, retry with a proportionally reduced budget. To prevent runaway inlining (e.g. of
a main consisting of a while loop), the code size budget could also be given
as a compiler flag. Loops could also be annotated with max-unroll alongside
expected iter count.

Intensity annots: intensity on f and stmt, iter count on loop, probability on
ifte and case branches (normalize sum to 1).
Function intensity is on a per-instance basis...
Bound callee intensity by caller or increase caller's intensity to ensure
consistency?

The compiler is complicated by the fact I must deal with code no sane
programmer would write, such as _++. Ideally I would nip that in the bud by
restricting the language, but requiring a superficial resemblance to C
renders that impossible. A future language targeting Core could be more
Haskell-like...

Right s = pipeline2structured "main:()->(); main _ := {var x = 1; while (x) return ()}"
sdefuns s M.! (FPoly "main" [] $ fun2coreT (TyCon"Unit") (TyCon "Unit"))
--Generates reasonable-looking output... but I need a suite of larger programs to test the pipeline on.
Next: convert to Core, find all the ways my current repr is wrong.
I haven't implemented short-circuiting ops yet.
Ifte, while, case and call will require generation and use of new BB funs.
h() :=
if cond
then return f()
else g()
end
--Can be optimized to jumpi to f directly, but then I need to convert
fun2core# of the const to get/recognize the Core fun.

With malloc available, stack spilling is possible... but just fail if
dup/swap > 16 are attempted for now.
Sometimes I may know an argument is irrelevant without being able to prune
it by inlining, e.g. in f(x,y) where f is a large function.
Then I could use an explicit arbitrary@t op.
If I just want to revert and don't care about the revert message, I can
revertValue (arbitrary()). I should then be able to consume any two words that happen to be on the stack...

Stack scheduling benchmark:
fac n := {
 var accum = 1;
 while (n) {
  accum *= n;
  n--;
  }
 }
Mutated vars should be eagerly swapped to their ultimate positions.

Making the simplifying assumption that inlining replaces
push2 ret, push2 f, jump, jumpdest, jump, jumpdest with the function body (ignoring reuse of arguments
that were in locals) and that 24 gas is saved (the minimum, excluding savings
from specialization):
code size increase: |body|-10
gas saving: 24
For an exec count of 1000, assuming code size limit not encountered, the
body would need to be >130B to make inlining not worth it.
That assumes you won't eventually need to jump anyway... the target f also
needs to be inlined.

Consider a series of functions fN:()->(). f1();f2();...;fN() should be
TCO-able as if it were fN (f<N-1> (… f1())).
f(g(h(x))) should be TCO-able even if it's not returned!
Opt: when a non-state value is 0-sized, eliminate it. Then f1();f2() is in
TCO-able form.
Ignoring a suffix of the stack isn't just the purview of revert and return;
a jump which ultimately doesn't return can also do so. Add an ignored tuple
argument? Alt: an op forget :: a -># ().
The key to TCO is identifying pass-through functions of the form
f x = g x', where x' is a coercion of x. Then any instance of f can be
replaced with a coercion of g.

How to identify BBs where I can ignore a suffix? Perhaps I should distinguish
conts of potentially unbounded size (f,S) from those which are bounded
(e.g. stop) at the type level.
But consider f();stop() - the return address of f is not of potentially
unbounded size, so stack cleanup can be elided in 
bb (scope...ret,env) = jump f (stopf,env). That can be captured by stopf not taking ret (or some parent of bb not doing so).
Only when the arguments passed to the branch are of potentially unbounded size (which can only be because the last argument is a return cont) must you
ensure the stack is exactly the arguments; otherwise it can be args++S, where
S is some garbage.
That can be viewed as ++ not supporting a left argument which is of unbounded
size.
Ordinary functions are of the ordinary -># type!
OK, so $ret : ReturnCont b...
Problem: jump $ret args should logically have $ret ToS, but it's actually
of unknown size.
Solution: split return cont into an ordinary function
(b,Stack) -># End and Stack?
Then mkCont f rest should be replaced with pushStack a S.
The constraint for a branchE then becomes "the stack must include the args as
a prefix", which enforces stack == args when the last argument is :: Stack.
When making a call, the type of the return function is blinded... instead of
((v...scope),Env) -># End, it becomes ((v,Stack),Env) -># End.
But it's associated with a particular stack; that risks introducing type
errors not recognized by Core itself!
Proposal: introduce a new Branch call f arg ret rest env which is polymorphic
and enforces ret takes (b,rest)?
Problem: all stacks have the same type. That could in theory be solved with
rank-2 polymorphism to assign a UID to each stack, but Core is monomorphic.
It's still trivial to create
well-typed, non-compilable Core because a hypothetical Core author might
still write f stk = g (stk,stk).
It's simplest to just use jump, then opts have fewer branch cases to deal with. Ensure that Stack is used linearly in a separate check from the
typecheck.
The first main call needs a stack; introduce an emptyStack primitive.

A problem with let s = extendStack a s': stack layout isn't obvious from the
branchE alone, you must look at the definition of s.
Really, functions are polymorphic:
f : A -> B becomes
f : forall stk . ((A, ((B,stk),Env) -># End, stk), Env) -># End (?)
That prevents confusion between stacks; extendStack can simply be a tuple
(making layout obvious in word-level IR). The type distinguishes (rest,stk)
from stk in size!
For stk = (), (B,Env) -># End should be coercible to an acceptable return...
But I don't want to reintroduce polymorphism after all the effort I went to in order to eliminate it...
For now, just use an opaque Stack and accept the one case where I need to
look at the def...

f();stop()
The stop Core function should be coercible to forall a . a -># End?

*******iPhone notes

Aha! Since Pair is just cons on a hetlist,
f : a -> b
Becomes
forall stk . a * (b * stk -> End) * stk -> End.
(Ignoring Env)
Then fully expanded nested tail calls become well-typed!
As does a compilation of ifte cond f() g() where the retf is pushed before the branch.
If the stack is one big tuple of the form functions expect, you can incrementally push arguments and jump to functions directly.
f();stop() can also be compiled to tail call directly from f to non-returning stop by choosing stk = Pair b scope for stop.
A non-returning f : (a,b,c…) -> () becomes
forall stk . (a,b,c…stk) -> End.
That neatly captures that functions which ignore a suffix of the stack are polymorphic in it!
Non-returning stop with no arguments:
stop : forall stk . stk -> End
Problem: how to reintroduce polymorphism?
Function values are rank-2 polymorphic; instantiating them for a specific stack is a noop at runtime.

C functions converted directly to Core take a single argument; they should jump directly to implems that flatten all the arguments and returns. For () and other 0-sized types that means removing them!
I can split tuples that appear dynamically (eg as the result of derefs) and inline the converted C functions to their friendly flattened equivalents.
A function f which takes an a, ignores it and then stops is in practice fully polymorphic in stk. How to expose that to the optimizer? Perhaps polymorphic implem functions which are then inlined and used, no coercion necessary. Coercions essential to optimization are a code smell, indicating the type system can’t capture enough info.
Note arbitrary coerce is distinct from coerce-like ops such as rank-2 fun instantiation - in the former case you hope you’re right, in the latter you’re telling the reader why you are.

Word-level Core could still type the words; a multi-word T could be divided into T#1, T#2 etc.
Straight-line ops could then be typed:
dup1 : Code (a * a * stk) -> Code (a * stk)
Note: to ensure a is indeed single-word, you need a kind system.
(*) : 1 -> n -> (n+1)
If n is fixed, you must be in the skeleton of main… if the EVM weren’t a stack machine, you’d be able to use fixed-address global instead of stack variables.
Dup1’s first argument is its continuation, the remainder of the code. All straight-line ops have such a continuation, branching ops do not.
Need to distinguish between the function types: compile-time function, Code, Cont (callable from a branching op).
A defun nm code takes a Code a and introduces a Cont a to scope.
But could I capture fallthrough? Problem: the code fallen through to may be more general than just supporting use by the code above it, so you’d need a constraint.
defall: (t > Code b) => (Code b -> Code a) -> t -> (Code a, t) —might need a type way of communicating “adds nms to scope”.
Idea: names as effects/constraints for typed module construction. Find that “typed asm in Haskell” talk.
How to treat quantified subtyping systematically? Need an algebra for it.
forall a b . (a,b) ~ (forall a . a, forall b . b) because they’re independent.
What is a quantified type? It’s like a function which takes an unordered map and produces a concrete type.
Operations: SKI-type combinators? App, select, K. A forall a is also a forall a b…
There might be a relation to reasoning about options in a contract DSL.
How to treat nested foralls?
Todo make a stack of lower-level typed Cores, it seems a fruitful avenue of exploration.

*******/iPhone notes

Preinlining: in case e of A -> f(args1); B -> f(args2); …, all fs share a
return address. One could make a variant of f with a fixed ret...
For that to work with a polymorphic stk the retf pushed would need to be
parameterized with the given stk.
Related: ifte cond f(x) g(x) => (ifte cond f g)(x)
If there's a shared stack suffix, you might need coercion of the fs to get it
to work. ifte cond f(a,(b,c)) g((a,b),c)

Flattening argument tuples to a*b*c*stk ensures functions with uniform
representations are implemented using fs of the same type.
Cont a --as a monomorphic wrapper for forall stk . a*stk -> End doesn't
generalize to flattened multi-arg funs; you'd need Cont1, Cont2 etc.
However, every C function which might appear uncoerced as a value is a Cont1.

Nice, with a flattened, append-based stack repr I can safely represent
functions with multiple return conts. That can be done with retf, Stack as
well...

f ((a*b*c)*stk) = f' (a*b*c*stk) --because runtime repr is equal, any use of
f can be replaced with a safe coerce of f'.

All values on the stack are word-padded; represent that explicitly? Pair
already does that...
It strikes me one can inline from both the back and front: a normally exiting
contract must revert/return at some point. Out of gas ~ revert(), so if the
only leaf branch is returnValue(X) and the contract has no side effects, you
still need to prove termination to optimize it to just returnValue(X).
Note aggressive gas opts such as "elide storage writes that will never be
read" would optimize GasCoin to main () := {}. That's acceptable, focus on
external observability via log,call,return/revert.

How to implement new scheme?
C functions -> forall stk . Cont (A*Cont (B*stk)*stk) where
Cont t = (t,Env) -># End. Introduce TyForall in T, note it's only for Core
onwards - and only for quantified polymorphism.
All functions : forall stk . Cont (a*b*c … stk) for some a,b,c… closed over
stk.
Zero-cost primop instT t.
Primops for (a -> b) <-> forall stk . (…) conversion.
Invariant: a rank-2 type will never mention the stk in scope, so there's no
need to worry about shadowing.
jump : (a -># End) -># a -># End, in practice restricted to b = End.
The second argument should be a value; for nullary functions that don't return the stack may be dropped entirely.
jumpi : Word -># (a -># End) -># (a -># End) -># a -> End
stop : PersistentState -> End
When do I need to create quantified values? $ret is not quantified, it just
has the top-level stk in scope.
Pre-opt I can just directly convert each (->) function to -># and
instantiate it... sharing polymorphic Core functions becomes relevant when
optimizing.
They shouldn't pose much trouble because only stk is ever quantified and it's
always shadowed (so you can stop at Forall when inlining).
Need a primitive for pushing Core funs (as opposed to C (->), which must
first be converted to a Core fun).

Argh: I need to reimplement sizeof logic when I introduce TyVar "stk"?

Note: pseudo-inlining is just fallthrough (as opposed to Core-level
inlining). Fallthrough requires the callee is static, so it should be
possible to simply update the word-level BB's target stack layout.
BB spec: invars, outvars, op DAG, branch.

First changes: add TyForall Name T :: T, change CC in Structured.Convert
Will using only a single stk tyvar mess things up somehow? I'll find out.
Convention: the var for the stack is $stk.

I won't flatten the tuples immediately; that wouldn't be precise anyway since
some may be wrapped up in newtypes.
scope = vs (including $ret and $stk) => v1 * v2 * … vN.

Troublesome edge case: I'd like to eliminate all zero-sized on-stack args.
But what if a function just takes (), not even env? Then I simply can't apply
that simplification...
It's a bit ugly that the type of Cont a is (a,Env) -># End, when (a,Env)
means a * Env * (). No matter, there's no runtime impact and I can simplify
it away.

Oh sweet: if a function never returns I'm not limited to just not pushing
ret, I can omit pushes from lower on the stack as well.

There's a mismatch in my code: a Cont is (FunVar,Scope), but a lhs is
(FunVar,Pattern). FunVar includes type, which is inferred from scope.
The Name component is allocated. All funs have a scope, so you might as well
generate the funvar directly.
A Core function push is polymorphic in stack; a Call should take a C function
(a -> b), convert it to a polymorphic Core function and instantiate it on
the stack type (arg * scope).
Instantiation is a Core op to enable rank-2 funs to live on the stack.

f x:
call (x*f*scope,env) =
 let coref = fun2corefun# cf
     instf = instantiate coref --stk = scope
     ret = instantiate retf    --stk = stk
 in jump instf (x * ret * scope, env)
--Need to look at Structured to see actual scope at time of call.

Would it be simpler to emit a function for every stmt as I did in CFG? Yes!
coreBlock stmts next --the next is to be jumped to after the last stmt
 (lhs := Op opE):rest => append op to ops? No.
  It instead becomes:
   lhs = opE
   callNext --two ops and a branch
 If v th el : rest =>
  nextIf <- coreBlock rest next --return a cont?
  (ft,scope) <- coreBlock th nextIf --the scopes will be equal
  (fe,_) <- coreBlock el nextIf
  branch $ Jumpi v ft fe scope 
  --defines and returns the cont of a function which just branches!
coreBlock [] cont = return cont
Keeping (fun,pat) in state rather than an argument confuses things and
makes it hard to recursively call coreBlock.
coreFun:
 flattenedCont <- coreBlock body returnNull
 insert a call to flattenedCont

Consider case b of True -> {}… b will be on the stack in {}, but not after
the case. Do I need to emit more declares to catch [Declare s1] (next_s2)?

Don't force tuple erasure yet, instead cfun calls a flattened implem fun like
this:
cfun (x,y)*ret*stk =
 cfun' x * y.fst * y.snd * ret * stk
x = call f v --f returns a single value? Yes; note f must be a C function.
return stmts must therefore be compiled to push a single value.
When f is static it can be inlined; otherwise going from
x*y*z*stk to (x,y,z)*stk has no runtime cost, so it's still fine to push a
shared suffix.
The exec cost of each branch of jumpi cond f1 f2 val --depends on word ordering. Finding a good compromise is tricky... changes can be propagated
across a graph of statically called funs.
Coercion makes modifying the word ordering (CC) of all funs on a per-type
basis dangerous; don't do so, instead dynamic calls must comply with the CC.
That's for C functions; a known set of Core functions can have their args
jointly rearranged.
Tuple erasure might fit in a C-level pass: x :: Pair a b => x1,x2. Problem:
var x = f() :: Pair a b. Note vars are pushed in order, while a is ToS.
Add var Pair x y = f()?
x.fst => x1
A Core-level transformation would be simpler, but you must adjust the type
of BBs from (a,b)*stk to a*b*stk together.
Core lhses should not be required to be in [Var] form; state arguments may
vary. Explicitly separate state as a kind? Not passing them in one tuple
would be necessary.
Restricting lhs to var and doing a bunch of let x = arg.fst.(…) would work,
but the Core would become unreadable.
StackAndState : Type -> State -> Argument?
Ops may return stack and state...
The stack could be a field of an EVM state... no, I want the state to
be of variable type (but State kind) to be able to split out pointer region
maps.
By forcing explicit Type and State (,), I can ensure lhses are in a normal
form. Normal doesn't mean runtime-equivalent for stack, cf (a * b) * c vs
a * b * c. FW Idea: associative value and type constructors (*).

***
Back in the saddle after interruption:
Let lhs be tuples. Functions polymorphic in stk (and only the tyvar stk).
An additional State kind, state Pair and Nil, 
Arg : Type -> State -> Argument
Core functions are Argument -># End... but what is the kind of End?
What is the kind of Bytestring?
Tuple erasure isn't simple to do immediately; it must be done later.
Algo: divide functions into SCCs, replace f ((a,b),c) = ... with 
f ((a,b),c) = f' a*b*c.
Any call f x where f args = f' (re args) and re is a zero-cost reordering
can be replaced with f' (re x).
In jumpi b f1 f2 x where both f1 and f2 apply the same reordering (and they
will since tuple erasure converts es to normal form), it can be replaced with
jumpi b f1' f2' (re x).
--Jumpi simplification: b can be put on the stack.
Problem: th and el resist that, but they're vars, produced by ops...
Solution: either accept slightly more complex branch => stack calcuation, or
put b*th*el ToS and rely on opts in word-level Core.
return and revert can take (off*len) as stack; the absence of stk tells the
compiler you can ignore the preexisting stack rather than pop it.

After normalization, all bindings returning tuples should be in explicit form.
Need to extend Value to include state-level Nil and Pair and Arg.
Ops such as deref# may return an Arg.
Mutable state should be linear; it may be borrowed repeatedly, but consumed
only once. FW: datatypes that accept State params, e.g. to hide private
memory Rust-style.
If C function -> Core function conversion converted to normal form from the
start, things would be easier... no need to deal with dynamic functions
specially.
Then Core functions generated from structured can be independently processed
into normal form; the C functions start in NF.
Tuple-locals become (v1,v2,...) when used as exprs; v.fst becomes a sub-v.
Zero-sized types are eliminated.

Problem: with tuples eliminated, must tuple derefs turn into multiple derefs?
No: normalization makes tuples explicit, that's all. The lhs of a deref op
can simply be a tuple. The return type ((),a) can be normalized to a!
Exprs no longer return a single v... indeed, it may return no vs at all.
Are there any side-effecting, non-call exprs which return ()?
Struct field access may return a tuple; its lhs should be made explicit as
well. Since an E may be a call, all Es must pass their result on the stack
and call a cont?
That works fine with inlining: in f scope = let x = op in g (x*scope),
f's body is inlined to g's with the x parameter let-bound instead of passed.
As a result, it will no longer pollute the stack.

Problem with ([Var],[Var]) repr:
a*b*stk --ends with a var
[a] --just an a?
a*b --ends with b, that's fine
[] --ends with ()
It's a slight complexity, not a big problem?
Bigger problem: stk is a tuple. In practice it'll be :: tyvar stk, so NF
means flat tuple with no syntactic tuple types.
Core fun type: ([T],[T]) -># End?
Nil, Pair, SNil, SPair, Arg : Type -> State -> Arg
Always pass an Arg; (->#) : Arg -> End -> Type. 
Introduce Cont : Type -> State -> Type?
jump :: Arg (Cont a s * a) s -># End
jumpi could take cond, t, e on stack... do that for now, opt later.
Real repr:
body ::=
 let v = op v in body |
 jump v |
 jumpi (cond*th*scope,s) body --th passed dynamically, body takes (scope,s)
There's no clean LC equivalent and lets don't cross the jumpi boundary; all
the args to body must be in (scope,s). LC is perhaps not the right
abstraction...
An explicit Code a s type? That doesn't let you group and manipulate lets,
because all scope would need to be explicit. th still shouldn't be on the
stack... use "let ops in jumpi cond th el v".
Ops also take and return an Arg, ([Var],[Var]).

Tuple erasure:
Step 1: in all Values (lambda lhs, op lhs and rhs, branch arg), 
>>= tupleVarToVars. Convert x :: Pair a b to x.fst, x.snd, then recurse.
Convert all zero-sized types to [].
Step 2: x = vs.fst@ts => x = copy (select_fst vs ts)

Note for writeup: Core's jumpi doesn't fit JUMPI well. Core will not be the
be all and end all of EVM IR's...

Should scope in trueMain be fixed to ()? Does that offer any benefits?

Thought: a stack of SElems alongside a dynamic stack could make sense. That
would require quantifying both over stk and sstk.
Use case: a recursive function that allocs new pointers in each call.
I need that for allocs to be able to spawn new state vars and pass them
across call boundaries. Until that's done I can only use memory slices within
a single BB.

After I get an asm backend done, I could make a word-level Core -> Yul backend and compare perf.

For writeup: major steps of Structured->Core: 
In Structured creation: tuple erasure.
In Core creation: structured control flow and Call pseudo-op to branch.
After Core has been produced, SSA to erase stack assignments.
Copy erasure.
Debug: check Core is well-typed.

Pruning dead vars in cyclical code: identify statically called subset, form
SCCs. Vars demanded by lower SCCs or dynamic calls are live.

How to commute the call graph, e.g. reordering C function calls or lifting
constant calls out of loops?
Stack frame rule: f : forall stka stks . Cont (a * stka) (s * stks) can be
extended with stack and state it's guaranteed not to touch by theorems for
free. Minimize function arguments, explicitly call extended f?
Downside: it might not be efficient to swap its args to ToS. You could perhaps solve that by reversing the transformation later.
Extending Core with a host language for reasoning about call graphs: 
f vs => g symEs or if symE then ... else ...
Well-behaved while loops can have their vs -> vs effect expressed as a single
letrec expression.

C-level state parameters for functions would be cool... a -> b as a type
synonym for Fun a b Env Env, calls as sugar for passing default state.
Effect typing: reads/writes state. Effects should be wrappable in pure APIs if you isolate them (e.g. don't let a pointer escape). Mutating the internal state of an object is a side effect... it should consume a handle to the object? With linear typing you could have session types, e.g. UPtr -> Ptr.
free as an op that consumes the pointer...
Pointers and objects parameterized by accessible range.
Const pointers?

C<->Core fun conversion enables safe function coercion, e.g.
(a -> b) -> ((a,Word) -> (b,Word))... but that requires substituting the
stk in the argument for an external (Word*stk).
Note: hardcoded tyfams (complex type relationships) are expressible in Core;
you can reject a subset of programs which look well-typed from the C type
checker's perspective. However, the annotations which feed tyfam-derived info
to the C type checker must be added manually in response to type errors.
C-level foralls would be cool, ex: 
apply : forall a b . (MPtr a -> MPtr b, MPtr a) -> MPtr b
That could then be applied to existentially quantified fields, e.g. in an
AST or closure.
Issue: you'd need sizeof and similar constraints. Does template polymorphism
preclude Haskell's repr -> kind approach?

jumpi_el (cond * th * rest, s) =
 if cond > 0 
 then th (rest,s)
 else el (rest,s)
el should be a Core function, not a var...
Nice, that reflects the fact that th is dynamic but el must be static.
Relevant opt: shared suffix lifting (to before the branch).
Structured will exclusively emit jumpis with static Core function th and el.
A C function should be represented as core2c coreF; when statically called
c2core (core2c coreF) can be simplified to coreF.
A single function can always be copied (~loop unrolling); a SCC can also be
copied, which can be more size efficient. Variants can then be specialized.
Each fun has a list of call sites with symEs; if one var is always a single
symE then it can be pushed down into the fun.

Would it be worth it to word-split C types in Core, a la the old IR?
Then field access can be expressed directly using shift, mask and or ops,
and var dropping logic needn't be duplicated in a separate word-level Core.
Primitives could also be implemented directly as EVM ops.
There would be two kinds: Word and State.
WPair : Word -> Words -> Words.
Problem: need to distinguish w1*w2*() and w1*w2?

Implement field access directly, don't group fields. Symbolic eval of shift,
or and mask can optimize foo.bar.baz more generally than a dedicated opt.
Implement primfun calls directly as ops.
p->field should also be optimizable: incrementing the pointer is equivalent
to performing a shift.
mload (p+k) costs 12 gas... it should also be possible to aggregate reads.
Is there an optimal normal form?
Recognizing shift lets you optimize across field dataflow, e.g.
var x; x.field = y.field --should be convertible to x = y mask m
Field aggregation: the or of contiguous slices == a larger slice.
Between branches (when stack must be explicit) fields can be considered
separate?
Assume every *ptr from C has valid padding wrt the need to mask; assume
struct in struct.field has the padding corresponding to field's constructor.

Ops are generated at the Structured level. They should correspond to EVM
opcodes, so compilation will be trivial.
Mathops => EVM ops and mask_n.

Lifting out constant expressions from loops: consider x + y in a while loop
where x, y aren't touched. Recognize the given vars aren't mutated in
while->while. Lift the op out and replace it with a param.
That can be used to cache globals if you know their memory isn't modified...
But for now that just means "no memory is modified".
FW: a SElem stack.
With explicit SElem returns you could avoid unioning new allocs.
If the user could control it, they could e.g. make a State DT for storing
allocated List cons cell memory, guaranteed to contain reachable(result)?
Linear SElems stored in C values would be cool...

Proper function extension:
a*Cont (b*stk)*stk --a -> b
stk = c*Cont (d*stk)*stk
Res: forall stk . a*Cont (b*c*Cont (d*stk)*stk)*c*Cont (d*stk)*stk
Corresponds to C function type (a,(b,c) -> d,c) -> d
f becomes \(a,cont,c) -> return cont(f(a),c)

*********************Value -> ([Var],[Var]) changes**************************

Structured.Convert
Q: Also erase newtypes? No, Fun and Ptr are newtypes and have invariants.
The rest don't... FW, if at all.
When a tuple is added to scope, it must be flattened first.
An E may now return multiple vars, or none.
Ah, now that sizeof is relevant to value flattening it must be in the convert
monad's reader.
Plan: erase tuples here, not in Core.

Should I ditch the Const op? It seems ugly to retain the higher-level lang I
went to such trouble to eliminate. A better alt could be symbolic exprs:
integer literals, typed asm symbols, Con {field: sym}
Then I can still CE struct.field.
Invariant: the exprs should be implementable with a push. The same SymE type
could be used to represent State vars and for symbolic simplification.
Note: function symbols are Core functions... and they're quantified!
Problem: EVMC's Fun datatype decl says it's a Short newtype, giving it its
sizeof. So SymE would need to include the core->C coercion operator?

For now, limit effects to binary did/n't modify mutable storage region.
p = alloc(n) consumes (and does not return) a splittable ap, mem and produces
(mem ||| new_chunk). That should correspond to an op so p->new_chunk can be
remembered.
Since I don't use the output of state, emitOp can return a [Var] for now?
The returned vars should be new and depend on Arg type params.
It only takes Args! That makes pure stack ops a bit more awkward, but I
can't distinguish between SUnit and no State just by looking at the vars...

Convert deref directly to a primop? Calls take and potentially modify all
state, but let you share code.
Calling is more general, optimize later.
Should I move the Core types Arg, Cont, SPair, SUnit to AST.DTs?

Should the region param to a pointer be Memory or a Memory# slice?
EVMC's type system can't handle the latter yet, but it's an interesting idea
to explore. I'd need a subslice relation.
Functions that alloc would return a different slice type than they consumed.

Argh: with tuple erasure, CaseTag must now take a list of vars!
A nil tag is common, but using a Pair as a tag is a bit maniacal.
Nonetheless, it must be supported.
For writeup: simple rules mean you encounter "maniac programmer" edge cases.

Structured.Convert has grown too large... TODO decompose it.
Candidate modules: Monad, assign logic, case logic...

revertValue() should be optimizable to revert 0 0. The latter 0 by inlining
sizeof, the former by realizing the value of zero-size pointers is
irrelevant. If I enforce that, void pointers will not be usable...
That's fine, note it and encourage users to use Byte pointers instead.

Now that I can tell whether a type is zero-sized, I could elide zero-size pointer read/writes and assignments. That belongs in Core opt...

FW: By adding quantification to the C type system, I could represent
Core funs directly. I would also need to add richer overloading, where types
aren't just a set of QP types but can do computation to impose constraints.
Ex: 
 sizeof : P a -> PN n --where n is a function of a
 cfun2corefun : (a -> b) -> forall stk. Cont t Env 
--where t is a function of a, b
Simplest approach: introduce a v = TyFam a..z constraint, unify v with
result when a..z are monomorphized. The tyfams could be primitive.

**********************Word-level Core****************************************

I need to make a number of changes to Prim and Prelude... first step:
generalize compile-time module loading so I can split the modules.
Result: a namespace from which I can selectively import library modules.
Alternative definitions of sugar functions (plus, fromWord etc) would be cool - overflow checks could be enabled, for example.
Also alloc and indexPtr definition (word-pad, mask vs overflow check vs
nothing). Prelude should import sensible defaults.
FW: Core and asm definition syntax, so I can define the EVM op functions as
non-primitive.
Two IO functions: dirNamespace, modNamespace
State: namespace, Read: path.
Goal: keep the rest of the pipeline pure.
I need to parse the decls, so I might as well bucket the decls by type and
key.
Merging may fail due to duplicate keys both within and between modules; report the culprit module(s) if so.
Ops: add decl to DeclBucket; merge DeclBuckets. Associate decls with module
name so I can report conflicts that appear after multiple merges.
What happens if multiple modules in the path match the given name? Don't
complain, just choose the first one.
complete :: ModuleName -> Namespace -> Either MergeError DeclBucket
In the pure pipeline, give the text the module name Main.

Path and whether to use the statically parsed Stdlib should be compiler
options.
TODO change Module structure to reflect..?

Open class functions complicate decl conflict detection... but they're worth
having.
Instances with sub-instances with a specified match order would be powerful,
that's like closed type families.
instance f : T where {
 f : T'; f p := s; ...
 }
 

I'll use --functor to associate each CST node with a srcspan. I then need to
enhance the srcspan with ModName. Not filepath... a module namespace is not
inherently associated with a file system. But it is with the source text for error messages...
Nice, I can add ModName to each location annotation using Generic.
I should be able to use Generic to infer the span from the location of each
element... the end of x in Con x y is the start of y. That fails for the
last element.
Compiler commands:
State: path, stdlib_on (default True)
add-path dir
stdlib-off
load(dir|filepath)
compile text --e.g. import Mod
compile modname
I'll just use single locs for now, getting spans working is not top prio.

A datatype clash on TyCon implies a clash on .tagTyCon... but I'll just
naively report all clashes for now. The loc of .tagTyCon should be the loc of
its parent data decl.
Quirk: the r value in the con ImplList (Ptr r (ImplList r a)) will refer to
the r in "region r". .implList_hd will have hd's loc.
Should I change implList_ to impl<Con>? No, that forces the user to remember
the field's Con when accessing an Impl<TyCon> field.

Recall issue: when allocating Nil, the max size of ImplList will be allocated. While that allows safe con change, sometimes minimal alloc would
be desirable.
Loading a variable-size DT could leave garbage if it's allocated to a minimal
store... that's fine, deref shouldn't require a case.

Location info could be hidden behind refs that are only accessible in the
error path using a monad; that would guarantee they don't affect compiler
output.

Ah: .tagList should be a boxed field, desugaring to 
.unImplList->tagImplList. Unboxed constructors of boxed datatypes (i.e.
Impl<TyCon>) should be compiled to copy/id ops.

Module structure: it makes sense to cache the defun, instance and global name
sets, but their info should be in a single map since they're in the same
namespace.
The Module type is a misnomer, since it's the entire program.
Plan:
Generate PreModule --done modulo testing
File IO API (load folder, file) --dmt
Update compile-time Stdlib loading --done
Split Stdlib: EVM, alloc (with initAlloc, memOffset), ptr, int etc.
 Which types are essential to compilation?
  Int and Pair are treated specially in sizeof
  Array has a dedicated construct in E; construction and indexing has
   dedicated logic.
  k => fromWord k#; k# is Int Unsigned 32
  Boxed datatype construction and destruction uses Ptr
  Code and returndata deref uses alloc/scratch.
 Which types are used in desugaring?
  Globals are given type Ptr r a
  (a,b,c) => Pair, Unit
  Struct (a,b,c) => Append, Unit
  Array (a,b,c) => array construct
If I had proper namespacing I could desugar and typecheck stdlib modules
separately. But Core relies on fixed global addrs...
For now put the types the compiler mentions in Types.evmc.
TODO figure out the dependency hierarchy; it's all tangled together atm.

Q: What do I need to support NatList?
Nil : NatList
NCons : Nat -> NatList -> NatList
Memory : Region is a kind sig... NatList should be supported out of the box,
otherwise Memory:Region would break.

Now:
Pair, Struct, Array desugaring.
Pair a b => Append{first:WordPad a, second: WordPad b}
Pair {fs:es} =>
 map each f using {fst => first, snd => second}, fail if f not in the map.
 result: Append{mapped fs:WordPad es}
Struct and Array take only syntactic tuples (not Pair), fail on 
Struct/Array{...} to prevent the user from using their own datatype with that name.
Apply the same desugaring in Pat.
Struct (a,b,c) must also be desugared in T; Array is an ordinary datatype
there and Pair is a tysyn.

Without memory var initializers,
trueMain (stk,env) = jump main (stop*stk,env)
If main is only called there, it can be inlined. Then even if main returns,
the return should be inlinable to a stop branch.
If it ignores $ret, it should be possible to avoid pushing stop.

Special types the compiler is aware of:
Int, Ptr, Fun
Pair, Struct, Array, WordPad, Append

Core compilation: keeping Core types (of kind Word and SElem) in T is fine.
Fun a b => flatten a to ws,
forall (stk : Words) . CoreFun (ws...stk s) Env
Note: since tag exprs may contain allocValue@Code (an instance which errors if called at runtime), the consts from the Serialize phase must be used in Structured for case,assign,Con. For now, fix global layout after Mono and serialize to bytes. Use the same DT for serialized consts as for push.
Note multi-word consts will need to be split when pushed.

TODO support general pattern matching for function lhses.

Pat only supports record constructors; that means I also need to pass in
constructor fields into desugar(S|E|P).

Possibly worth writing about: was it a mistake to try to break out the
desugaring steps into generic ops? The monolithic "one big desugarE function"
approach was more concise, and it was clear in which function the work was
being done. The downside was more context needed to be passed at once,
necessitating a typaram for DTsInfo.
Need to identify and express the underlying pattern for it to be interesting.

Typechecking is surfacing bugs in Desugar and stdlib; good.
IC why allocRawMem's (memAllocPtr = n + pw) generated **memAllocPtr = ...:
PVar g => Deref (Var g), then Var g => Var "deref" :$ Var g --in
Desugar.Desugar.substGlobals. Solution: switch the order.
Nice catch: I'd missed defining returndatacopy.

Problem: the type checker complains the tag of Addr is queried. Addr has tag
scheme Nil, so TC expects no tag to exist. However, DeclBucket generates a
tag field for every DT; inferring non-explicit tag schemes is done later in
Desugar.Datatypes. The question is then: should types with tag scheme
Nil have a Unit tag? Then Unit becomes a special case with itself as a tag.
I need to detect clashes between ordinary fields data Foo = Foo{tagList: ...}
and generated tags; that's done in deconflictBucket, so adding tags later
would duplicate that work.
Hackily removing tag fields once their tycon is known to have tag scheme Nil
wouldn't work: it would falsely complain of a field clash if another DT
had tag<TyCon> as a field.
Decision: it's best to minimize the impact of the new Desugar on the rest of
the pipeline; typecheck should be able to accept its output without changes.
DT's with tag scheme Nil should therefore have no tag.
But SEP must know whether tagList is boxed in order to compile
xs.tagList = v... but SEP di is used in Desugar.Datatypes!
Solution: infer separately which DTs have tags and whether they're boxed.
Currently BDTs have tag scheme Nil... should that change?
IsTag has a fiBoxed flag, suggesting I intended them to be tag.
The current issue seems to be caused by .tagAddr being added to fields after
Desugar, probably in FIKS.
The problem with giving BDTs a non-Nil tag scheme is that ImplList should have a nil tag.
Possible solution: desugar away .tag<BDT>. What should you be able to do with
it? Cons{tagList: 255} should not be accepted, either in E or Pat; Cons implicitly sets it to 1.
The only remaining use is xs.tagList, which could be desugared to
*(xs.unImplList).tagImplList. Annoyingly, that's not exactly the same scheme
as for xs.hd, which would result in implList_tagList.
OK, so I pass in richer info than diBoxedFields: for the purposes of desugaring, a field can either be unboxed or boxed (normal | tag).
The type checker must be aware of normal boxed fields since they occur in
patterns Cons{hd: x, tl: xs}, but not of boxed tag fields.
TODO remove fields from tysigs + the FIKS step, it's cruft.
tagList can still be in fieldInfo for interface purposes, it should never be
interacted with after desugar.
A BDT has a boxed tag iff its ImplDT has a tag.
A UBDT has an unboxed tag iff it has an explicit tag decl or >1 constructors.
I can add the tag fields after processDTs, then the criterion becomes:
if boxed, ImplDT has tag scheme /= Nil
if unboxed, DT has tag scheme /= Nil
Oh no... there's a cyclical dependency there: before the pattern bdt.tagTyCon  can be safely desugared, you need to know the full set of boxed tags.
So it needs to be in DInfo!
Inefficient solution: infer which DTs have tags before processDTs.
Turns out TC doesn't accept boxed tags yet anyway... oh well, the desugaring
code for them will be ready when I adapt TC  to support them.

TODO add support for boxed tags. 
I can't eliminate FIKS because it must run after tysyn subst, otherwise
"import Prelude; type R = Region; List : R -> Type -> Type; main _ := ()"
fails. Recall: kind sigs used to be mandatory for BDTs; I just copied the
kind sig from BDT to ImplBDT.
I can still auto-generate the kind sig of BDTs and copy it to ImplBDT, but I
should not check the given kind for validity in Desugar.Datatypes.
The check that tycons with a concrete kind have a data definition should also
be deferred until FIKS! The arity check must also be deferred, since
type Arity1 = Type -> Type --hides arity.
Must be checked after tysyn:
 BDT region param is Region --FIKS checks
 DT kind arity matches params, is concrete --FIKS checks
 tycons with concrete kinds have DTs --FIKS checks
OK, I'll keep FIKS as it is for now... but where are the fields added to
tysigs?
It's in HM.AddConsAndFieldsToTySigs... and HM uses tysigs for both fields and cons. 
It's debatable whether that's a bad idea... I'd need to cache the
signatures somewhere anyway. I'll leave well enough alone for now.
Next: global layout, treat global pointers as constants in Serialize and
Structured.
How to support memOffset et al? They should be constants so they can be used
in const expressions, so they shouldn't be functions.
One solution: memOffset : (); memory memOffset; always place *Offset last.
Then the pointer can be used as &memOffset.
The same approach could be used for end: end : (); code end = ();
I'll rename it to codeOffset for consistency.
All code global pointers must compile to labels, so it can be treated
consistently (except that it's always placed last).

IC why I used normalizeContent in fromWord k: an Int s 0 should return an
empty list of content when serialized, not a Left [].

main _ := foo; code foo = &memOffset
--complains Ptr Memory uint32 isn't mentioned...
Sizeof must catch cycles, but caching the sizes of all types feels like a
code smell... it invites this sort of error, especially if I use new monotypes in Structured.
Alternatives:
1) Try to catch all monotypes, including those that will be generated in
Structured.
2) Smarter *potential* cycle detection for any combination of well-kinded
tycons; lazy computation and caching of sizeof.
Potential cycle detection seems challenging, consider
data Foo a b c d = Con a (Foo b c d ()). That has a bounded size, but looks
problematic since it's recursive.
Then there's datatypes which take tycons as params, e.g.
data Rose f a = Leaf a | Node (f (Rose f a)), which diverges for some f but
not all.
There are also divergent datatypes which are recursive in tycon but not in
params, e.g. data Diverge a = D (a, Diverge (a,a)).
Hacky solution: forget identifying divergent DTs just from datatype
descriptions, instead compute+cache sizeof incrementally and detect divergence using an arbitrarily chosen stack height limit (e.g. 100).
It's analogous to GHC's strict bounds on type family recursion; you'd only
encounter the limit if you're deliberately messing with the type system.
The con would be 1) arbitrary recursion limit, which is a pain even if the
user can increase it; 2) the need to pass around the cache.
The cache can be dropped, it's just inefficient to do so.
The solution for now might be mundane: the error is triggered by &memOffset,
but its type shouldn't be Ptr Memory Word anyway...
No, it's triggered by memAllocPtr. Perhaps because it lacks an initializer?
It must be in exploredGlobals, so it's monoGlobal that failed.
Sizeof.computeSizeof takes a set of monoTs; it's fed exploredDTs monoS by
pipeline2sizeof. Evidently the global tysigs weren't explored.
As I suspected, main _ := D (); data D a = {D a; E (D (a,a))}" leads to an
infinite loop rather than a detected cycle error.
I could explore the global tysigs, but it's rearranging deck chairs on the
Titanic: the one-and-done sizeof calculation must go.
Replace with an incremental sizeof with a cache + recursion limit.
Consider data D a = {D1 (D(Left a)); D2 (D(Right a))}... that would lead to
exponentially many datatypes with a BFS, but thankfully DFS is simpler.
With n typarams I can encode a counter:
data D a1 .. aN = D (a1, D a2 .. aN ()). However, without a clever trick to
recognize D () .. () has sizeof zero, that'll hit the recursion limit.
With no GADTs, a datatype which contains any parameterization of itself
unboxed should fail... as such, there's no need for a recursion limit, just
a stack of tycons.
I can make a type which leads to exponentially many parameterizations anyway,
using datatypes D1..DN as a counter:
data D1 a = D1 a;
data D<n> a = D<n> (D<n-1> (Left a), D<n-1> (Right a))
TODO document that the compiler does not guarantee O(n) cost for crafted
programs, and that anyone running the compiler as a service should sandbox it.
Q: Even if you detect and prevent trivial recursion (D params indirectly containing D params'), can that be defeated with a Y combinator-like
construction?
D : (Type -> Type) -> Type -> Type
data D f a = D (f (D f a))
f may or may not contain D f a (cf Proxy or Maybe). Recursion can still be
trivially detected for a particular instantiation.
New API:
new :: DTsInfo E -> Either [Name] SizeofCache
--Failure mode: trivial cycle (D params contains D params')
query :: SizeofCache -> MonoT -> Either SizeofError (Integer,SizeofCache)
Failure modes: recursion depth exceeded (100), cache size limit reached (1000).
Note a parameterized type may contain another parameterization of itself,
e.g. Maybe (Maybe Byte); that's not an error.
Statically detecting simple cycles is nontrivial; consider
data D = D (D,D). The top-level con is Pair, not D. Cycle detection must
operate on monomorphic datatypes.
main _ := D (); data D a = D a | E (D (a,a)) diverges in mono, not sizeof...
Mono also needs a recursion limit.
main _ := f (); f : a -> (); f x := f (x,x) also diverges.
Why can't I drop exploredDTs from Mono? Because DTs aren't side-effect free,
they can alloc code globals.
Radical proposal: fuse the Mono, GlobalLayout, Serialize, Sizeof and Structured steps.
After a function is instantiated to be monomorphic, convert it to Structured.
Place globals as they're encountered, replace global pointers with constants.
Exploring code globals triggers serialization of their initializer;
exploring any global triggers sizeof of its monoT.
Any use of a monoT triggers exploration of it (including null(), since you
need to know its word size), which in turn triggers exploration of its
field types, tag type and tag scheme.
TyApp f ts => explore f@ts, vf <- push2 (label f@ts), return vf
Nice: if Structured is fused with Serialize, it's easy to use the same Content type for code globals, tags and pushes.
I'll start without support for allocValue@Code in consts.
Unshadow operates on MonoS... todo move it before HM.
Mono, Sizeof, Layout, Serialize, Structured... that's 5 stages getting
merged... hopefully it'll be simpler the second time around.

Next: move unshadow before TC. --done
Trivial exception vs throwError bugs:
ghci> pipeline2typechecked "main:()->();main _ := ()"
*** Exception: No con info for Unit
CallStack (from HasCallStack):
  error, called at .\Desugar\Desugar.hs:235:27 in evmc-0.1.0.0-inplace:Desugar.Desugar
--main _ := Unit correctly throws NoSuchCon. Solution: convert tuples to CST
--Pair/Unit and recurse on them rather than to AST directly.
Pat does not have the same problem: "main () := {}" fails with a throwError in TC.
main x := Pair {} and Pair {fst: x, snd: x} now throwError, complaining
NoSuchCon Append and WordPad respectively. That's correct, but confusing;
TODO explain the cause may be desugaring when rendering the error message.
Struct () () --should be an overapplied con, either a desugar error for Pat
or type error for E. I'll leave it as it is for now; TODO explain
overapplication is also wrong when rendering.

ghci> pipeline2typechecked "main _ := {}"
*** Exception: Compiler error: tycon "Fun" not in scope despite FIKS
CallStack (from HasCallStack):
  error, called at .\Typecheck\HM.hs:780:18 in evmc-0.1.0.0-inplace:Typecheck.HM
Removing the special-casing for -> in the kind check ensures it correctly
complains iff Type and Fun are not in scope.
What if one gave Fun a different representation...?

Generating calldata using the interface: support allocValue@Calldata a la
allocValue@Code in Const?
Export f@ts, global addresses in interface. TODO support marking functions
exported to force placement.

Modify Desugar to take PreModule (simplifying Desugar.Datatypes) --done,
 --modulo debugging
 Get old desugaring working on stdlib, then update rules.
 TODO change Struct type syntax to Struct (a,b,c) from Struct a b c to mirror
 exprs and patterns.
 --Convert strings to inline byte array literals again? Yes.
  Global context I need currently:
   global set for g => *g
   Con fields for Con args => Con fs --E' -> E, P' -> Pat
   Boxity for 
    Con fs => ImplTyCon (allocValue (ImplCon fs'))
    x.field => *(x.unImplCon).implTyCon_field
   string numbering --no longer needed
Pair a b => Pair {fst:a, snd: b}
Struct, Array desugaring can be done on pairs.
Con args => Con fs can be deferred.
How many transformations can I separate into their own traversals?
 --Prohibit memory global initializers --done
 --Also make WordPad the padding primitive
  Pair a b in PET => Append (WordPad a) (WordPad b)
  Pair {fst: a, snd: a} => ditto
  x.fst => x.first.unWordPad in PE
  Opt: p...field.unWordPad = v => p...field = WordPad v
  Local assignment behavior:
  x.a...fields = v =>
  tmp = x.a
  tmp...fields = v
  x.a = tmp
 --Change in Sizeof: compute field offsets per monotype
Complete Structured --use ppr to test
 --ampersand needs addPtrBytes
Core --initially no opts
Stack: Core -> Asm --initially naive, just ops in order
Test using setcode
Opts: op pruning, naive inlining, TCO, D/CE
Stretch: mem splitting/alias analysis, symbolic eval of memory alongside
stack values. Symbolic effects => more efficient, reorderable ops.
Module objects: initially a plain recursive call to compile using the same
namespace, ignore code size concerns. 'module ModName' returns a byte array
literal. code m = module ModName;
codecopyPtr(memp, &m); returnPtr(memp)
Pathological case: a module contains a module literal of itself.
Add label end : Ptr Code Byte, set it in Asm. Ensure *Offset work.

CE+DCE should let me optimize storage global writes, avoiding the "value
straddles two words" check. Lay out storage vars naively (sequentially), no
minimizing straddling.

FW compiler maintainability issue, possible functional pearl: figure out how
to state rewrite rules in isolation (e.g. Struct tup => ...) while fusing the
traversals to make it fast.
Challenge: the type changes from CST -> AST; finer-grained types might
introduce more steps inbetween.
It's related to opts: apply rs until fixpoint would be a useful combinator.
Maintain exec intensity, code+exec gas cost per-block caches at the same
time?

Ah: a ptr to an a is never padded. If Pair is the wordpad primitive, 
&(p->fst) will therefore be p+padding bytes of fst. So p->fst = short will *not* be efficient, you need to use p->fst_ = WordPad short instead!
data Pair a b = {MkPair {fst_: WordPad a, snd_: WordPad b}
The expr Pair a b could then be desugared to MkPair (WordPad a) (WordPad b).
data WordPad a = {WordPad {unWordPad: a}}
sizeof WordPad and offset of unWordPad would require special treatment.

SAR lets me save 2 gas when dividing a signed integer by 2^n...
I'll use SHR rather than SAR for >> on signed ints to make it useful for
manual record construction; whether to use SHR or SAR seems to be a free
choice in the C89 standard.

0x0100
x, y
addmod
--Has the same gas cost and code size as
x, y
add
0xff, and
--The latter is preferable due to more flexible stack scheduling.

Q: Are defaults checked for monomorphism?

Strategy: get a minimal subset working, then extend. A Core consisting only of EVM ops can be trivially (if inefficiently) compiled.
First: just locals and integer literals.
Need writePtr and evm_return for Hello World.

Q: Should alloc even be a primitive? No: every primfun that compiles to
multiple (non-DUP/SWAP) instructions should be a function; leave it to the
inliner to decide whether to inline and accept the initial perf hit.
Eliminate UPtr, the memory optimizer should operate on bytestrings.
plus : (Int s l, Int s l) -> Int s l
plus (a,b) := {
 a :: Int s l;
 var m = mask (PN :: PN l) --Word
 return unsafeCoerce (and(m,add(coerce a, coerce b)))
}
EVM ops (and, add etc) should be true primitives. coerce and unsafeCoerce must be primitives because they operate at the word level, which C doesn't see.
mask is a useful primitive: mask@n is 256^n - 1 (instances for 0 to 32).

For writeup: what to make primitive is an important choice. Too much and you make the compiler inelegant, too little and you don't use the full power of
the Core abstraction (and must rely on potentially unreliable opt to make the functions performant).

I do NOT need to fix global layout before Structured conversion.
Structured/Core gives me an upper bound on global addresses that are actually
used; D/CE lets me tighten it. Global placement and D/CE interact; place
globals incrementally, then substitute their labels and D/CE further.
Note exploredGlobals doesn't cover all accessed globals; alloc accesses
memAllocPtr.
First approximation: just scan for all globals used and place them.
FW: Identify globals that are accessed, but have no observable effect on output; prune them. That's particularly useful for memAllocPtr = memOffset
when no allocs are performed.
Spec:
All m/s/t globals that remain should point to a non-overlapping, contiguous
byte space at 0. *Offset should point to the end of it. They may be placed in
any order.
Note that means program behavior (&g1 > &g2) may depend on compiler
optimization choices; an EVMC program can have multiple valid compilations.
Global lifting and location sharing opts would complicate that further, but
*g should be guaranteed not to interfere with other globals or alloc'd space,
as long as you don't mess with the allocPtr.
Note: since pointers are only 16b, I can't express very large storage arrays
using globals as they're defined now.
Code globals remain labels...
FW:
Let users control layout with a 'layout region x, y, z' decl.
Set layout incrementally based on Structured (e.g. most common global at 0).
That opt would be most useful for allocPtr.
Compiler-controlled word-padding of globals.

TODO unify serialized and Const repr. Non-code global pointers should be
bytes! FW: CE constant expressions (global initializers and DT tags).
Ex: &(g->field) should be permissible.
So I need to determine layout before serialize.
FW: if sizeof were incremental (a stateful server), I could call it lazily and without worrying about whether I've computed all sizes.
Nested monads rather than pipelines would let me add effects...

Memory lifting would be useful for sub-word global write opt: you could lift
the spare bytes until they're accessed. Writes could even be deferred!
A CALL/CREATE potentially touches all storage... analysis across multiple
calls would be needed to rule out slots.

GetField, SetField and GetFieldPtr should be removed and replaced with their
constituent EVM ops.
p->field can be straightforwardly optimized after naive compilation: unused 
words are simply dropped. p->field = e still needs to use +k.
Const should be eliminated and replaced with push.
Issue: labels and bytes may be mixed in a word. A label may also appear
truncated across several words.
CE should be able to deal with labels.

No need to worry about tuple erasure? Tuple-typed vars can be distinguished
from their content; field get and set are still cheap.
Could I easily could get stronger function lhs pattern matching by declaring
the params as locals and then performing an assignment? No, because a
failing pattern match ends the BB.
As such x:A, y:B should be on the stack for lhs (x,y):(A,B).
Conversion to tuple must then be undone when inlining.

Var kinds: Word (T,Int) or SElem
Problem: Core functions refer to SElems, are quantified and are a word.
So I need to add a tycon WordOf : Type -> Nat -> WordK
WPair : WordK -> Words -> Words
Do I need or have any use for WNil? Yes, for expressing 
revert (Arg (off,len) (mem,sto,tsto,ext)) - where stk has been dropped because it's irrelevant.
State should also be future-proofed to take a stack of states.
Argh, that means ([Var],[Var]) doesn't fit again. It'd have to be [Var],
stack: Maybe Var or explicit Arg, (W/S)Pair/Nil.
The latter is best; it's general and avoids overloading Pair as both a C-level and Core-level construct. It can be ill-kinded, but so can [Var],Bool.
Cont : Words -> State -> Type
The kind quantified on should be specified in forall; in future functions
will be quantified on both the Words and State stack.
That way I can kind-check Core functions despite quantification.
All ops still take and return an Argument, but they never take a stk.

Pointer aliasing: allocs, globals and struct fields are disjoint. Does pointed type tell me anything about disjointness? If not, that's fine.
Cop-out: split memory only within static call graphs.
FW: alloc becomes a primop that consumes an AP but not mem.
For now it just becomes another mem-touching op. Express directly as its
implem. Then I can use the Prim definition; skip uptrs.
If I must initialize to prevent corrupt reads (e.g. for alloc 1 :: MPtr (WordPad Short)), do so manually. If I pre-alloc a buffer for scratch memory,
I can assume allocs produce zeroed memory. I could save the buffer and its
size in globals in order to dynamically expand it... allocating 2^n bytes
ensures linear overhead.
For now: no memory opts, analyze and improve later.
Recognize that mutating mem ops consume their mem argument, schedule all reads to the old state before them. That applies only within a BB; branches
consume their arguments by default.
Since I'm compiling a single exe, I might as well replace global ptrs with
constants? That would let me CE more, and if I don't use global identities
anyway it's no loss. Doing one thing at a time lowers the hurdle I need to
jump...

You get zero-sized type => no vars for free
Compile primfuns directly to ops, only define their types in Prim.evmc.
Only word-padding remains... n bytes of padding must be followed by 32-n bytes of content. Values are byte-aligned, simplifying matters.
deref once again becomes a series of mloads (optimize to copy when possible).

Byte access on stack and in memory can be optimized with BYTE and MSTORE8
respectively.
Field access should make use of padding; when the other bytes in an affected
field are zero, don't mask.
Extension: data Zero and Arrays or structs thereof are zero.

Padding should be taken into account when derefing and writing to pointers.
Reading an uninitialized ptr is ofc UB... do padding/value-based opts force
me to use UPtr? A word-padded byte can be written to using mstore8 (p+31),
but mstore p is more efficient.

*****************************************************************************

Overall design goal for Core ops: they should be trivially compilable to
word-based Core.

Core first-pass compilation:
Allocate a fun for each op, inline later...
Pattern assignment is complex...
scope: Arg (v*rest) env
x = v => let x = v in next (Arg (v*rest) env
--Note assignment keeps the v on the stack!
*(p :: Ptr Memory a) => 
 let (Arg () mem') = writeMem (Arg (p,a) mem)
 in next (Arg (v*rest) env)
x.fields.field => --unoptimized
 old = x.fields
 new = old{field=v}
 x.fields = new --recurse
Con (check=True) fieldps =>
 if tag scheme = Nil:
  Con (check=False) fieldps = v
 else:
  t = v.tag
  b = k == t
  jumpi b (f: Con (check=false) fieldps = v) revert (v*rest,env)
--
Con {field: p, fieldps} =>
 v' = v.field
 p = v' --expr, not op
 Con (check=False) fieldps = v

Reordering f x should be ez if f, x are independent and involve no control
flow: inline the ops until the jump, then you're set (stack scheduling
should do the rest).

***

Storage API: byte-aligned storage pointers are necessary for field access,
but expensive and limited to 16b.
Word-padded API:
data Slot a = Slot Word
get : Slot a -> WordPad a
set : (Slot a, WordPad a) -> ()
toPtr : Slot a -> Ptr Storage (WordPad a) --fails if slot too large
Accessing fields or indices could use custom DTs... SlotArray a?
sload_n : (PN n, Word) -> Array n Word?
Problem: I have no way of checking whether a Nat is 0 or to do type-level
arithmetic. I can't convert Nats to Peano repr at the type level either.
The best I can do is use PN->Word, implement the logic dynamically and
rely on specialization (CE, DCE).
With pointer reasoning I could optimize an impl of sload_n that used alloc
to one that pushed to the stack... Actually, allocating an on-stack array and
setting its indices in a loop (which should then get unrolled) should work.
Static index writes to an on-stack array of a whole-word type should be zero-
cost.

Idea: If BBs can be compiled down to word IR and then stack code in isolation, you could peep the compilation to inform exec cost and code size
while optimizing Core.

Look into applicability of concepts from database normalization (e.g. 3NF) to
application-specific data structure design (e.g. my IR types). "No redundant data in key (non-cache) fields" is an obviously desirable property, preventing bugs caused by inconsistency.

***

Closures:
//If you want to close over a large type, better to alloc and use a pointer
//instead.
data Closure r a b = Closure {fun:()->(),closed:()} region r;
mkClosure : ((c,a) -> b, c) -> Closure r a b
call : (Closure r a b, a) -> b
Pushing an unknown number of words can't be done in C, but perhaps in Core
using coercion... would need to be careful opts don't mess it up.
Store c word-padded.