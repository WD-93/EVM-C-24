Abstract interpretation could let me pass symbolic info across
function boundaries, and run a variety of analyses using the same AI
combinator.
For writeup: Optimizing an IR using AI*
Useful opts: identify constant and identical parameters, remove them.
That's not necessarily always optimal... need to define when adding/
removing a parameter is equivalent.

Can AI be run backwards to get liveness? A custom analysis is simple
enough post-SCC of BBs.

Normalizing symbolic repr: ff(00n) is worth recognizing. Also ff*.

TODO make prettyCore with a verbosity flag for var types to visualize
opts. It'll still get pretty large... an incremental viewer as well?

Opt search: you can bound the depth on speculative choices
(e.g. copy SCC for specialization).
Proposal: refine top-level choices using est. cost using simple opt
of the result as a heuristic; pursue the most promising ones.
Need to avoid cycles or convergence: make a choice once.
The same applies to stack scheduling.
TODO make use of exec intensity vs code size.

What are the minimal opts I can start with?
Just SSA.
First opts: inlining, dead var elimination applied to the whole
function graph. $ret is dynamic, and may be assumed to consume all of
its arguments in general.
If a function is only called statically, its args may be changed; you
can delete the dead vars. Then the params passed at all call sites
must be changed.
If all calls to f have a given property (e.g. param x = param y), you
can use that while optimizing. Need a disjunction op for facts... is
it a lattice? param = symE is a fact.
In jumps which are calls (f,args,retf,args'...), retf,args' should be
considered a call site. There may be multiple such call frames in
a jump, cf f(g(h(x))).
A param at a jumpi call site is dead iff it's dead in both branches.
However, that doesn't mean the var is dead: a var is dead iff it has
no live uses. An op is dead iff all of its lhs vars are dead.

What does it mean for a fun to "only be statically called?".
Branches: terminating, jump, jumpi, JT jump (n1, n16).
A jump to e should only be considered able to jump to one of the fs
mentioned by e.

Param Core by ops type: first list, then var=>op map after SSA.
Opt: cache opt-relevant info, update it alongside Core changes.

First opt: dead var/op elimination applied to the whole function
graph.

How can Core be malformed? That should trigger an error.
Nonexistent instruction (not in opcodes or extended opcodes including
variadic or, xor etc).
Nonexistent label or slice out of range.
Contract size > 24k should fail in Stack/Asm.
FW: replace N5 with a multiplier based on max BB size:
jump (jt + k*tag). That would allow direct jumps, saving 12 gas and
4 code bytes per case.
You can increase k to get more space between the BBs, hopefully
increasing utilization of the gaps. If k is 2^n, you can save 2 gas by
shifting instead of multiplying.
If a DT has only one case and the target BBs fit within 256B, you
can use jump (jt + tag) instead, where tag takes Stack-dependent
values. That would require having tag labels in Structured...
Another alt is 16b tags, where each tag is a function pointer; then
it's just jump tag.

To fix: Core must contain the N1/N5 JTs map; it might as well also
contain a counter.
op=, _++, _-- implem --requires evalEP
Add divergent primfuns to Core.

The Maybe E in OPAssign is for the function... that works.
var x; x += 1 --fails with a type error
IC, the error is that I assumed the opfun type was Pair a b :-> c, but it's
actually Tu2 a b :-> c. Fixed.

The code will be extremely slow without inlining, given there are primfuns
(null, coerce...) everywhere and basic Prelude funs are defined in terms of
them.
Small functions should always be inlined... but how small is enough?
push2 ret, push args, push2 f --is at least 6 bytes
Simple heuristic: an op's size is n bytes. Inlining improves visibility and
enables new opts, so it should be favored.
I also need to prune dead locals between jumps to optimize away the implicit
null() in local declaration.
Dead var elim within BBs is trivial; doing so between functions requires
changing the lhs.
That can be done for f(xs) which depends only on x' by redefining it as
f(xs) = g xs', where g xs' has f's original body.

What should evalEP support? Just EPLocal and EPDeref;
Array (x,y,z) |= e makes sense but isn't worth it.

Stack needs a call graph + fallthrough decisions made for it before codegen.
Opt uses the same graph, so might as well generate it now.
Hack: every $ret var in a lhs is initially called $ret...
A problem with using Structured to infer which args are return addresses:
f : a -> b --can be coerced to (a, b -> c) -> c, (a,b->c,c->d)->d and so on.
That implies
f'(a,ret) := {
 //f's original body that sets b...
 return ret b
}
should be possible to simplify to f, discarding a suffix of the lhs!

Type info in Core is not guaranteed to be complete... so f value and behavior
must be observed.
as,ret,stk -eventually-> ret,bs,stk can be observed
Mode: perhaps or definitely.
If ret,bs,vs,stk -> ret',bs,vs',stk where ret',vs' don't depend on bs, you
can TCO: the continuation ret,vs,stk ~ ret',vs',stk.
The transformation can be more complex than just a rearrangement; TCO is
basically expedited inlining.
TCO can be applied to jumpis along as the suffix is a cont in both cases.
The simplest TCO case is return f(a): 
call (scope,ret,stk) = jump f(a,fret,scope,ret,stk)
fret (b,scope,ret,stk) = 
 jump (ret,b,stk)
TCO => call (scope,ret,stk) = jump f(a,ret,stk)
Observation: fret,scope,ret,stk ~ ret,stk

If a param ops(xs) is only demanded in one branch of an ifte, it can be deferred.
That requires passing xs, which is free when xs = {} or already passed.
That's particularly useful for function constants, which can then be deferred
until the BB where they're called.
Side-effecting exprs may also be split, as long as they're executed before
any consumers and executed at most once.

Invariant: words below $ret are passed unchanged to the cont.
That's how you can drop words from the lhs: if you never deconstruct
v:stk, it might as well be stk.

Multireturn:
f : a --> {b,c,d}
a -> b --is a special case: a --> {b}
Noreturn funs: a --> {}
f a := {...return1 a...return 2 b...}
f a {x => ...; y => ...; z => ...}, where the conts have locals in scope.
Effects can be expressed in terms of inSt -> outSt a la a Core function's
signature, with linear types.
Inductive linear types would let you express ownership of inductive
structures on the heap... but it would be nice to be able to store them on
the stack as well.
Keep linear types in state, use values on the stack to determine what's 
there, a la gt : (a : Word, b : Word) -> Either (b >= a) (a > b)
A "ptr to unique object" consists of both an on-stack value and state;
loading the unique object to the stack modifies the state, but not
necessarily the heap.
An object's invariants may be temporarily broken, but that requires consuming
the linear object type - to return it you must reestablish them.
Linear types for effects, dependent types for invariants...
to implement need to extend Core and then build on top of it.

The output of a while loop can be represented symbolically using
f_loop(xs). Estimate dataflow: x = f(vs), y = input y because it's unchanged
etc.

Simple local estimation of jump dests: push f => {f}, op xs => union, param
=> any.
coerce 42 :: a -> b then has fname set = {}... that should not be used to
conclude false, nor should (f * b) + (42 * iszero b) be assumed to be f when
inlining... it should just be used to approximate live functions & JTs.
Local estimation can later be extended to AI.
To preserve relations between vars, interpretation should transform stacks
rather than individual vars.
Oh, nice: if the state becomes bottom (impossible), the branch can be pruned.
The UB rules better be sound...

jump {jt} (args) has live vars <= union of live vars of each f in JT.
Rather than an explicit fixpoint, you can recurse:
setLive v => setLive its dependencies. That works for vars in SCCs as well.
First construct global CFG from trueMain, then compute.
CFG arrows are labeled by stack...
If f is called with S1 and S2, its arg for the purpose of opt is (S1 | S2).
The return to the cont is f(S1) | f(S2) which is <= f(S1 | S2).
Opt opportunities for f(S1) inform whether it's worth copying and
specializing.

Calls can perhaps be identified by lhs shorter than stack.
Program state: f,stk; state | exiting branch
stk[i] = k | f | {fs}
"Eventually jumps to" should be at the limit of what you can track...
f -> g in f -> g -> h is not interesting.
Jump arrow mode: perhaps (for dynamic f), definitely (for static).
A jump to k (labels mentioned: {}) is UB.
Refcount = number of possible sources; if a f/jt has refcount 0 it can be
pruned.

In the compilation of f(g()), push f is in a different BB from the call.
To inline it and avoid the early push, constant info must be propagated and
dead vars pruned from the stack.
Building the CFG requires iteration toward a fixpoint:
f(args) -> [end], end = exit | jump v?
Specialize with args = union of each possible call site.
That means if cond then f(1) else f(2) end (only call sites) should
specialize to jump to end.
It may be impossible to infer "ultimately exits or returns to ret" due to
jumps to coerced functions.
I can use the set of all labels pushed in any function + fs mentioned in
JTs to make an initial pruning of unreferenced functions.
*ptr = f --taints memory with f, now any read may contain f.

Is it safe to assume any call to f : A -> B, jump f,args,ret,scope will
eventually either exit or jump ret,<B>,scope?
Breaking that could be UB... I could add a hint field to Jump/Jumpi.
Every Core BB could be given the hint "exits or returns to $ret".
Nice, that should let me treat calls as temporary diversions in the CFG, with
each callee treated as a root of a DAG.
I can also assume vars below ret are unchanged in the continuation.

In while, the while body should fall through to the cond check while the
while start should jump to the check (or copy it). That can be achieved using
exec intensity est that gives loops a default exec intensity of k*inbound
intensity.
Invariant: excepting calls, outbound intensity should equal total inbound.

CFG and AI are inseparable...
Plan:
AI (k|f|{fs}, stack = [that]), build graph
FW: estimate exec intensity
Inline, TCO
CE and symbolic simplification
DCE: jumpi, jump (jt+k)
Replace uses of small constant params with constants
Compute live params, drop rest when possible
FW: memory opt
Start with stk = [symE], state = opaque vs

TODO fix: the else branch in Jumpi must ofc be SSA'd! Branch needs an ops
param then.
Also adding $trueMain.

*Estimating exec intensity********************************************
The algo will need to be changed once intensity and branch probability
annotations are added.
Problem: C functions are polymorphic, so they don't correspond to a
single instance. That could be resolved via optional type pattern
guards on annots, e.g. @intensity k if List a -> ().

Simple to start with: assume all branches have equal probability;
exec starts at the main entry point.
Algo: divide BBs into SCCs; for each SCC, compute the intensity effect 
of entry at each enterable BB in it. Effect: intensity for each BB in
the SCC + entry intensity for each out-edge to a lower SCC.
Equation: for each BB b, intensity[b] = sum p[i][b] * intensity[i] for 
each in-edge i.
ifte probability 0.5 implies while loop iteration count 1... too low.
A recursive function could get an infinite intensity, cf
f() := {
 if cond1
 then if cond2
      then return ()
      else ()
      end
 else ()
 end;
 f()
}
With 50% branch probability, that gives a 75% probability of
recursion, i.e. f is expected to call f 4 times.
That's fine... the real problem is
f() := if cond then {f();f();f()} else () end
Quick and dirty solution: compute intensity numerically, iterate over
each BB at most N times.
With inlining and TCO, there isn't a clear separation between C
functions in Core.

Consider HOFs: should I record expected number of calls for each
parameter, or just ignore params other than $ret?

With mutually recursive functions, an SCC could have back-edges to
multiple members. If you treat back-edges as out-edges and cap
intensity to prevent divergence... each entrypoint has a different
out-edge vector.
Really simple scheme: if an SCC is nontrivial, treat back-edges as
exits and just scale intensity by 4.
Note internal edges are not back-edges: SCCs form a tree with
out-edges, exits and back-edges as leaves.
Ex: A -> B or C; B,C -> D; D -> out or A. B and C will have half the
intensity of A.

Degenerate case: SCCs with no leaf nodes. Those can be replaced with
revertValue(). Reverting code paths can be assumed to have intensity
0, leading to an increase in the intensity of their siblings.
Problem: function calls. A C function either diverges, exits or
returns, so you can make a leaf vec for it as well.

*SSA******************************************************************
SSA can be done on a per-BB basis.
Map vars to version numbers (starting with 1). Transform vars without
changing the type: just concat !<n>. Post-SSA you don't need to care
about vno, so keeping the same type makes sense.
Map is slow for strings... a HashMap could make more sense, but that's
only worth applying once functionality is complete.
Mapping from Var to (Value,OpE) lets you see in which position the
var is.
The source of a var is either the function LHS or an op; if it's not
in the map then it's in the LHS.
Copies are eliminated.

When optimizing, I need to introduce new vars... I should really
pass the counter through the phases.

*Control flow*********************************************************

Inlining, tail call opt, DCE (jumpi and case)
Rewrites: BB copy, SCC copy
A straight-line C function can be inlined by inlining the Core
function, then the jump to the static return.

Inlining implem:
Given f statically calls g:
for each var bound by g's ops, alloc a new one of the same type.
Replace g's params with f's jump params (less ToS); also do so in the
op map. Union the updated op map with f's op map, then replace f's
branch with g's (with vars updated).

The then branch of a JUMPI costs 1 gas more due to the JUMPDEST; that favors
making the less likely branch the then branch.
However, if the else branch just falls through to another BB there'll be a
JUMPDEST either way.
The JUMPI price is paid whether you jump or not; making a commonly called BB
the then branch places less pressure on fallthroughs.
Inverting the cond lets you flip the branches; that can be done cheaply for
some expressions, e.g. x > k => <k+1> > x.

*Function params******************************************************
Dead function param elim, identical var elim
Note constant params can be inlined into blocks, rendering the param
dead. Idential params x, y can have y replaced with x with the same
effect.

*Sym eval*************************************************************
Constant propagation
Simplification: x+0,x*1,x-x,x+x...
Shift/slice interacts with mem ops.

*Memory opt***********************************************************
Divide pointers into p+k groups, where p is p+0 and k is in group 0.
Split the mem state when no-aliasing is guaranteed.
Problem: I can't pass the mem across BB boundaries without a stack of
states a la the word stack. With AI all the info can be
in the symbolic value of the single mem param.

Symbolic mem repr: ix:byte, m1 || m2. Recognize the minimum range of
read/write ops.
The initial mem is all zeroes... need a repr for slices and to ensure
repr is normalizing (makes AI easier).
To recognize non-aliasing ops and move them across BBs, need state
stack.

Target opts:
Parallelization
Read and write aggregation
 Simple approach: if a slice overlaps an already-loaded word, fetch it
 from there. Favor loading from p rather than p+k.
Redundant write elimination
Adjacent writes => copy
Remember scratch addr (used for sub-word writes), add 
*scratch = arbitrary ops to forget it?

Simplest read aggregation opt:
mload(p+k) byte b => mload p byte b'
You can also load sufficiently close slices in a single word p'' when
their original loads p, p' don't overlap.
Leftmost, rightmost and byte slices are ideal - 6 gas to extract.
It's also possible different derefs overlap, e.g.
v1 = *p
v2 = p->field

Aliasing info should be propagated across BBs via AI. It's
especially relevant for specialization.
State isn't just a list of separate symbolic vars, but relations
between them.
disjoint(p1,p2) could be stronger than just *p1, *p2 not overlapping:
any of their children could inherit non-aliasing.

*Stack****************************************************************

In later stack-scheduling phase: recall treegraph scheduling.
Tree scheduling is trivial, modulo opportunistic use of words ToS.
 That can be applied greedily; it modifies the remaining trees.
Trees can be partially executed to expose constants for sharing,
letting you recover sharing after splitting small constants to
eliminate false dependencies.

Complication: ops can consume resource vars, meaning any ops that 
borrow that var must be scheduled before them (if at all).
Resource splitting also helps eliminate false dependencies there.

All instructions push 0 or 1 words.
Most take <=2 arguments.
Min cost = sum of all op gas costs; the goal is then to minimize
stack shuffling.
Hack: mcopy(_,_,0) has no effect other than popping its arguments...
so if you know the third var from the top is zero and you want to pop
three, you can use mcopy instead of pop.

The simplest halfway decent scheme is the one from the old EVMC:
treegraph + topological order. It'll still be painfully slow with no
opts at all (and therefore no inlining of primfuns), but should still
produce runnable code.
Compute dependency relations:
lhs = op rhs depends on all instructions in lhs,
and if it consumes a var it also depends on all ops that borrow it.
An op may depend on multiple vars from the same op, e.g.
sload(call(...)), where call returns both $sto and its pushed word.

TODO apply PBT to bytecode generation, in particular to the stack
shuffling ops.
Dups of consumed vars produce garbage words on stack, e.g.
x-- => push 1, dup x, sub. Backtrack and aggressively GC if you try
to dup or swap out of range?
Opportunistically popping is only not worth it if you'll exit or
never return. However, accepting garbage on the stack requires
changing the callee or $ret's signature.

An opt choice monad: I want both search and heuristic plug-ins.
Search: v <- best scoreF vs --apply scoreF to the result, try each of
vs and pick the best one.
Need to limit depth as well...
The problem and choices should be defined separately from the solver.
v <- query QueryCon...
The problem of compiling a BB is: you have a given initial state
and target state (vs, state vars, tree of ops).
You can emit swaps, dups, pops and ops. You know the symE of each
var (so you can share constants).
Hacky opt: mcopy _,_,0 to pop 3.
The score need not be inherent to the compilation problem (which only
requires functional correctness).

Dramatic reordering may be optimal before running an op, e.g.
swaps to bring the stack to a state where you can just run the other
ops.
Implementing optimal greedy moves: if two words to be consumed from
the same commassoc fold op are ToS, apply the op.

Simple scheme:
First need to identify static jumps; one of each can be
pseudo-inlined. Since unique jumps to f should already be inlined
after opt, always place a jumpdest.
A jump is static if opmap[jumpvar] = (jumpvar, push f).
Prefer to pseudo-inline BBs where that is the only use of f, so you
can eliminate the push.
The initial state is (vars,state); the end state is (vars',state');
pseudo-inlining means you can't target a branch.
The actual goal is a symE, so you can share constants...
Consider () => (1,2,3); that should be compiled to optimal code.
Subject to treegraph constraints, prefer to push the ToS last?

Silly opt (perhaps Sol uses this?): since SUB takes args in the wrong
order (x-k being much more common than k-x), it's cheaper to do
i = 0-iters; while (i++)
than i = iters; while (i--)
while (i++) would benefit from deferring the push 1, add until the
body of the while loop since the result of i++ doesn't rely on it.
Recall: sharing -1 and adding it has the same effect.
1+ should be treated like an atomic op and run opportunistically.
If the effect of partial compilation (e.g. running subtrees) could
be computed before the subtrees themselves were optimized, you could
divide and conquer using a heuristic.
When you consume rather than dup+pop a last use, you save 6 gas.
Given a stack and tree + naive opportunistic consumption/pop, you can infer the cost...
Each tree has an optimal stack prefix ds consisting of its 
dependencies; the rest it pushes itself. If a suffix of ds ds' is ToS and last-use, you save 6*length ds'.
That does not consider swap+consume... you can push a constant for a
later tree and swap it with a var to consume for the same cost as a
dup!

Problem for tree search: different choices may have different numbers
of choices. Bounds on cost let you prune the search... A*?
LB >= total op cost, UB = naive compilation.
It might be worth precomputing and caching optimal solutions for
scheduling problems of size <= k per "shape" (op graph up to
isomorphism).
Isomorphism means: bijection between standalone and fold ops, var
names + isomorphic graph.

Searching backwards and forwards: if the end result is a single word
on stack = op(a,b), the state before that must be a,b?
Peephole opt lets you use higher-level planning to find the
approximate path, then tighten it...
A,suffix => B,suffix ~ A => B, where vars in suffix can be treated
like constants. That means every use should be a dup; ignore dup out
of bounds for now.
If memory is constant, mload k can be treated like a constant.
A tree with no dependencies in either direction and no push
(e.g. mstore(global,k)) can be run immediately.

I could detect dup/swap out of range and backtrack to GC more
aggressively.
Revert/return has a weaker stack constraint: just mem and the top two
values of the stack.

Enumerating topological sorts: 
enumerate g = do
 leaf <- leaves g
 add trees which only had leaf remaining as deps to leaves
 enumerate (delete leaf g)
Need to reread treegraphs paper. Key Q: is it optimal for EVM?
The EVM has instrs which take > 2 args and push 0 results, as well as
side effects.
The key limitation seems to be that I start with vars already on the
stack, whereas the paper does not. Perhaps also that the target is
multiple vars.

$trueMain () =
 let m = push main
     r = push ret
 jump m, r
ret () = stop
If main is only called from trueMain, main can be inlined; $ret
becomes a static value and jumps to it can be inlined to stop.
If main() can be inlined, the statically known stack can be
propagated through the skeleton with non-recursive control flow.
What can be done with that? Global lifting, constant sharing...
$trueMain and ret perhaps need to be added in Core.
The divergent C primfuns stop, revert, evm_return need to be defined
in Core. They all have dead $rets.
stop places no constraint whatsoever on the final stack, only ext,
sto and tsto need to be in the right state!

if cond then f(e1) else f(e2) end --where f is only called here
Then f has a static $ret; the else branch should just fall through to
f, which may fall through to end.

From old Stack.hs:
"We know nothing of the target layout we're aiming for, so we eval the 
root vars in arbitrary order."
That's thankfully no longer true, we can prefer to push the rightmost
var of the target layout first.
Op lhses are keys...

Dep graph algo:
source : Map Var LHS --param
--LHS can be used as op key
borrowers = {} : Map Var (Set LHS)
consumer = {} : Map Var LHS
depends = {} : Map LHS (Set LHS)
for (lhs,(op,args)) in ops:
 for v in args: lhs depends on source of v
 (_,svars) = args
 effs <- look up consume/borrow effect per svar arg in args for op 
 if |svars| /= |effs| the op is malformed
 for svar in svars:
  if borrowed: borrowers[svar] += lhs --default borrowers[svar] = {}
  else consumer[svar] = lhs --fails if two conflicting consumers
dependencies from borrowing have already been recorded
for (svar,lhs) in consumer:
 lhs depends on all lhses in borrowers[svar]
a depends on b => depends[a] += b
a depends on all bset => depends[a] union= bset
Compute dependency count per op at the same time.

Use depends + count to compute the treegraph.
Inter-tree dependency may be due to state vars rather than word vars.
State var deps are weak: if A->B->C and A-svar->C, you can prune the
state var dep. That could give you larger trees...
Why are they weak? Because they don't push a word to the stack that
must be saved; they only influence ordering, so indirect deps render
them redundant.
Ex:
c = mstore(k,v)
b = mload(k+16)
a = mload b
TODO check that's correct...

Scheduling trees with choice: in the worst case (all trees 
independent) there's a factorial number of combinations.
Simple #alts limiter: instead of depth, keep nchoices and divide it
by the number of current choices before recursing; if available
choices > nchoices just pick from the first nchoices.
Essentially, the number of possible cases explored is evenly divided
among the subproblems.
That overestimates alts explored if one has many more alternatives
than the rest, but avoids interaction between subproblems.

Simple heuristic: ignore unchanged suffix of stack.
Prefer to eval trees which produce BoS vars first.
Vars are either args or the result of ops; also either to be returned on the
stack or not. Those to be returned should always be dup'd.
You can compare start/current and target stack side-by-side.
There should be some cost heuristic (swap count?) which tells you when you
can make a move at zero cost vs optimum.
Iterate over current v target vars from BoS:
 while same: skip, inc match len, add var to in-place set
 if in in-place set: dup, inc match len
Early permutation: if garbage encountered, eval target and swap it in?
If top is garbage, pop it.
Runnable mstore(k1,k2) can be run for free; it can also be run for free if
it's mstore(k,x) where x is tos and last-use... or not last use.
General principle: 
1) runnable, consumes none, returns none => free
2) runnable, consumes ToS as last argument => free
Vars which are not last use (in-place or not) can be treated like unshared
constants when estimating cost of run.
Useful query: given ToS, which subtrees are free to run?
That should be useful for
mstore(p,a); mstore(p+32,b); ... recognizing independence would also be
valuable there.
Once a var has one remaining use, it's worth at least 3 gas to consume it.
If the stack is correct so far but x needs to be pushed where x = a runnable
tree, you can run it for free. That the stack is a suffix of the target
implies the tree will consume no vars.
That's useful for function calls: push $ret early, no need for swapping.
If the ToS is last-use and a runnable tree has it as its rightmost argument,
run it.

To track leaf trees:
leaves : set of leaves
pending : tree => #deps
rdeps : tree => trees that depend on it
When a tree is run, decrement #deps for each tree' in rdeps[tree]; if
it becomes 0, add tree' to the leaves set.
Trees have a root op, so they can be indexed by LHS.
FW issue: if I introduce a linear pointer consume, it might have an
empty LHS...
I just need a single branch-and-max action taking a list of choices;
it passes a lhs to each branch rather than an index to keep it O(n).
Making an action rather than a combinator means the interpreter has
minimal insight into the structure. The max must therefore run the
entire continuation, rather than evaluate cost locally. That's fine
for this problem, where the end state can be inspected to est cost
(cost = instr length) and there's no opportunity to divide and
conquer.
A choice applicative could expose parallel choices.
If ending up in the same state is common, one could add a memo
capability...
Q: Is there any point in exploring the tree of choices evenly? Once
you've found one solution, that sets an upper bound on min-cost of
nodes worth exploring.
My intuition suggests the early choices matter the most, which would
fit the simple search scheme.

--Functor
data Choices b = Choose [LHS] (LHS -> b)
With a GADT it could be [a].
H said free monads are expensive... but I'm not aiming for speed, and
they're probably not that expensive in this context.

If partial trees are to be executed out of order, need a DS for deleting one
op at a time from the relevant tree.
Tree characteristics: maybe var produced, external vars used.

Each BB becomes a list of asm instructions which may jump to another bb.
For now pick an arbitrary bb to fall through to.
Whether a BB statically jumps to f can be determined by looking at the
jump var's op DAG (it should simply be a push).
Invariant post-Opt: if f jumps to g, there should be >= 2 such fs.
Knowing and using the target layout complicates things when falling through..
the first var of the layout must be removed, so the fallthrough decision
must be made first.

*Misc*****************************************************************
LT/GT can be considered commutative: normalize to LT and emit either lt(x,y)
or gt(y,x) depending on which is cheaper.
Ditto for SLT/SGT
If you only care about truthiness, XOR ~ !=.
0 > _ ~ 0, x > 0 ~ x modulo truthy; x = 0 ~ iszero(x)
*copy(_,_,0);r ~ r, it does not consume.
mcopy(p,p,_) is also a noop

TODO systematically identify symmetries, identities and absorbents.
Perhaps hevm can help...

Rather than introducing a new revert_0_0 branch for revertValue(), one could
add an mem = emptyMemSlice();() op and pass that as the mem.
returnValue() can be replaced with stop.

*FW*******************************************************************
Reordering across BBs, e.g. lifting vars out of loops.

The function calling convention f,args,$ret,rest is just one
possibility. One could write lower-level code that dynamically pushed
and popped to the stack safely without pushing $ret addresses.
Ex safe repr:
n, <n words> ++ stk
That size-tagged array would have an existential Nat param n, like:
data Arr where Arr :: exists n . N n -> <n words> -> Arr
Dynamic ops on N provide you with type-level facts, e.g.
gt : (N a, N b) -> Either (b >= a) (a > b).
The facts are zero-sized, so the Either would have Bool repr.
Case on the either should expose the facts.
jumpi on N n gives you either n ~ Succ n' or n ~ 0.
Problem: how to express T ++ stk if stk is simply a hetlist?

data Arr stk = Arr (exists n . Cons (N n) (NWords n stk))
data NWords n stk where
 Z :: stk -> NWords n stk
 S :: Cons W (NWords n stk) -> NWords (Succ n) stk
User-level constructors could have repr = their argument, allowing
you to express syntaxes of words using them.
The length n becomes an existentially quantified type; any other
distinguishable constructors would need a similar explicit tag.